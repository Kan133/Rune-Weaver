# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the Dota2 V2 mainline has now both pushed `IntentGovernanceDecisions` into the downstream consumer chain, cleaned up `selection_pool` production authority, landed the first cross-feature planning/grant-seam split so unresolved cross-feature asks no longer die before Blueprint, closed the concrete propagation bug where a cross-feature bound reward updated `selection-pool.source.json` but failed to reflow into `data.weighted_pool / rule.selection_flow / ui.selection_modal` generator parameters, and aligned provider-side Dota2 ability identity so provider export only closes when Lua, KV, and export all agree on one authoritative runtime `abilityName`. The active blocker is no longer downstream governance drift from schema surface noise, it is no longer the real-host `selection_pool` inventory update miss where a `16-slot storage panel + block draws when full` request stopped at review artifacts instead of reaching source/runtime truth, it is no longer the `rw-test3` stale-pool bug where the bound reward never appeared because generated weighted-pool code still contained only the pre-bind six objects, and it is no longer the provider-side lie where `grant-seam` exported an `abilityName` that did not exist in KV/Lua. The preservation repair for unrelated local-only updates has now also been re-proved with a real `--write` on `rw-test3`, so the remaining slice is back to narrower evidence closure: rerun the resolved provider/consumer live proof on top of the stricter provider authority, keep tightening exploratory grounding quality where exact host symbols still drift, and decide which repeated exploratory outputs are mature enough to stabilize.

# Done since last update

- Landed a shared downstream governance consumer seam in [intent-governance-view.ts](/D:/Rune%20Weaver/core/wizard/intent-governance-view.ts) so blueprint/family/planning code can consume normalized governance contracts instead of re-reading raw `IntentSchema` surface detail.
- Switched governance-affecting blueprint consumers to the new seam:
  - [blueprint-semantic-shaping.ts](/D:/Rune%20Weaver/core/blueprint/blueprint-semantic-shaping.ts)
  - [seam-authority.ts](/D:/Rune%20Weaver/core/blueprint/seam-authority.ts)
  - [clarification-policy.ts](/D:/Rune%20Weaver/core/blueprint/clarification-policy.ts)
  - [module-planning.ts](/D:/Rune%20Weaver/core/blueprint/module-planning.ts)
  - [builder.ts](/D:/Rune%20Weaver/core/blueprint/builder.ts)
- Switched Dota2 `selection_pool` create admission and contract diagnostics to governance-decision authority in [authoring.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/authoring.ts), including a small negation fix so explicit `no UI` wording no longer falsely satisfies the UI atom.
- Split `selection_pool` family production authority out of the old mixed [authoring.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/authoring.ts) file into focused internal layers:
  - [admission.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/admission.ts)
  - [seeding.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/seeding.ts)
  - [update-merge.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/update-merge.ts)
  - [materialization.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/materialization.ts)
  - [shared.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/shared.ts)
- Removed old example authority from the production family path:
  - deleted the old production `examples.ts` file
  - moved demo/example seeds to [__fixtures__/examples.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/__fixtures__/examples.ts)
  - stopped exporting example fixtures from [index.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/index.ts)
- Rebuilt source-backed bounded update merge so inventory/object-count updates now come from current source truth + `UpdateIntent` delta + bounded scalar extraction rather than example replay/defaults.
- Re-proved the real failing host case on `D:\\rw-test1`:
  - reran `dota2 update "给当前功能增加一个存储面板，16格，存满了就不能再抽了" --host D:\\rw-test1 --feature standalone_system_75dh --write`
  - confirmed [selection-pool.source.json](/D:/rw-test1/game/scripts/src/rune_weaver/features/standalone_system_75dh/selection-pool.source.json) now contains `inventory.enabled=true`, `capacity=16`, and `blockDrawWhenFull=true`
  - confirmed generated UI/runtime now consume that contract instead of hardcoding `inventoryEnabled = false`
- Updated downstream tests to match the new authority boundary instead of the old schema-surface assumptions:
  - [builder.test.ts](/D:/Rune%20Weaver/core/blueprint/builder.test.ts)
  - [planning.test.ts](/D:/Rune%20Weaver/apps/cli/dota2/planning.test.ts)
  - [resolver.test.ts](/D:/Rune%20Weaver/core/patterns/resolver.test.ts)
  - [authoring.test.ts](/D:/Rune%20Weaver/adapters/dota2/families/selection-pool/authoring.test.ts)
- Re-proved the important drift cases after the consumer audit:
  - `天赋 / 祝福 / 奖励` word-swap prompts now keep the same `selection_pool` admission verdict and the same satisfied/missing contract atoms
  - the corresponding Dota2 blueprint dry-run path now keeps the same `status=ready`, `admission=admitted_explicit`, `moduleNeedsCount=0`, and the same four templated module roles across that cluster
  - a non-selection `no UI` fireball prompt still stays outside `selection_pool` and continues to plan as one weak gameplay backbone
- Landed the first staged cross-feature planning/gating split:
  - `clarify-cross-feature-target` is now `write-blocking-unresolved-dependency`, not a pre-Blueprint hard stop
  - the Dota2 planning/update CLI now stops only on `blocksBlueprint`, and unresolved target identity is carried forward to write authority instead of killing planning authority
  - unresolved cross-feature weighted-selection prompts can now continue through Blueprint as a weak local `selection_pool` shell instead of collapsing into an early block
- Landed the narrow Dota2 grant seam pieces:
  - [grant-seam.ts](/D:/Rune%20Weaver/adapters/dota2/cross-feature/grant-seam.ts) now owns provider export sidecars, consumer binding sidecars, and write-blocking dependency checks
  - [selection-flow.ts](/D:/Rune%20Weaver/adapters/dota2/generator/server/selection-flow.ts) now exposes a generic outcome hook so cross-feature reward application no longer pollutes `selection_pool`
  - [bridge/index.ts](/D:/Rune%20Weaver/adapters/dota2/bridge/index.ts) now respects `grant_only` vs `auto_on_activate` attachment mode
- Closed the real cross-feature local-shell propagation miss on `rw-test3`:
  - added a `selection_pool` lifecycle/materialization seam that refreshes existing write-plan pattern entries from source-backed authoring instead of letting `grant-seam` hand-edit only the source artifact
  - `grant-seam` now updates the local shell by calling that family seam, so refreshed `selection-pool.source.json` truth is recompiled back into `input.key_binding`, `data.weighted_pool`, `rule.selection_flow`, and `ui.selection_modal` parameters before generation
  - regression coverage now proves both append and replacement binding paths refresh generator-facing parameters, and the weighted-pool generator now emits the appended bound object from the refreshed entry set
  - reran a real `dota2 update --write` on `D:\\rw-test3` with the existing `SEL_R003 -> micro_feature_q74b` binding, and confirmed [standalone_system_sp3i_weighted_pool_data_weighted_pool.ts](/D:/rw-test3/game/scripts/src/rune_weaver/generated/shared/standalone_system_sp3i_weighted_pool_data_weighted_pool.ts) now includes `SEL_R003` while [selection-grant-bindings.json](/D:/rw-test3/game/scripts/src/rune_weaver/features/standalone_system_sp3i/selection-grant-bindings.json) remains intact
- Closed two false-clarification / false-trigger drift points that were blocking provider-style prompts:
  - runtime-long-lived ability shells are no longer treated as storage persistence just because `timing.duration.kind = persistent`
  - `grant-feature` alone no longer implies a cross-feature target clarification; the clarification now depends on real cross-feature structure or explicit prompt signal
  - system/event grant activations no longer promote `normalizedMechanics.trigger`, and blueprint planning now skips `input.key_binding` whenever no concrete trigger key is present
- Aligned the Wizard runtime-vs-external persistence boundary end to end:
  - `wizard.create` and `wizard.update` prompt packages now explicitly tell the model that bare `persistent` defaults to runtime/session-long existence unless the prompt names cross-match retention, save/profile storage, external storage, or an external owner boundary
  - prompt constraints now keep the external-storage gap only for real cross-match or external-storage asks instead of generic runtime-long-lived wording
  - prompt hints/raw facts now split `runtime_persistence` from `external_persistence`, and fallback no longer upgrades generic long-lived runtime asks into `external-system writes`
  - clarification authority now triggers persistence questions only for external persistence/storage governance rather than for every runtime-long-lived shell
- Re-ran live CLI evidence on `D:\\rw-test1`:
  - unresolved cross-feature weighted draw prompt now reaches `selection_pool` family reuse (`admitted_explicit`, 4 family modules, no pre-Blueprint stop) and is only blocked later by the real F4 integration conflict already present on the host
  - a grant-only provider prompt can now reach a full exploratory dry-run write review without false persistence clarification and without the broken empty-key `input.key_binding` path in the stable sample
- Closed the real provider identity drift that caused the in-game `AddAbility(...)` failure after the bound reward finally appeared:
  - added [provider-ability-identity.ts](/D:/Rune%20Weaver/adapters/dota2/provider-ability-identity.ts) as the single helper for authoritative provider runtime ability resolution, Lua/KV identity parsing, and content-level identity validation
  - [grant-seam.ts](/D:/Rune%20Weaver/adapters/dota2/cross-feature/grant-seam.ts) no longer guesses `abilityName` from write-plan paths; it now exports `grantable_primary_hero_ability` only when the provider write plan closes exactly one authoritative Lua+KV binding
  - [synthesis/index.ts](/D:/Rune%20Weaver/adapters/dota2/synthesis/index.ts) now sanitizes explicit `abilityName` hints once, propagates that runtime symbol into both synthesized Lua and KV artifacts, and rejects LLM candidates that drift away from the authoritative symbol instead of writing mismatched synthesized content
  - [post-generation-validator.ts](/D:/Rune%20Weaver/adapters/dota2/validator/post-generation-validator.ts) now performs an honest provider-export check: exported `abilityName` must exist in `npc_abilities_custom.txt`, its `ScriptFile` must resolve, and the Lua runtime symbol must match the export
  - new regression coverage now proves:
    - provider export is withheld when Lua/KV identity is missing or drifted
    - synthesized explicit ability names are sanitized once and reused across Lua/KV
    - bad provider exports are rejected by validator instead of slipping through to runtime
- Reworked the Dota2 `ui.selection_modal` card tray layout so it no longer relies on one wide `right-wrap` content rail:
  - [selection-modal.ts](/D:/Rune%20Weaver/adapters/dota2/generator/ui/selection-modal.ts) now groups live options into centered dynamic rows instead of mapping all cards straight into a single wrapping container
  - [less-styles.ts](/D:/Rune%20Weaver/adapters/dota2/generator/ui/less-styles.ts) now styles a dedicated `modal-options` / `modal-options-row` stack so 3 options stay centered in one row while 5 options can expand into centered multi-row layout
  - refreshed the live host output on `D:\\rw-test3` so [standalone_system_sp3i_selection_modal_ui_selection_modal.tsx](/D:/rw-test3/content/panorama/src/rune_weaver/generated/ui/standalone_system_sp3i_selection_modal_ui_selection_modal.tsx) and [standalone_system_sp3i_selection_modal_ui_selection_modal.less](/D:/rw-test3/content/panorama/src/rune_weaver/generated/ui/standalone_system_sp3i_selection_modal_ui_selection_modal.less) now match the new generator output
- Re-proved a stacked update flow on the real cross-feature host `D:\\rw-test3`:
  - first real update `将选项数量从3提升到5` correctly moved [selection-pool.source.json](/D:/rw-test3/game/scripts/src/rune_weaver/features/standalone_system_sp3i/selection-pool.source.json) to `choiceCount=5` and `display.minDisplayCount=5`
  - that same unrelated update also exposed a new preservation bug: because the prompt did not carry cross-feature semantics, the update path deleted [selection-grant-bindings.json](/D:/rw-test3/game/scripts/src/rune_weaver/features/standalone_system_sp3i/selection-grant-bindings.json), cleared the bridge-side selection grant plan, and collapsed the workspace dependency/export truth for the existing `micro_feature_q74b` reward binding
  - a second update with explicit preservation wording for the existing `micro_feature_q74b` reward binding successfully restored the cross-feature seam while also adding the requested 16-slot inventory contract
  - final live host truth on `D:\\rw-test3` is now:
    - `choiceCount=5`
    - `inventory.enabled=true`
    - `inventory.capacity=16`
    - `inventory.blockDrawWhenFull=true`
    - restored selection grant binding on `SEL_R004 -> micro_feature_q74b:grantable_primary_hero_ability`
  - rebuilt the host after the two updates with `yarn prod:panorama` and `yarn prod:vscripts`
- Closed the cross-feature consumer update-preservation hole that the stacked `rw-test3` proof exposed:
  - [grant-seam.ts](/D:/Rune%20Weaver/adapters/dota2/cross-feature/grant-seam.ts) now preserves existing `selection-grant-bindings.json` truth, dependency edges, and feature-contract state during local-only source-backed updates unless the update actually carries cross-feature mutation authority
  - preservation lives in the Dota2 grant seam itself rather than in CLI-side delete exceptions, so unrelated updates now keep the sidecar in the write plan and no longer fall into `safe-delete`
  - new regression coverage proves that a local-only update preserves the existing binding sidecar and keeps diff classification from deleting it
  - a real `--write` on `D:\\rw-test3` using a pure UI-title change now refreshes [selection-grant-bindings.json](/D:/rw-test3/game/scripts/src/rune_weaver/features/standalone_system_sp3i/selection-grant-bindings.json) instead of deleting it, without restating cross-feature semantics in the prompt
  - that real write also preserved the workspace-side dependency/export truth for `micro_feature_q74b`, so the local-only update path no longer strips the consumer's cross-feature seam while modifying unrelated selection UI detail

# Next intended moves

1. Rerun the real resolved provider/consumer proof on top of the stricter provider identity seam:
   create or reuse a clean provider -> bind it into a local draw shell -> verify the bound option appears -> then verify hero grant behavior and dedupe semantics.
2. Stabilize the provider create prompt path enough that repeated runs stop drifting between the exploratory grant-only shell and spurious blocked interpretations.
3. Keep reducing exploratory grounding warnings by tightening exact-symbol backing on exploratory cases rather than reopening prompt heuristics.
4. After that evidence pass, decide which repeatedly successful exploratory outputs are ready to graduate into stabilized reusable assets.

# Current blockers / risks

- The primary blocker is no longer the old downstream governance drift where blueprint/family admission could change because of schema-surface prose or labels.
- The primary blocker is also no longer the `selection_pool` source-backed update miss where inventory/storage-panel requests failed to reach source/runtime truth on a real host.
- The main blocker remains:
  - provider/consumer revalidation still needs a fresh resolved-target proof pass on the updated planning/admission surface, but the proof is now blocked by runtime confirmation rather than by local-shell generation drift
  - exploratory grounding still needs harder exact-symbol evidence on non-family asks
  - exploratory outputs are still `requiresReview=true` until repeated evidence justifies stabilization
- New concrete blocker truth after this round:
  - unresolved cross-feature planning is now materially healthier, and provider export authority is now honest, but grant-only provider creation still shows LLM sample drift between runs; one sample can reach full exploratory dry-run review, while other samples can still regress into governance-blocked interpretations
  - because of that drift, and because the stricter provider export now refuses mismatched Lua/KV identity instead of papering over it, the end-to-end resolved provider/consumer proof still needs one clean rerun before it is honest to mark closed
- Residual implementation risk:
  - this round audited the Dota2 mainline consumers that affect blueprint status, family ratification, and planning verdicts, but it was intentionally not a whole-repo raw-schema purge
  - `selection_pool` authority is now cleaner and no longer example-driven in production, but some internal family files are still larger than ideal and may deserve another cleanup pass after mainline evidence is refreshed
  - documentation for wizard / intent-schema / blueprint consumption is now lagging the shipped seam and should be refreshed in a later docs round

# Notes for other sessions

- New downstream seam to use when a consumer needs governance truth:
  - [intent-governance-view.ts](/D:/Rune%20Weaver/core/wizard/intent-governance-view.ts)
- Current authority rule after this round:
  - if a branch changes `blocked / weak / ready`, family admission, module/backbone planning, or reusable-fit judgment, it should consume `IntentGovernanceDecisions`, not raw schema prose
  - raw `IntentSchema` fields remain fine for display text, seed/proposal filling, and bounded parameter extraction
- Current `selection_pool` rule after this round:
  - production family code no longer depends on `talent_draw_demo`, `reward_draw_demo`, `15-slot` example defaults, or `"Talent inventory full"` as write authority
  - example/demo prompts now live in fixture-only code and should not be reintroduced into production `selection_pool` paths
- Validation completed this round:
  - `npm run check-types`
  - `npx tsx adapters/dota2/families/selection-pool/authoring.test.ts`
  - `npx tsx adapters/dota2/families/selection-pool/lifecycle.test.ts`
  - `npx tsx core/blueprint/builder.test.ts`
  - `npx tsx apps/cli/dota2/planning.test.ts`
  - real-host write verification on `D:\\rw-test1` for the 16-slot inventory update
- Additional validation completed this round:
  - `npx tsx adapters/dota2/cross-feature/grant-seam.test.ts`
  - `npx tsx adapters/dota2/generator/server/weighted-pool.test.ts`
  - `npx tsx adapters/dota2/bridge/bridge-wiring.test.ts`
  - `npx tsx adapters/dota2/generator/server/selection-flow.test.ts`
  - `npx tsx core/llm/prompt-constraints.test.ts`
  - `npx tsx core/wizard/intent-schema.test.ts`
  - `npx tsx core/wizard/intent-schema-layers.test.ts`
  - `npx tsx core/wizard/update-intent.test.ts`
  - `npm run check-types`
  - live CLI dry-run and write verification on `D:\\rw-test3` for the repaired cross-feature replacement/binding propagation path
  - live CLI dry-runs on `D:\\rw-test1` for:
    - unresolved cross-feature weighted draw planning
    - grant-only provider exploratory create planning
- Validation completed for the provider identity alignment round:
  - `npm run check-types`
  - `npx tsx adapters/dota2/cross-feature/grant-seam.test.ts`
  - `npx tsx adapters/dota2/synthesis/index.test.ts`
  - `npx tsx adapters/dota2/validator/post-generation-validator.test.ts`
  - `npx tsx adapters/dota2/bridge/bridge-wiring.test.ts`
- Validation completed for the centered `selection_modal` layout refresh:
  - `npx tsx adapters/dota2/generator/ui/selection-modal.test.ts`
  - `npm run check-types`
  - `yarn prod:panorama` on `D:\\rw-test3`
- Validation completed for the stacked cross-feature update proof on `D:\\rw-test3`:
  - `npx tsx apps/cli/index.ts dota2 update '将选项数量从3提升到5' --host D:\\rw-test3 --feature standalone_system_sp3i --write`
  - `npx tsx apps/cli/index.ts dota2 update '在保留当前绑定到 micro_feature_q74b 的奖励不变的前提下，添加一个面板，抽取到的东西会加入到面板中去，总共16格，抽满就不能抽了。' --host D:\\rw-test3 --feature standalone_system_sp3i --write`
  - `yarn prod:panorama` on `D:\\rw-test3`
  - `yarn prod:vscripts` on `D:\\rw-test3`
- Validation completed for the preservation-fix round:
  - `npx tsx adapters/dota2/cross-feature/grant-seam.test.ts`
  - `npm run check-types`
  - `npx tsx apps/cli/index.ts dota2 update '把选择界面的标题改为Choose Your Rewards，不改变现有玩法。' --host D:\\rw-test3 --feature standalone_system_sp3i --write`
  - verified post-write host truth:
    - [selection-grant-bindings.json](/D:/rw-test3/game/scripts/src/rune_weaver/features/standalone_system_sp3i/selection-grant-bindings.json) still contains `SEL_R004 -> micro_feature_q74b:grantable_primary_hero_ability`
    - [rune-weaver.workspace.json](/D:/rw-test3/game/scripts/src/rune_weaver/rune-weaver.workspace.json) still keeps the `grants -> micro_feature_q74b -> grantable_primary_hero_ability` dependency edge
