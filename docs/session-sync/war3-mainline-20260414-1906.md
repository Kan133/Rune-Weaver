# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update did not broaden the implementation path. Instead, it anchored the upstream reference base for the War3 lane so future skeleton and host-contract tightening can proceed from a stable canonical stack.

# Done since last update

- Added a read-only upstream mirror area:
  - `references/war3/github/`

- Mirrored these upstream repositories into that area:
  - `w3ts`
  - `wc3-ts-template`
  - `warcraft-vscode`
  - `warcraft-template-classic`
  - `lib-stdlib`
  - `war3map`

- Added:
  - `references/war3/github/README.md`
  - `knowledge/warcraft3/github-upstream-survey.md`
  - `knowledge/warcraft3/skeleton-diff-checklist.md`

- Updated:
  - `knowledge/warcraft3/README.md`
  - `docs/hosts/war3/WAR3-KK-TSTL-PROVISIONAL-HOST-WORKSPACE-CONTRACT.md`
  - `docs/hosts/war3/WAR3-TSTL-SKELETON.md`

- Landed the current canonical upstream stack for the War3 lane:
  - TSTL authoring reference:
    - `wc3-ts-template`
    - `w3ts`
  - Classic Lua host/workflow reference:
    - `warcraft-template-classic`
    - `warcraft-vscode`
    - `lib-stdlib`
  - parser/reference only:
    - `war3map`

- Explicitly recorded that Python authoring paths such as `PyWC3` are not the War3 mainline baseline.

- Anchored three important judgments in the docs:
  - authoring mainline is `TypeScript -> TypeScriptToLua -> Lua output`
  - Classic Lua workflow evidence is useful, but separate from the TSTL authoring seam
  - these upstream repos provide structure/workflow evidence, not KK 1.29 runtime proof

# Next intended moves

- Primary next move:
  - tighten `tmp/war3-tstl-skeleton` against the canonical upstream stack rather than continuing broad reference gathering

- The first concrete skeleton tightening items are now explicit:
  - add real build/dev/test seams
  - add a definitions-generation placeholder
  - decide whether RW should keep `maps/demo.w3x` or move toward upstream naming (`maps/map.w3x` / `map.w3x`)
  - document `warcraft.json` compatibility or deliberate non-adoption

- Secondary next move:
  - reflect only the necessary host/workflow facts into validator / intake / handoff code after the skeleton/contract alignment is decided

# Current blockers / risks

Primary blocker:

- RW now has a better upstream evidence base, but `tmp/war3-tstl-skeleton` is still a lightweight placeholder and has not yet been tightened against the chosen canonical stack.

Secondary blockers / risks:

- The upstream stack improves planning confidence, but still does not prove KK 1.29 runtime behavior.
- `warcraft-vscode` and `warcraft-template-classic` are Classic Lua workflow references, not direct TSTL templates.
- `wc3-ts-template` is the best TSTL authoring reference, but it is still structure evidence rather than version proof for KK.

# Notes for other sessions

- Do not resume broad ecosystem search as the default next move; the current reference set is enough for the next implementation slice.
- The active seam is now:
  - compare the current RW skeleton against the canonical upstream stack
  - then tighten the skeleton and host contract deliberately
- The most important new knowledge files are:
  - `knowledge/warcraft3/github-upstream-survey.md`
  - `knowledge/warcraft3/skeleton-diff-checklist.md`
- The mirrored upstream evidence now lives under:
  - `references/war3/github/`
