# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: Step 2 can now honest-close after the bounded `builder.ts` authority-closure refactor moved seam authority, clarification policing, shared lexical probing, module semantic shaping, and blueprint status policy out of the builder orchestration path and revalidated the current grammar-v1 core. The active Dota2 work is now to refresh the package-6 acceptance matrix against that authority-closed core: re-prove one supported pass path, one honest-block path, and one seam-gap probe without adding new product-code routing or reopening closed family slices by default.

# Done since last update

- Landed the bounded `reward/progression` admission:
  - `core/blueprint/builder.ts` now admits only the selection-confirmed same-feature session-local threshold progression slice
  - progression truth is lifted onto `rule.selection_flow` as `progression.selection.local_threshold` plus round-counter / level-state expectations
  - `adapters/dota2/patterns/index.ts` and `adapters/dota2/generator/server/selection-flow.ts` now carry the same narrow progression contract and runtime update path
- Landed the bounded `spawn/emission` admission:
  - `core/blueprint/builder.ts` now admits only the no-target forward-linear-projectile slice and emits `emission.projectile.linear.forward`
  - `adapters/dota2/patterns/index.ts` now declares `dota2.linear_projectile_emit`
  - `adapters/dota2/assembler/index.ts` and `adapters/dota2/generator/index.ts` now thread projectile speed / distance / radius into the existing lua+kv ability route without adding a scheduler bus or generic composition layer
- Closed the standalone `entity/session state` hidden blocker:
  - standalone state no longer collapses into `data.weighted_pool` / `state.session.snapshot`
  - grammar-v1 now admits only pattern-owned session-local state inside `data.weighted_pool` / `rule.selection_flow`
  - standalone session store / shared state bus remain explicit honest-blocks
- Closed the Step 2 package-level authority gap:
  - `core/blueprint/builder.ts` is now orchestration-first instead of carrying the primary grammar-v1 rulebook inline
  - seam authority, clarification policy, shared lexical probing, blueprint semantic shaping, and blueprint status policy now live in dedicated helper/policy modules
  - package 2 can now be judged landed enough for current v1 instead of still blocked on builder-embedded heuristic authority
- Refreshed package-6 and targeted seam tests:
  - `core/blueprint/builder.test.ts`
  - `core/blueprint/package6-current-seam.test.ts`
  - `core/patterns/resolver.test.ts`
  - `adapters/dota2/patterns/index.test.ts`
  - `adapters/dota2/generator/server/selection-flow.test.ts`
  - `adapters/dota2/assembler/linear-projectile-emit.test.ts`
  - `adapters/dota2/assembler/short-time-buff-cooldown.test.ts`
- Revalidated the new seam truth with:
  - `npx tsx core/blueprint/clarification-policy.test.ts`
  - `npx tsx core/blueprint/seam-authority.test.ts`
  - `npx tsx core/blueprint/builder.test.ts`
  - `npx tsx core/blueprint/package6-current-seam.test.ts`
  - `npx tsx core/patterns/resolver.test.ts`
  - `npx tsx adapters/dota2/patterns/index.test.ts`
  - `npx tsx adapters/dota2/generator/server/selection-flow.test.ts`
  - `npx tsx adapters/dota2/assembler/linear-projectile-emit.test.ts`
  - `npx tsx adapters/dota2/assembler/short-time-buff-cooldown.test.ts`

# Next intended moves

1. Refresh the package-6 acceptance matrix on the authority-closed core with one supported pass path, one honest-block path, and one seam-gap probe.
2. Keep package 4 at the new grammar-v1 read unless new code truth disproves the admitted reward/spawn paths or the explicit standalone-state boundary.
3. Freeze `scheduler/timer`, `reward/progression`, `spawn/emission`, and standalone-state boundary truth unless new code truth proves a same-size truthful extension.

# Current blockers / risks

- Primary blocker: fresh post-refactor acceptance evidence is not yet re-established on the authority-closed core, so Dota2 cannot yet claim that the generalized path stays honest end-to-end under validation without case-specific compensation.
- Package-level read after the authority closure:
  - package 2 can now be judged `landed enough for current v1`
  - package 4 remains `landed enough for current v1`
  - the next honest pressure is package-6 evidence refresh, not another Step 2 seam or builder authority pass
- Mainline risk: compensating for the need for fresh Step 6 evidence with docs-only confidence, stale pre-refactor matrix reads, or case-specific product code instead of revalidating the generalized path.

# Notes for other sessions

- Seam status is now:
  - `scheduler/timer` Phase A: stage-closed
  - `reward/progression`: stage-closed
  - `spawn/emission`: stage-closed
  - standalone `entity/session state` boundary pass: stage-closed
- Step status is now:
  - Step 2: honest-closed for current v1
  - active Dota2 mainline step: Step 6
- Package-6 baseline now includes:
  - admitted `timing.cooldown.local`
  - admitted selection-local progression
  - admitted forward-linear-projectile emission
  - honest-block on standalone shared state
- Do not reopen frozen seams or redirect Dota2 back into another Step 2 seam-expansion or authority-closure pass by default.
- The next Dota2 evidence pass should validate the authority-closed core rather than re-argue whether builder still owns the grammar-v1 rulebook.
