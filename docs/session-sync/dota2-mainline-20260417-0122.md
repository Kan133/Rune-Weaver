# Current focus

Active mainline step: 2. Blueprint / pattern selection

Current slice: the bounded Phase A `scheduler/timer` seam-expansion package has now stage-closed for the current grammar-v1 boundary. The next active Step 2 work is to open the bounded `reward/progression` seam-expansion package deliberately, not to reopen frozen-seam package-2/package-4 work, rerun package-6 preflight, or broaden `scheduler/timer` past the newly admitted local-cooldown slice.

# Done since last update

- Landed the bounded Phase A `scheduler/timer` admission in `core/blueprint/builder.ts`:
  - `FinalBlueprint` no longer honest-blocks the entire timer family indiscriminately
  - one narrow admitted slice now exists only when the request is a timed self-apply effect with same-effect local cooldown
  - the effect `ModuleNeed` now emits `timing.cooldown.local`
  - `initialDelaySeconds`, `delaySeconds`, `tickSeconds`, `intervalSeconds`, delayed selection resolution, post-selection scheduling, and cross-module scheduler asks still honest-block
- Declared the same narrow timer truth in `adapters/dota2/patterns/index.ts`:
  - `dota2.short_time_buff` now advertises `timing.cooldown.local`
  - the catalog now explicitly says delay / periodic / post-selection scheduler orchestration remains deferred
- Landed the minimum truthful write-path support in `adapters/dota2/assembler/index.ts`:
  - `dota2.short_time_buff` KV entries now accept effect-local cooldown metadata
  - effect-module/binding parameters override global ability parameters
  - no standalone scheduler module, timer bus, or TS fallback stub was introduced
- Refreshed targeted tests for the Phase A buckets:
  - `core/blueprint/builder.test.ts`
  - `core/blueprint/package6-current-seam.test.ts`
  - `core/patterns/resolver.test.ts`
  - `adapters/dota2/patterns/index.test.ts`
  - `adapters/dota2/assembler/short-time-buff-cooldown.test.ts`
- Revalidated the bounded package with:
  - `npx tsx core/blueprint/builder.test.ts`
  - `npx tsx core/blueprint/package6-current-seam.test.ts`
  - `npx tsx core/patterns/resolver.test.ts`
  - `npx tsx adapters/dota2/patterns/index.test.ts`
  - `npx tsx adapters/dota2/assembler/short-time-buff-cooldown.test.ts`
  - `npx tsx adapters/dota2/assembler/modifier-applier-honesty.test.ts`

# Next intended moves

1. Open the bounded `reward/progression` seam-expansion package as the next active Step 2 slice.
2. Keep the new `scheduler/timer` boundary frozen unless new code truth proves a same-size truthful extension.
3. Reuse the refreshed pass/block/gap baseline so reward/progression work does not reintroduce false-ready collapse.

# Current blockers / risks

- Primary blocker: Step 2 is still incomplete overall because `reward/progression`, `spawn/emission`, and broad standalone `entity/session state` remain open seams even though the bounded Phase A timer slice is now stage-closed.
- Current scheduler/timer remaining boundary:
  - `initialDelaySeconds`
  - `delaySeconds`
  - `tickSeconds` / `intervalSeconds`
  - delayed selection resolution
  - post-selection scheduled execution
  - reward/spawn-coupled scheduling
  - anything that would require a standalone scheduler module or grammar-v1 reopen
- Mainline risk: compensating for the remaining seam gaps with a new case, docs-only claims, or case-specific product code instead of landing truthful family support.

# Notes for other sessions

- `scheduler/timer` is no longer the default active next seam.
- Current next-seam ranking is now:
  - `reward/progression`
  - `spawn/emission`
  - broad standalone `entity/session state`
- Do not describe the scheduler family as broadly admitted.
- The only admitted scheduler/timer path today is:
  - timed self-targeted apply-only effect
  - same-effect local cooldown
  - `timing.cooldown.local`
  - `dota2.short_time_buff` on the existing lua+kv route
- Package 2 and package 4 still remain incomplete overall; this is a bounded seam close, not a declaration that Step 2 is done.
