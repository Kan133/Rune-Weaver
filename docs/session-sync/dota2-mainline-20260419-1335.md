# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: prove that Dota2 V2 no longer treats grammar/pattern admission as the feature boundary, then turn that into honest initialized-host evidence. This session removed another layer of grammar-biased control-plane language, fixed a real negative-constraint leak that was still injecting `ui.selection_modal` into no-UI asks, and re-proved an initialized-host exploratory write on `D:\test3`.

# Done since last update

- Reduced remaining grammar-era bias across planning/resolver/update wording:
  - `core/blueprint/seam-authority.ts`
  - `core/blueprint/blueprint-semantic-shaping.ts`
  - `core/patterns/resolver.ts`
  - `core/wizard/update-intent.ts`
  - `core/llm/prompt-packages.ts`
  - `core/llm/prompt-constraints.ts`
  - `core/retrieval/dota2-bundles.ts`
  - `core/schema/types.ts`
  - `core/blueprint/blueprint-status-policy.ts`
  - `core/blueprint/builder.ts`
- Shifted the semantics from old `admitted/current seam` wording toward:
  - governance-blocked only for real ownership/safety/host impossibility
  - safe unknown mechanics continue as `weak` plus synthesis-required
  - update preservation now prefers persisted module truth over old pattern-only hints
- Fixed a real product-facing bug discovered on initialized-host proof:
  - Wizard emitted negative constraints as English sentences like `The feature must not add UI.`
  - builder only filtered `Do not / no / without ...`, so the negative line was being mistaken for a positive UI requirement
  - builder now recognizes `must not / should not / feature must not ...` negative constraints, preventing accidental UI module injection
- Updated the affected tests to match V2 truth instead of old grammar-era assumptions:
  - `core/blueprint/builder.test.ts`
  - `core/blueprint/package6-current-seam.test.ts`
  - `core/blueprint/seam-authority.test.ts`
  - `core/patterns/resolver.test.ts`
  - `core/wizard/update-intent.test.ts`
  - `apps/cli/dota2/planning.test.ts`
- Validation that passed this round:
  - `npm run check-types`
  - `npx tsx -e "import('./core/blueprint/builder.test.ts').then((m) => m.runTests())"`
  - `npx tsx -e "import('./core/blueprint/package6-current-seam.test.ts').then((m) => m.runTests())"`
  - `npx tsx core/blueprint/seam-authority.test.ts`
  - `npx tsx core/patterns/resolver.test.ts`
  - `npx tsx core/wizard/update-intent.test.ts`
  - `npx tsx apps/cli/dota2/planning.test.ts`
- Fresh initialized-host proof on `D:\test3`:
  - review dry-run prompt: `做一个主动技能，不要UI，不要inventory，不要persistence。按Q生成一个跟随玩家2秒的火焰球，每0.5秒对附近敌人造成一次伤害。`
  - first proof exposed a real no-UI leak because `ui.selection_modal` was injected by negative-constraint misclassification
  - after the fix, the same prompt produced pure synthesized module resolution with zero UI routes
  - `npm run cli -- dota2 run "<same prompt>" --host D:\test3 --write -v` passed
  - final result on initialized host:
    - no resolved reusable patterns
    - 4 synthesized modules
    - host validation passed
    - runtime validation passed
    - final commit decision: `exploratory`
    - `requiresReview=true`
    - workspace state updated with feature `micro_feature_sewp`
- Latest proof artifact:
  - `D:\Rune Weaver\tmp\cli-review\dota2-review-1776576748109.json`

# Next intended moves

1. Re-prove dependency-driven revalidation with a provider/consumer pair and confirm required vs optional breakage behavior on update/delete.
2. Tighten synthesis grounding so host enum/API warnings keep shrinking instead of relying on post-hoc review.
3. Add one more initialized-host exploratory proof that combines partial-pattern reuse plus synthesized missing modules, not all-synthesized.
4. Decide whether the coarse `state.session.feature_owned` fallback should split further before more exploration cases are added.

# Current blockers / risks

- The blocker is no longer grammar gating or lack of an initialized-host exploratory commit proof.
- The current blocker is evidence quality and convergence:
  - dependency-driven revalidation still lacks a fresh provider/consumer proof
  - synthesis still produces host enum/API grounding warnings that need better retrieval coverage or validation ratchets
  - exploratory outputs are write-capable and lifecycle-truthful now, but they remain review-required and are not yet stabilized reusable assets
- Do not regress by reintroducing `grammar-v1` denial or `resolver-as-gate` behavior just to hide synthesis uncertainty.

# Notes for other sessions

- `D:\test3` is now the freshest initialized-host proof surface for Dota2 V2 exploratory write-path validation.
- The real lesson from this round is that the remaining risk moved from “can the system attempt this mechanic at all?” to “does the system preserve negative constraints and stay grounded while synthesizing?”
- If another session continues this lane, the highest-value next step is dependency evidence and synthesis-grounding reduction, not more seam-admission debate.
