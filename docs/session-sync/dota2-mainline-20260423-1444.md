> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after `TextButton` exact grounding closure and promotion-readiness proof harness landed
> Do not use for: claiming the provider seam is already formally admitted or that reveal-only runtime is stabilized

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: structured Panorama reference coverage now closes the last fresh reveal-only UI unknown honestly, and a proof-first Dota2 promotion-readiness harness now judges repeated exploratory seams without mutating formal registry truth. Fresh `D:\rw-test24` now proves both `skill_provider_demo` and `reveal_batch_demo` can write, validate, and doctor-pass on an initialized host while remaining `exploratory` and `requiresReview=true`.

# Done since last update

- Closed the fresh-host `TextButton` residual at the reference-data seam instead of by policy softening:
  - added structured Panorama `api-type` backing for `Panel`, `Label`, `TextButton`, and `Image`
  - removed the `panorama_tsx` intrinsic allowlist dependency for `Panel/Label`
  - re-ran `npx tsx core/retrieval/dota2-bundles.test.ts`, `npx tsx core/retrieval/host-symbol-index.test.ts`, and `npx tsx adapters/dota2/synthesis/index.test.ts`
- Added a Dota2 repo-side promotion-readiness harness plus tests:
  - positive candidate: `grant_only provider export seam`
  - negative control: exact-grounded reveal-only weighted-card output still must remain exploratory
  - split readiness spec from readiness harness so governance truth no longer carries temp-host execution and synthetic proof wiring in the same module
  - refreshed the harness to use an honest local `selection_pool` shell probe and canonical `entryId`-based selection grant bindings instead of older demo-prompt/objectId leftovers
  - re-ran `npx tsx adapters/dota2/governance/promotion-readiness.test.ts`, `npx tsx adapters/dota2/governance/reusable-assets.test.ts`, `npx tsx adapters/dota2/governance/feature-governance.test.ts`, `npx tsx adapters/dota2/cross-feature/grant-seam.test.ts`, and `npx tsx adapters/dota2/bridge/bridge-wiring.test.ts`
- Re-proved fresh host behavior on `D:\rw-test24`:
  - seeded a clean x-template host
  - ran `dota2 init`
  - wrote `skill_provider_demo` with the stable definition-only provider-shell wording
  - wrote `reveal_batch_demo`
  - `npm run cli -- dota2 validate --host D:\rw-test24` passes
  - `npm run cli -- dota2 doctor --host D:\rw-test24` passes after one-shot Panorama build via `yarn prod:panorama`
- Re-ran stable family regression:
  - `npm run cli -- dota2 validate --host D:\rw-test-1` passes
  - `npm run cli -- dota2 validate --host D:\rw-test-2` passes
- Re-ran `npm run check-types`

# Next intended moves

1. Use the new readiness harness to decide whether the `grant_only provider export seam` deserves its first manual promotion packet, while keeping reveal-only as an exploratory control.
2. Add one positive stale-host recovery proof where raw `metadata.grounding` survives, so the upgrade path is backed by end-to-end host evidence instead of only validator/repair tests.
3. Extend structured Panorama reference coverage only when fresh exploratory UI outputs introduce new intrinsic tags; do not reopen allowlist fallback for `panorama_tsx`.

# Current blockers / risks

- Older exploratory hosts without preserved raw grounding are still regenerate-only. `D:\rw-test14` remains the canonical honest failure case.
- The `grant_only provider export seam` now has a proof-first readiness harness, but it still has no formal promotion packet or admitted registry entry.
- Reveal-only weighted-card output no longer carries the `TextButton` unknown on the fresh path, but it remains exploratory and review-required; exact grounding must not be treated as promotion evidence by itself.

# Notes for other sessions

- Current fresh proof host:
  - `D:\rw-test24`
- Current fresh review artifacts:
  - `D:\Rune Weaver\tmp\cli-review\dota2-review-1776926045215.json`
  - `D:\Rune Weaver\tmp\cli-review\dota2-review-1776926559363.json`
- Stable selection-family proof hosts remain:
  - `D:\rw-test-1`
  - `D:\rw-test-2`
- Use this provider-shell wording when reproving the fresh host path:
  - `Create one gameplay ability shell only for later external granting. No activation key, no player input, no auto-attach, no grant logic, and no modifier application. It defines one primary hero ability for later external granting.`
