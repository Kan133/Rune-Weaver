> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-20
> Read when: resuming same-day War3 mainline work or checking the current bounded shadow lane
> Do not use for: claiming shared-core adoption, write-ready status, or runtime proof

# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

Current slice: keep the external bounded demo-probe lane blocked honestly while tightening the same canonical `setup-mid-zone-shop` shadow lane into:

- `current-slice sidecar`
- `War3ShadowRealizationPlan`
- `War3ShadowDraftBundle`
- `War3ShadowSiteEvidenceReview`
- review package / export / validate / probe-input

In parallel, the Workbench-facing War3 anchor/product seam has now been cleaned up so the UI shell no longer depends on a single god component or a silent multi-source fallback chain.

This is still not shared-mainline host-realization adoption, not a write-ready claim, and not runtime proof.

# Done since last update

- Split the Workbench War3 anchor lane into explicit controller / sections / types / utils seams:
  - [War3AnchorPanel.tsx](/D:/Rune%20Weaver/apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx) is now a thin shell
  - [useWar3AnchorPanelController.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/components/war3/useWar3AnchorPanelController.ts) now owns state, requests, copy fallback, and artifact/preview actions
  - [War3AnchorPanelSections.tsx](/D:/Rune%20Weaver/apps/workbench-ui/src/components/war3/War3AnchorPanelSections.tsx) now owns the presentation sections
  - [war3AnchorPanel.types.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/components/war3/war3AnchorPanel.types.ts) and [war3AnchorPanel.utils.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/components/war3/war3AnchorPanel.utils.ts) now hold DTO/utility truth instead of keeping them inside the panel body
- Tightened Workbench product-mode truth so normal UI loading paths now stop at one declared source instead of silently cascading through bridge/backend/fixture fallbacks.
- Re-ran repository/workbench validation after the UI cleanup:
  - `npm run check-types`
  - `npm run test`
  - `npm --prefix apps/workbench-ui run build`
  - `npm --prefix apps/workbench-ui run lint`
- Tightened the adapter-local shadow plan so declaration-site / realization-site review contracts are explicit instead of path-hint-only:
  - `runtime-hook-bootstrap-call-site`
  - `shop-target-declaration-site`
  - `trigger-area-realization-site`
  - `host-binding-review-surface`
- Added `War3ShadowSiteEvidenceReview` as a deterministic artifact derived from the shadow plan plus shadow draft bundle:
  - expected markers per site
  - linked draft file roles / path hints
  - dependency labels between sites
  - all-markers-present vs partial/missing review checks
- Tightened the bounded draft outputs and compatibility consumers so the site contracts stay visible all the way through:
  - draft bundle linked site ids
  - write-preview compatibility threading
  - implementation-draft source evidence
  - exported package file index
  - probe-input review section
- Tightened the local TSTL skeleton host-root evidence without widening scope:
  - `tmp/war3-tstl-skeleton/src/host/bootstrap.ts` now carries a review-only runtime-hook site object
  - `tmp/war3-tstl-skeleton/src/features/setupMidZoneShop.ts` now carries review-only declaration-site / realization-site marker objects
  - package validation against the current skeleton now upgrades from shape-only to:
    - `shopTarget = declaration-site-evidence`
    - `triggerArea = realization-site-evidence`
  - these remain review-only evidence classes, not runtime proof
- Re-ran bounded validation on 2026-04-16:
  - `npm run check-types`
  - `npx tsx adapters/war3/intent/shadow-realization.test.ts`
  - `npx tsx adapters/war3/intent/review-package.test.ts`
  - `npm run review` in `tmp/war3-tstl-skeleton`
  - `npm run test` in `tmp/war3-tstl-skeleton`
  - `npm run war3:export-skeleton-demo -- --out-dir tmp/war3-review-site-tightening-v2`
  - `npx tsx scripts/war3-validate-review-package.ts "<exported-package-dir>"`
  - `npm run war3:build-skeleton-probe-input -- --package-dir "<exported-package-dir>"`
- Ran one bounded external probe retry on 2026-04-16 after the local tightening.
  - result stayed blocked by external availability
  - observed error in the probe output: `402 invalid_request_error` with membership verification failure wording

# Next intended moves

1. Keep the same canonical feature and same authoring seam.
2. Wait for external probe availability to recover, then rerun the existing bounded demo-probe loop over the tightened exported package.
3. If more adapter-local work is needed before that retry, keep it limited to reviewability / determinism improvements on the same shadow lane or the newly-thinned Workbench anchor seam.
4. Do not broaden into shared host realization, second-feature probing, or runtime/toolchain claims unless one of those becomes the only honest next move.

# Current blockers / risks

- Primary blocker remains external probe availability.
  - the 2026-04-16 bounded retry still failed with provider-side membership verification / request availability output
- KK 1.29 runtime behavior is still unproven.
- War3 still cannot claim:
  - shared-core host-realization adoption
  - shared routing adoption
  - write-ready status
  - runtime proof
- The stronger declaration-site / realization-site evidence now lives at review-contract level and skeleton host-root review level only.
- The Workbench/UI cleanup removed a product-shell mixed-authority hotspot, but it does not change the primary external-probe blocker and must not be misread as runtime evidence.

# Notes for other sessions

- Same-day War3 truth is still:
  - Step 7 remains active
  - external probe availability remains the primary blocker
- The current bounded shadow lane is now:
  - `current-slice sidecar -> War3ShadowRealizationPlan -> War3ShadowDraftBundle -> War3ShadowSiteEvidenceReview -> review package/export/validate/probe-input`
- The current tightened evidence picture for the canonical feature is:
  - runtime hook: `host-evidence-found`
  - shop target: `declaration-site-evidence`
  - trigger area: `realization-site-evidence`
  - shadow site evidence contracts: 4, all markers present in the generated draft bundle
- Do not reinterpret this as:
  - shared `HostRealizationPlan` adoption
  - shared routing adoption
  - write-ready proof
  - runtime proof
- Keep focus on the same canonical feature and the same authoring seam:
  - `TypeScript -> TypeScriptToLua -> Lua output`
- Workbench/UI note for other sessions:
  - product-mode loader truth is now stricter, and explicit dev/test modes are the only remaining place where fixture-style fallback is allowed
  - the War3 anchor panel is now easier to extend in bounded sections, but `handoff/preview` and `map-preview/card` subgroups are still the best next cleanup candidates if more UI work becomes necessary
