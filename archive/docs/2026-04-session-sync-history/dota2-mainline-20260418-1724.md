# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: keep the authority-closed Dota2 core on Step 6 while de-familyizing the Wizard/create/update semantic front door enough that package-6 evidence no longer depends on family-coupled intent shaping. The supported `selection_pool` path still exists, but create/update semantic interpretation, relation discovery, clarification, and update delta contracts now run through generic Wizard/core seams first and only re-enter adapter-local source-backed enrichment after generic blueprinting.

# Done since last update

- Landed the narrow Wizard/create/update de-familyization seam without reopening Step 2:
  - `IntentSchema` authoritative contract removed Wizard-owned readiness / clarification fields
  - `WizardInterpretation` now carries only `intentSchema + clarificationPlan`
  - `UpdateWizardInterpretation` now carries only `requestedChange + updateIntent + clarificationPlan`
  - create/update relation grounding now uses derived `WorkspaceSemanticContext + RelationCandidates` after Wizard instead of giving Wizard full workspace authority
- Added generic workspace semantic sidecars:
  - `core/workspace/semantic-context.ts` now derives `WorkspaceFeatureHandle` / `WorkspaceSemanticContext`
  - aliases are derived only from stable feature-level handles, not object-level catalog labels
  - `core/wizard/relation-resolver.ts` now produces `RelationCandidate[]` without rewriting `IntentSchema`
- Put clarification into the real create/update mainline:
  - shared `apps/cli/helpers/wizard-flow.ts` now runs Wizard, relation discovery, clarification planning, and TTY follow-up
  - non-interactive create/update now stop before blueprint when structural clarification is still required
  - create/update review artifacts now include clarification / relation / workspace semantic sidecars
- Removed core/planning family authority from the create/update semantic front door:
  - `core/blueprint/builder.ts` now consumes only `IntentSchema` / `UpdateIntent`
  - `apps/cli/dota2/planning.ts` no longer imports `resolveSelectionPoolFamily` as create/update authority
  - Dota2 source-backed enrichment moved to `adapters/dota2/blueprint/enrichment.ts`
  - `selection_pool` same-feature merge remains downstream in adapter-local enrichment rather than in Wizard/core builder
- Hardened the generic update contract:
  - `core/wizard/update-intent.ts` now stays family-free while still deriving bounded deltas such as trigger rebinding, inventory add, and generic object-count expansion
  - `CurrentFeatureContext` remains a generic bounded snapshot instead of a family hint bag
  - create/update semantic JSON export remains review/demo-only and is not workspace authority
- Refreshed targeted validation on the new seams:
  - `npm run check-types`
  - `npx tsx core/wizard/intent-schema.test.ts`
  - `npx tsx core/wizard/update-intent.test.ts`
  - `npx tsx core/wizard/stability-harness.test.ts`
  - `npx tsx apps/cli/dota2/planning.test.ts`
  - `npx tsx apps/cli/dota2/semantic-artifacts.test.ts`
  - `npx tsx apps/cli/wizard-cli.smoke.test.ts`
  - `npx tsx core/llm/factory.test.ts`
  - `npx tsx core/blueprint/clarification-policy.test.ts`
  - `npx tsx core/blueprint/seam-authority.test.ts`
  - `npx tsx core/patterns/resolver.test.ts`
  - `npx tsx adapters/dota2/families/selection-pool/authoring.test.ts`
  - `npx tsx adapters/dota2/families/selection-pool/lifecycle.test.ts`

# Next intended moves

1. Re-prove the remaining Step 6 honest-block path and seam-gap probe on top of the now-generic Wizard/create/update front door.
2. Keep adapter enrichment scoped: do not let `selection_pool` regain Wizard/core/planning authority while more cases are added.
3. Keep improving Wizard semantic coverage and clarification quality through the dedicated harness instead of compensating with family heuristics.

# Current blockers / risks

- Primary blocker remains unchanged: package-6 matrix completeness is still missing one fresh honest-block path and one fresh seam-gap probe on the authority-closed core.
- New local risk after this lane: if later work quietly reintroduces family proposal logic into Wizard/core planning, the semantic front door will look generic again while actually routing through case-shaped authority.
- Clarification is now an honest stop in non-interactive mode; demo and automation callers must be prepared to inspect the exported artifact instead of expecting silent continuation through ambiguity.

# Notes for other sessions

- Step status is unchanged:
  - Step 2 remains honest-closed enough for current v1
  - active Dota2 mainline step remains Step 6
- Current semantic front-door truth is now:
  - Wizard must always produce best-effort `IntentSchema`
  - create-time workspace context is post-Wizard sidecar only
  - `CurrentFeatureContext` / `UpdateIntent` remain generic contracts
  - adapter-local source-backed merge / authoring compile / fill contract activation happen after generic blueprinting
- The still-supported `selection_pool` case path is now evidence that adapter enrichment can recover the bounded source-backed path without core family imports; do not treat that as permission to push new family authority back into Wizard or core builder.
