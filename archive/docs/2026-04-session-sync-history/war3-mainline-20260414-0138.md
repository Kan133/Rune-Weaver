# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update tightened the host model using both public TSTL references and community Lua/JAPI references.

# Done since last update

- Updated:
  - `docs/WAR3-KK-TSTL-PROVISIONAL-HOST-WORKSPACE-CONTRACT.md`
  - `docs/WAR3-TSTL-SKELETON.md`
  - `tmp/war3-tstl-skeleton/config.json`
  - `tmp/war3-tstl-skeleton/src/host/bootstrap.ts`
  - `tmp/war3-tstl-skeleton/README.md`

- The host model is now expressed more clearly as:
  - TypeScript authoring
  - Lua runtime output
  - Lua + JAPI-capable host path
  - `w3x` final artifact

- Public reference line remains:
  - `w3ts`
  - `wc3-ts-template`

- Community reference line now explicitly includes:
  - `h-lua-sdk`
  - `map-luamaker`

- This shifts JAPI from being treated as only deploy metadata toward being treated as host capability metadata.

# Next intended moves

- Continue aligning War3 host-binding slots against the TSTL skeleton and Lua/JAPI-capable runtime target
- Avoid reopening the question of whether KK needs a hidden extra host layer unless contradictory evidence appears

# Current blockers / risks

Primary blocker:

- even with the Lua/JAPI host capability assumption tightened, the exact KK-side build/package workflow is still not proven from a real sample

Secondary blockers / risks:

- public and community references are now strong enough for host-shape planning
- but still not enough to claim exact KK production behavior

# Notes for other sessions

- Current working judgment is now:
  - KK should be treated as a normal TSTL-to-Lua-to-w3x path
  - JAPI is part of host capability, not a separate authoring language
  - future War3 work should target the TSTL skeleton rather than PyWC3
