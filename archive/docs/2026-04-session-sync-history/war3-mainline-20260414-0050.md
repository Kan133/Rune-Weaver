# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update translated the current public and local evidence into a provisional KK 1.29 + TypeScript-to-Lua host workspace contract.

# Done since last update

- Added:
  - `docs/WAR3-KK-TSTL-PROVISIONAL-HOST-WORKSPACE-CONTRACT.md`

- The new contract explicitly separates:
  - publicly observed TSTL War3 project shape
  - locally observed War3 sample evidence
  - KK-specific inferred assumptions that are still unverified

- The provisional contract now treats the best current target workspace as:
  - a TypeScript project
  - paired with a map-folder workspace
  - producing Lua for runtime
  - with Rune Weaver artifacts in a separate RW-owned area

- The contract also makes the current proof ceiling explicit:
  - runtime-hook: map-script anchor only
  - shop-target: declaration shape only
  - trigger-area: rect / region shape only

# Next intended moves

- Wait for real KK platform evidence if available
- Once KK behavior is observed, tighten the provisional contract into a validated KK host contract

- If no real KK sample is available yet, the next local step should be:
  - align future host-side draft generation against this provisional TSTL-style workspace shape instead of PyWC3

# Current blockers / risks

Primary blocker:

- KK 1.29 Lua / TSTL runtime mechanism is still not proven from a real sample or authoritative public doc

Secondary blockers / risks:

- public TSTL references are strong enough to guide workspace shape
- but not strong enough to prove KK-specific runtime behavior

# Notes for other sessions

- Do not treat the new host-workspace contract as KK-official.
- Treat it as a planning contract and validation target only.
- This is still a good move because it gives B-group a better target than PyWC3 while waiting for real KK evidence.
