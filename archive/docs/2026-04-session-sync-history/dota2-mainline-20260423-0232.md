> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after the definition-only provider shell Stage 2 write-path closure on fresh `rw-test14`
> Do not use for: claiming reveal-only weighted-card semantics or broader exploratory output graduation are already settled

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the definition-only provider shell write path is now closed end to end on a fresh host. Canonical definition-only provider truth now stays a shell definition instead of fake `resource_pool` / runtime state, Blueprint emits the minimal `gameplay_ability` backbone, `create-readiness` no longer blocks on plain `moduleNeeds`, and Dota2 synthesis writes Lua + KV shells through the existing gameplay-ability path. Fresh `D:\rw-test14` proof now covers the full chain: `demo:talent-draw` write, `skill_provider_demo` provider-shell write, explicit consumer update that binds one selection reward to that provider, `validate` PASS, and `doctor` PASS after host init plus non-interactive Panorama build. The active blocker has therefore moved past provider-shell Stage 2 and back onto the next unresolved selection/generalization boundary.

# Done since last update

- Closed the definition-only provider shell Stage 2 blocker in the main chain instead of papering over it with prompt handling:
  - `core/wizard/intent-schema/definition-only-provider.ts` now keeps definition-only provider truth as shell definition semantics, strips fake resource/state carry-over, and broadens external-grant-only wording recognition
  - `core/blueprint/blueprint-semantic-shaping.ts`, `core/blueprint/builder-semantic-projection.ts`, `core/blueprint/module-planning.ts`, and `core/blueprint/seam-authority.ts` now route this shape to a minimal `gameplay_ability` backbone and stop demanding unrelated session-state authority
  - `adapters/dota2/blueprint/create-readiness.ts` now treats only `unresolvedModuleNeeds` as write blockers
- Re-proved provider-shell synthesis and identity closure:
  - the standard provider prompt now writes `skill_provider_demo` on `D:\rw-test14`
  - the alternate wording `Create one gameplay ability shell only ... for later external granting` now reaches dry-run `readyForHostWrite` instead of dying at Blueprint
- Fixed the update-side Dota2 grant seam so explicit provider-binding updates survive governance drift:
  - `adapters/dota2/cross-feature/grant-seam.ts` now recognizes `selection_pool` updates where prompt + resolved relation candidate + explicit local reward binding together imply one cross-feature selection grant mutation, even if the governed update delta failed to surface cross-feature semantics
  - fresh `D:\rw-test14` update proof now writes both `selection-grant-contract.json` and `selection-grant-bindings.json`
  - resulting binding is `SEL_R001 -> skill_provider_demo:grantable_primary_hero_ability`
- Regression coverage passed:
  - `npx tsx adapters/dota2/cross-feature/grant-seam.test.ts`
  - `npm run check-types`
- Fresh-host CLI proof passed:
  - `npm run demo:talent-draw -- --host D:\rw-test14 --write --force`
  - `npm run cli -- dota2 run "Create one grant-only provider shell..." --host D:\rw-test14 --feature skill_provider_demo --write --force`
  - `npm run cli -- dota2 update "Replace Talent Option R001 with the feature skill_provider_demo as one reward that grants its primary hero ability on selection. Keep the current F4 three-choice selection flow and all other behavior unchanged." --host D:\rw-test14 --feature talent_draw_demo --write`
  - `npm run cli -- dota2 validate --host D:\rw-test14`
  - `npm run cli -- dota2 init --host D:\rw-test14 --addon-name talent_draw_demo`
  - `node --preserve-symlinks node_modules/webpack/bin/webpack.js --config content/panorama/webpack.dev.js` and `npx gulp compile_less` inside `D:\rw-test14`
  - `npm run cli -- dota2 doctor --host D:\rw-test14`

# Next intended moves

1. Return to the reveal-only vs choose-one weighted-card boundary and decide whether reveal-only prompts stay clarification-only or gain a separate non-`selection_pool` exploratory realization path.
2. Reduce exploratory synthesis grounding / evidence warnings on the remaining non-family asks now that provider-shell write proof is no longer the blocker.
3. Decide which repeated exploratory outputs are ready to graduate into reusable assets without overfitting to the current provider/selection cases.

# Current blockers / risks

- The provider-shell Stage 2 blocker is gone:
  - definition-only provider shells now reach writeable host realization and fresh-host proof
- The main blocker is now:
  - reveal-only weighted-card prompts still do not have a proven non-`selection_pool` exploratory realization path, while ambiguous wording honestly lands on `selection_flow` clarification
  - exploratory outputs beyond the now-proved provider/consumer seam still carry review-required grounding risk
- Doctor pass on `D:\rw-test14` required host init plus explicit Panorama asset build; that is honest host-prep work, not a new Rune Weaver contract gap

# Notes for other sessions

- Fresh proof host: `D:\rw-test14`
- Fresh update review artifact showing the fixed consumer update path: `D:\Rune Weaver\tmp\cli-review\dota2-update-review-1776882332745.json`
- Fresh dry-run review artifact for the alternate provider wording: `D:\Rune Weaver\tmp\cli-review\dota2-review-1776882437598.json`
- Most important implementation note:
  - the missing consumer sidecars were not a provider-write problem; they were an update-side seam activation problem. The fix lives in `grant-seam`, not in prompt hacks or `selection_pool` case special-casing.
