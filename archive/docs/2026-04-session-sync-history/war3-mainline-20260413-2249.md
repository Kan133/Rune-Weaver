# Current focus

War3 mainline remains at Step 7: Product-facing integration, still on a narrow validated seam.

This round pushed one step past bridge preview and tested whether the current War3 slice can honestly enter the existing core Blueprint stage.

The answer is:

- yes, the current slice can be transformed into a valid Blueprint
- no, that does not yet mean the current core Blueprint surface is semantically tight enough for War3

# Done since last update

- Added a narrow Blueprint trial module for the current War3 slice:
  - `adapters/war3/intent/blueprint-trial.ts`
  - exported from `adapters/war3/intent/index.ts`
- The trial takes the current-slice bridge and runs:
  - `bridge.intentSchema -> BlueprintBuilder`
  - `validateBlueprint(...)`
  - drift-signal checks specific to the current War3 slice
- Verified the trial against the sample artifact with explicit current-slice inputs:
  - `shopTargetMode = existing-anchor`
  - `shopTargetSourceId = central_shop_proxy`
  - `shopUnlockMechanism = issue-order`
  - `shopOrderMode = neutral-target-order-by-id`
  - `shopOrderId = 852566`
  - `triggerAreaSourceId = mid_trigger_zone`
- Result:
  - Blueprint is produced successfully
  - Blueprint validation passes structurally
  - but drift signals are correctly detected
- Important observed output from the Blueprint trial:
  - the UI design surface is inferred as `hint`
  - but the UI module is still routed as `selection_modal` with pattern `ui.selection_modal`
  - unresolved War3 host-binding facts are not propagated into Blueprint assumptions
- Also fixed two small core TypeScript cleanliness issues that started surfacing during this round:
  - removed unused type imports in `core/blueprint/types.ts`
  - removed unused local in `core/blueprint/validator.ts`
- Re-verified `apps/workbench-ui` build after the round and it passes.

# Next intended moves

- Do not jump straight from this result to “War3 is now in core mainline.”
- The next narrow move should be one of:
  1. add a War3-specific pre-Blueprint normalization seam for the current slice, especially to keep `hint` from being widened into `selection_modal`
  2. teach BlueprintBuilder a narrower UI path for `hint` / lightweight feedback surfaces
  3. propagate unresolved host-binding facts into Blueprint assumptions or sidecar review artifacts so the Blueprint cannot look cleaner than reality
- Keep the scope narrow:
  - current slice only
  - no broad cross-host framework detour
  - no claim of complete War3 pipeline integration yet

# Current blockers / risks

Primary blocker:

- semantic drift between War3 slice meaning and generic Blueprint defaults
- concrete current example:
  - `hint` surface becomes `ui.selection_modal` at Blueprint module level

Secondary blockers / risks:

- unresolved War3 host-binding remains attached only on the bridge side, not visible enough once Blueprint is produced
- current core Blueprint stage is structurally valid for this slice, but not yet War3-tight
- runtime host binding is still unresolved for anchored shop handle realization

# Notes for other sessions

- This round is useful evidence, not failure:
  - it proves the War3 bridge can enter the current Blueprint stage
  - it also proves exactly where generic Blueprint behavior is too broad for the current slice
- Current honest summary:
  - `bridge -> Blueprint` is now reachable
  - but it still needs a narrowing seam before it can be treated as trustworthy War3 mainline flow
- If another session picks this up, the best next question is:
  - should we narrow BlueprintBuilder for `hint` surfaces directly, or add a War3-local pre-Blueprint adaptation layer first?
