# Current focus

War3 mainline is currently at Step 6: Validation of assumptions.

Exact slice:

- validate that the tightened War3 intake / handoff contract is now strong enough to support a narrow implementation-shaped output
- keep unresolved pieces explicit as host-binding issues instead of allowing broad model invention

# Done since last update

- Added explicit shop-order contract fields to the War3 intake / handoff layer:
  - `shopOrderMode`
  - `shopOrderId`
- These changes were landed in:
  - `apps/workbench-ui/src/lib/war3-intake.ts`
  - `apps/workbench-ui/src/lib/war3-handoff.ts`
  - `apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx`
- Verified `apps/workbench-ui` still builds successfully after the contract tightening.
- Ran a narrow order-semantics probe:
  - input: `tmp/war3_order_semantics_probe_prompt.txt`
  - result: `tmp/kimi-war3/order-semantics-v1-2026-04-13T13-59-14-342Z.final.txt`
  - summary status: `model-output-success`
- Confirmed that with the current contract shape, the model can remain honest at the order-family decision point:
  - it did not guess between `IssueTargetOrderById` and `IssueNeutralTargetOrderById`
  - it kept unresolved order-family/binding pieces explicit
- Tested the stronger host-binding hypothesis around:
  - `shopOrderMode = neutral-target-order-by-id`
  - `shopOrderId = 852566`
  - `shopTargetMode = existing-anchor`
  - `shopTargetSourceId = central_shop_proxy`
- A first longer host-binding probe hit the recurring `kimi` step limit, so a shorter equivalent prompt was used instead:
  - input: `tmp/war3_host_binding_probe_prompt_short.txt`
  - result: `tmp/kimi-war3/host-binding-short-v1-2026-04-13T14-03-23-944Z.final.txt`
  - summary status: `model-output-success`
- Confirmed that fixing those host facts collapses the remaining uncertainty mostly to runtime binding / parameter placement rather than broad host invention.
- Ran a minimal module-draft validation with an even shorter prompt shape:
  - input: `tmp/war3_module_draft_prompt_short.txt`
  - result: `tmp/kimi-war3/module-draft-short-v1-2026-04-13T14-09-24-113Z.final.txt`
  - summary status: `model-output-success`
- Confirmed that the current contract can now support a narrow module-level War3 host draft:
  - top-level constants for radius, hint text, hint duration, and order id
  - one setup function
  - grounded region creation and enter trigger registration
  - user/player gating
  - direct `IssueNeutralTargetOrderById(p, central_shop_proxy, 852566, u)` call shape
  - unresolved values kept explicit only where host binding is still missing, such as rect bounds / symbol declaration site

# Next intended moves

- Stop spending mainline time on broad prompt archaeology.
- Treat the current War3 contract as strong enough for narrow implementation-draft validation.
- Shift the next slice toward real host-skeleton work:
  - decide where runtime symbols such as `central_shop_proxy` are declared or bound
  - decide how generated-radius trigger rect bounds are materialized from map-space inputs in the actual host output path
  - keep unresolved host-binding seams explicit rather than smuggling in helpers
- If another validation pass is needed, keep it tied to one narrow host-skeleton consumer rather than another broad probe family.

# Current blockers / risks

Primary blocker:

- the remaining War3 uncertainty is now mostly host-binding integration, not schema ambiguity:
  - runtime binding / declaration site for `central_shop_proxy`
  - concrete rect bounds materialization for the generated-radius trigger area

Secondary blockers / risks:

- the repo-level `core/llm` provider path is still blocked by `401 Invalid Authentication`
- the current UI still captures the newer War3 schema fields as free-text rather than tighter enum/select controls
- longer `kimi` prompts still tend to hit `Max number of steps reached: 3`, so short prompt shapes remain the reliable validation surface

# Notes for other sessions

- War3 mainline should now be read as being past “pure schema tightening” and entering a narrow implementation-draft validation phase.
- The contract has materially improved in three layers:
  1. trigger-area realization
  2. shop target realization
  3. shop order mode / order id
- The best current evidence that the line is ready for a narrow host skeleton is:
  - `tmp/kimi-war3/module-draft-short-v1-2026-04-13T14-09-24-113Z.final.txt`
- That output is not final host realization, but it is good evidence that the current contract can support a minimal module-shaped draft without broad invention.
- The next valuable work should connect this contract to a real War3 host skeleton rather than expanding the probe family further.
