# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: close the Talent Draw create-path credibility gap so a fresh x-template host can go through:

- Workbench `Create`
- real CLI-backed write
- repair-build / launch
- in-game F4 runtime
- canonical evidence refresh

This session was not focused on adding breadth. It was focused on removing the specific Dota2 mainline failures that made the create-path look successful in the UI but fail or degrade at runtime.

# Done since last update

- Fixed the Workbench `Create -> dota2 run` transport on Windows so prompt delivery no longer breaks on:
  - Chinese prompt text
  - repo paths with spaces
  - `cmd.exe` quoting
  by switching the bridge to `--input-base64-env` instead of temp prompt files.
- Added a canonical Talent Draw case source at [talent-draw.ts](/D:/Rune%20Weaver/adapters/dota2/cases/talent-draw.ts) and routed the planning path through it from [planning.ts](/D:/Rune%20Weaver/apps/cli/dota2/planning.ts).
- Kept Talent Draw case logic out of `core/wizard`; the generic wizard remains generic. Case-specific canonical shaping now lives in the Dota2 side.
- Fixed fallback intent/planning so Talent Draw-like prompts now produce:
  - seeded weighted pool entries
  - full selection flow
  - `effect.modifier_applier`
  instead of an empty selection shell.
- Fixed trigger key extraction in [index.ts](/D:/Rune%20Weaver/core/wizard/index.ts) so prompts like `按下 F4` correctly resolve to `F4` rather than `F`.
- Fixed workspace integration so `integrationPoints` now record key bindings correctly, which lets governance block duplicate active bindings before the host gets polluted.
- Strengthened host/product validation:
  - added active key binding conflict detection
  - added selection-pool seed validation for `selection_flow + weighted_pool`
  - limited `ScriptFile` validation to Rune Weaver-owned ability entries so x-template example abilities no longer create false failures
- Fixed `effect.modifier_applier` naming consistency so KV/Lua outputs share the same ability name and host validation no longer fails on a phantom missing Lua file.
- Refreshed the canonical evidence pack for host `D:\AA` and got `PASS` in:
  - [manifest.json](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/manifest.json)
  - [doctor-output.txt](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/doctor-output.txt)
  - [validate-output.txt](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/validate-output.txt)
- Updated the evidence refresh path so it no longer hardcodes `talent_draw_demo`; it now accepts the current canonical Talent Draw feature shape and correctly exported:
  - [generated-files.json](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/generated-files.json)
  - [review-artifact.json](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/review-artifact.json)
  - [talent-draw-demo-runtime.mp4](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/talent-draw-demo-runtime.mp4)

# Next intended moves

1. Move from evidence closure to product-facing review quality:
   - add a clearer pre-write review surface for CLI / Workbench
   - explain selected patterns, files to write, and key integration points before write
2. Tighten Workbench create-path truthfulness:
   - make the create result show authoritative CLI stage summaries, not only the tail of host validation lines
   - make conflict failures and readiness blockers feel explicitly intentional rather than generic `exit code 1`
3. Decide whether the next Dota2 mainline step should be:
   - review surface first, or
   - second composite case, but only if it strengthens generalization rather than creating a second special path

# Current blockers / risks

- The current Talent Draw path is now credible for the canonical case, but it still relies on canonical-case detection rather than a generalized small-user parameter clarification engine.
- Workbench `Create` can still look like a generic product entry even though its strongest case today is the canonical Talent Draw path.
- The repo has accumulated Dota2 mainline changes across:
  - [planning.ts](/D:/Rune%20Weaver/apps/cli/dota2/planning.ts)
  - [dota2-cli.ts](/D:/Rune%20Weaver/apps/cli/dota2-cli.ts)
  - [validation-orchestrator.ts](/D:/Rune%20Weaver/apps/cli/helpers/validation-orchestrator.ts)
  - [post-generation-validator.ts](/D:/Rune%20Weaver/adapters/dota2/validator/post-generation-validator.ts)
  - [index.ts](/D:/Rune%20Weaver/adapters/dota2/assembler/index.ts)
  Any new session touching these files should read them first instead of assuming the old Talent Draw behavior still applies.
- Evidence refresh is now correct for the current canonical case, but downstream docs still talk in some places as if the runtime feature id were always `talent_draw_demo`.

# Notes for other sessions

- The honest Dota2 mainline claim is now stronger:
  - fresh host onboarding can reach a real Talent Draw create
  - the created feature writes server/shared/ui/Lua/KV outputs
  - duplicate key conflicts are blocked before host pollution
  - canonical evidence is refreshed and passing for `D:\AA`
- Do not describe the Workbench create-path as “fully general small-user create” yet. The strongest proof is still the canonical Talent Draw path plus CLI-backed lifecycle.
- If another session continues the Dota2 mainline, the best next step is likely a review surface / write-preview improvement, not a broad generator rewrite.
- If another session touches the evidence pack, keep using:
  - [demo-evidence/latest](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest)
  - not ad hoc folders or one-off export locations.
