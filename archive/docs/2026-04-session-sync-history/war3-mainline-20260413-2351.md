# Current focus

War3 mainline is still at Step 7: Product-facing integration, but only on a narrow validated seam.

Current emphasis:

- keep War3 moving through real intermediate artifacts
- avoid drifting back into broad workbench/product shell work
- use the current mid-zone shop slice to harden backend-facing review/export flow

# Done since last update

- Kept the current War3 chain narrow and explicit:
  - intake artifact
  - bridge
  - pre-Blueprint normalization
  - Blueprint trial
  - War3 assembly sidecar
  - write-preview artifact
  - exported review package

- Added and verified a pure backend export path:
  - `scripts/war3-export-review-package.ts`
  - npm entry: `war3:export-review`

- Verified backend export with explicit current-slice overrides and produced a real exported package under:
  - `tmp/war3-review/micro_feature_aym7-2026-04-13T15-48-54-080Z`

- Exported package now includes:
  - `bridge.json`
  - `sidecar.json`
  - `write-preview.json`
  - `setupMidZoneShop.ts`
  - `README.md`
  - `package.json`

- Re-verified `apps/workbench-ui` build after the recent review/export work and it passes.

# Next intended moves

- Primary next move:
  - tighten War3 host-binding manifest semantics

- Concretely, the next work should move unresolved host-binding from loose descriptive strings toward a more explicit contract, especially around:
  - anchored shop symbol sourcing/declaration
  - trigger-area materialization responsibilities
  - runtime hook expectations

- After that, the likely next move is:
  - add a backend validator for exported War3 review packages
  - or begin runtime-facing review against a real War3 project using the exported package as handoff

# Current blockers / risks

Primary blocker:

- host-binding is still not formal enough
- unresolved runtime facts are still mainly represented as review text rather than structured binding slots

Secondary blockers / risks:

- current backend export path still requires explicit overrides when source artifacts come from older schema-tightening stages
- War3 downstream flow is honest and useful, but still review-oriented rather than runtime-proven

# Notes for other sessions

- Do not pull War3 back into front-end/workbench expansion right now.
- A group is already working around workbench concerns, and Dota2/War3 workbench shapes may diverge.
- The current best War3 continuation is backend-facing:
  - review package export
  - host-binding manifest tightening
  - runtime-facing review prep
