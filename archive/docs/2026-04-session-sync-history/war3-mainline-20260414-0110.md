# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update tightened the external evidence story around the new War3 TSTL skeleton and provisional host contract.

# Done since last update

- Updated:
  - `docs/WAR3-KK-TSTL-PROVISIONAL-HOST-WORKSPACE-CONTRACT.md`
  - `docs/WAR3-TSTL-SKELETON.md`

- The docs now state more explicitly that the strongest public War3 TSTL references are:
  - `w3ts`
  - `wc3-ts-template`

- The provisional host contract now calls out a more concrete public project shape:
  - `package.json`
  - `tsconfig.json`
  - `config.json`
  - `scripts/`
  - `src/`
  - `maps/<map>.w3x`

# Next intended moves

- Continue treating KK as a normal TSTL-to-Lua-to-w3x path unless contradictory evidence appears
- Use the TSTL skeleton and provisional contract as the default War3 host target for future backend work

# Current blockers / risks

Primary blocker:

- we still need a real KK-backed sample or direct hands-on confirmation before claiming the exact packaging/build details

Secondary blockers / risks:

- public TSTL examples remain stronger for workspace shape than for KK-specific runtime details

# Notes for other sessions

- Current working judgment is now simpler:
  - no special hidden host layer is assumed
  - treat War3 KK as a normal TSTL authoring path that ends in `w3x`
- This should reduce over-modeling and let future War3 work target a standard TSTL project shape.
