# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update tightened the current host-binding proof layer so the active seam is now explicitly:

- `shop-target declaration-site proof`
- `trigger-area realization-site proof`

instead of looser shape-only host-binding language.

# Done since last update

- Updated:
  - `adapters/war3/intent/review-package.ts`
  - `scripts/war3-validate-review-package.ts`

- Added:
  - `adapters/war3/intent/review-package.test.ts`

- Tightened War3 validation result semantics:
  - `shop-target`
    - old strong status: `symbol-declared`
    - new strong status: `declaration-site-evidence`
  - `trigger-area`
    - old strong status: `region-materialization-evidence`
    - new strong status: `realization-site-evidence`

- Added explicit path hints into validation results:
  - `shopTargetValidation.declarationSitePathHint`
  - `triggerAreaValidation.realizationSitePathHint`

- Tightened the declaration-site proof rule for `shop-target`:
  - no longer upgrades just because a symbol appears somewhere generic
  - now prefers the declared site candidate from `hostBindingDraft`
  - only upgrades to `declaration-site-evidence` when the declared candidate itself contains the binding symbol

- Tightened the realization-site proof rule for `trigger-area`:
  - no longer upgrades on any single trigger seam clue
  - now requires grouped materialization statements in the declared realization-site candidate:
    - `Rect(...)`
    - `CreateRegion`
    - `RegionAddRect`
    - `TriggerRegisterEnterRegion`

- Validation script output now prints:
  - declaration-site path hint
  - realization-site path hint

- Added lightweight War3 review-package tests covering:
  - `runtime-hook = host-evidence-found`
  - `shop-target = declaration-shape-only`
  - `shop-target = declaration-site-evidence`
  - `trigger-area = rect-shape-only`
  - `trigger-area = realization-site-evidence`
  - missing `implementation-draft-plan.json` readback tolerance
  - exported `implementation-draft-plan.json` presence

- Verified:
  - `npm run check-types`
  - `npx tsx adapters/war3/intent/review-package.test.ts`
  - `npx tsx scripts/war3-export-review-package.ts ...`
  - `npx tsx scripts/war3-validate-review-package.ts ...`

- Fresh review package:
  - `tmp/war3-review/micro_feature_7180-2026-04-14T10-53-35-089Z`

# Next intended moves

- Primary next move:
  - tighten `shop-target` against a more real TSTL host project than the current skeleton if one becomes available

- Secondary next move:
  - tighten `trigger-area` from review-level realization-site evidence toward a more concrete host-project declaration/proof path

- After that:
  - decide whether any downstream review/write-planning surface should consume `implementation-draft-plan.json`

# Current blockers / risks

Primary blocker:

- The active package now points at explicit declaration-site / realization-site candidates, but the default TSTL skeleton still validates as:
  - `shop-target = declaration-shape-only`
  - `trigger-area = rect-shape-only`

Secondary blockers / risks:

- The stronger statuses remain review-only evidence, not runtime proof.
- `implementation-draft-plan.json` is still not a write-ready host plan.
- KK 1.29 runtime proof is still outside the evidence earned in this slice.

# Notes for other sessions

- Do not broaden this into generic host abstraction work.
- The important change is not new product surface area; it is stricter proof semantics.
- The default consumer artifact remains:
  - `implementation-draft-plan.json`
- The freshest package to inspect is:
  - `tmp/war3-review/micro_feature_7180-2026-04-14T10-53-35-089Z`
