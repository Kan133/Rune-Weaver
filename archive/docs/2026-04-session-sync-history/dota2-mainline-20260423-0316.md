> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after the generic provider-consumer contract layer and reusable-asset governance V1 landed
> Do not use for: claiming event/state/integration surfaces or automatic promotion are already generalized

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the Dota2-first control-plane follow-up on top of the selection and provider/consumer proofs is now landed. `FeatureContract` data/capability surfaces now carry stable `contractId` metadata, Dota2 content collections and grantable primary hero ability are the first typed adopters, dependency revalidation now keys on `kind + contractId + surfaceId`, and repo-side manual governance V1 now records admitted reusable assets without turning workspace truth into a second authority. Fresh host proofs on `D:\rw-test-1`, `D:\rw-test-2`, and `D:\rw-test14` still pass after this change, so the mainline can return to the remaining real semantic blockers instead of more provider/consumer drift.

# Done since last update

- Landed the generic typed contract helper layer in core:
  - `core/contracts/feature-contract.ts` now owns surface/dependency creation and compatibility checks
  - `core/schema/types.ts` and workspace persistence now carry `contractId` for typed `data` / `capability` surfaces and `targetContractId` for dependency edges
- Wired Dota2 first adopters onto the generic contract layer instead of adapter-local inference:
  - `adapters/dota2/content-collections/contracts.ts` now exports/consumes `content_collection:<collectionId>` as `kind:"data"` with `contractId:"selection_pool.object"`
  - `adapters/dota2/cross-feature/grant-artifacts.ts` now exports provider ability surfaces as `kind:"capability"` with `contractId:"dota2.primary_hero_ability.grantable"` and normalizes old artifacts on read
  - `adapters/dota2/cross-feature/grant-seam.ts` and `core/workspace/dependency-revalidation.ts` now validate compatibility on typed contract identity instead of surface naming alone
- Landed explicit reusable-asset governance V1 without inventing a second code authority:
  - `core/governance/reusable-assets.ts` defines the generic packet/registry validation rules
  - `adapters/dota2/governance/reusable-assets.ts` admits `effect.outcome_realizer` as the first pattern exemplar and `selection_pool` as the first family exemplar
  - `apps/cli/pattern-cli.ts` and `apps/workbench/workbench-commands.ts` now surface admitted vs untracked governance state
- Re-ran the key proofs on real hosts after the control-plane change:
  - `D:\rw-test-1`: `demo:talent-draw` write + validate PASS
  - `D:\rw-test-2`: `demo:equipment-draw` write + validate PASS
  - `D:\rw-test14`: provider-shell write, consumer reward binding update, `validate` PASS, `doctor` PASS
- Regression coverage passed for the new layer:
  - `npx tsx core/contracts/feature-contract.test.ts`
  - `npx tsx core/workspace/dependency-revalidation.test.ts`
  - `npx tsx adapters/dota2/content-collections/contracts.test.ts`
  - `npx tsx adapters/dota2/cross-feature/grant-artifacts.test.ts`
  - `npx tsx adapters/dota2/cross-feature/grant-seam.test.ts`
  - `npx tsx adapters/dota2/governance/reusable-assets.test.ts`
  - `npx tsx adapters/dota2/governance/feature-governance.test.ts`
  - `npm run check-types`

# Next intended moves

1. Return to the reveal-only vs choose-one weighted-card boundary and decide whether reveal-only asks stay clarification-only or gain a separate non-`selection_pool` exploratory realization path.
2. Reduce exploratory synthesis grounding warnings on the remaining non-family asks now that provider/consumer contract identity is no longer the blocker.
3. Keep the generic contract layer intentionally narrow until there is honest pressure to expand beyond `data + capability`; do not pretend `event/state/integration` are already unified.

# Current blockers / risks

- Primary blocker: reveal-only weighted-card prompts still lack a proven non-`selection_pool` exploratory realization path, while ambiguous wording honestly lands on one `selection_flow` clarification.
- Exploratory outputs beyond the now-typed seams still carry review-required grounding risk.
- Reusable-asset governance V1 is intentionally manual and reporting-first:
  - it does not auto-promote successful exploratory writes
  - it does not replace workspace lifecycle truth
  - it should not be mistaken for cross-host proof

# Notes for other sessions

- Core truth for this landing lives in:
  - `core/contracts/feature-contract.ts`
  - `core/governance/reusable-assets.ts`
- Dota2 first-adopter surfaces live in:
  - `adapters/dota2/content-collections/contracts.ts`
  - `adapters/dota2/cross-feature/grant-artifacts.ts`
  - `adapters/dota2/cross-feature/grant-seam.ts`
  - `adapters/dota2/governance/reusable-assets.ts`
- New writes now add typed contract metadata; old workspace/host artifacts remain readable through ingress normalization only.
- Governance remains repo-side/manual only. Workspace continues to own feature-instance lifecycle fields such as `implementationStrategy`, `maturity`, and `commitDecision`.
- Fresh proof hosts:
  - `D:\rw-test-1`
  - `D:\rw-test-2`
  - `D:\rw-test14`
