# Current focus

War3 mainline is currently at Step 6: Validation of assumptions.

Exact slice:

- validate that the War3 host workspace can turn real map inputs into a stable intake artifact
- validate that the intake artifact and handoff bundle can drive narrow, trustworthy probe outputs
- narrow the remaining runtime uncertainty until the open gaps are explicit schema or host-binding questions rather than model invention

# Done since last update

- Kept the current War3 artifact flow stable over the real sample folder map path:
  - `tmp/war3-samples/PyWC3/maps/test.w3x`
  - parser and derived workspace work remain anchored on real `war3map.w3i`, `war3map.w3e`, `war3mapunits.doo`, and `war3map.doo`
- Continued probe hardening using `scripts/war3-probe-runner.ts` with short ASCII prompt files.
- Validated a more conservative typed probe:
  - input: `tmp/war3-typed-probe-input-20260413-short-conservative-v2-case.txt`
  - result: `tmp/kimi-war3/typed-conservative-v2-2026-04-13T13-04-33-598Z.final.txt`
  - summary status: `model-output-success`
- Confirmed the conservative-v2 probe reduced hidden invention:
  - frame/UI invention was removed
  - ownership-mutation unlock assumptions were removed
  - missing runtime pieces were surfaced as `UNSPECIFIED IN PROMPT`
- Validated the cleanest native-grounded probe so far:
  - input: `tmp/war3-grounded-probe-native-whitelist-v1.md`
  - result: `tmp/kimi-war3/grounded-native-v1-2026-04-13T13-08-42-997Z.final.txt`
  - summary status: `model-output-success`
- Confirmed the native-whitelist prompt suppresses helper-wrapper invention much better than earlier probes:
  - output stayed on direct native vocabulary such as `CreateTrigger`, `TriggerRegisterEnterRegion`, `TriggerAddAction`, `GetEnteringUnit`, `GetOwningPlayer`, `GetPlayerController`, `GetPlayerSlotState`, and `DisplayTimedTextToPlayer`
  - the remaining unknowns were left inline as `UNSPECIFIED IN PROMPT`
- Read back the current intake contract and confirmed the next schema gap is real rather than speculative:
  - `apps/workbench-ui/src/lib/war3-intake.ts` currently captures anchor position, role, owner, and feature inputs like `shopInteractionMode`, `targetPlayers`, `hintDurationSeconds`, and `shopObjectId`
  - it does not yet capture trigger-area realization shape such as radius, width/height, existing-region versus generated-area mode, or other region-handle sourcing information

# Next intended moves

- Add a small trigger-area realization layer to the War3 intake / handoff contract so probes no longer have to guess how `mid_trigger_zone` becomes a real region or rect handle.
- Likely new fields should cover:
  - trigger area mode, for example `existing-region`, `generated-rect`, or `generated-radius`
  - trigger area size, for example radius or width/height
  - explicit hint text so literal drift does not keep filling itself in
  - possibly a more explicit shop unlock mechanism enum if we want the next grounded probe to stop leaving that part blank
- After the intake/handoff shape is tightened, rerun the native-grounded probe and check whether the only remaining uncontrolled fill is on narrow literals or whether host-binding uncertainty is now properly isolated.
- Keep treating all probe outputs as evidence for schema / boundary decisions, not as final host realization.

# Current blockers / risks

Primary blocker:

- the repo-level `core/llm` provider path is still blocked by `401 Invalid Authentication`, so real in-repo execution is still unavailable for War3 validation

Additional risks:

- the current War3 intake contract does not yet encode trigger-area realization, so grounded probes still cannot fully resolve region-handle sourcing
- shop unlock behavior is still not grounded to an explicit native/mechanism choice for KK / 1.29
- even the cleanest grounded probe still shows small literal drift, such as choosing its own hint message string and `5.0` duration when not fully pinned down by the prompt
- manual anchor ordering is still assembled as `anchor -> doodad -> manual`, not a true cross-type global order

# Notes for other sessions

- War3 mainline is no longer in blank host investigation. It has:
  - real parser coverage for the current P0 map files
  - real derived workspace and workbench artifact flow
  - real handoff bundle generation
  - multiple successful `kimi --no-thinking` probe runs through the file-driven runner
- The current best probe evidence is now the grounded native-whitelist run:
  - `docs/war3-probes/grounded-native-whitelist-v1-2026-04-13.md`
  - this is the cleanest evidence for native-grounded structure so far, but still not final generation
- The decision surface is now much narrower than before. The two most important unresolved points are:
  1. trigger-zone handle source / area realization policy
  2. shop unlock native/mechanism
- The next highest-value move is probably schema work, not another free-form prompt rewrite.
- If another session continues this lane, it should read:
  - `docs/war3-probes/2026-04-13-typed-conservative-v2.md`
  - `docs/war3-probes/grounded-native-whitelist-v1-2026-04-13.md`
  - `apps/workbench-ui/src/lib/war3-intake.ts`
  - `apps/workbench-ui/src/lib/war3-handoff.ts`
