# Current focus

War3 mainline is currently at Step 6: Validation of assumptions.

Exact slice:

- tighten the War3 intake / handoff contract so remaining uncertainty is expressed as explicit schema or host-binding questions
- validate that the tightened contract reduces grounded probe ambiguity without expanding into broad product shell work

# Done since last update

- Added a minimal trigger-area realization layer to the War3 intake / handoff contract in:
  - `apps/workbench-ui/src/lib/war3-intake.ts`
  - `apps/workbench-ui/src/lib/war3-handoff.ts`
- Added minimal panel plumbing in:
  - `apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx`
- New explicit contract fields now include:
  - `triggerAreaMode`
  - `triggerAreaSourceId`
  - `triggerAreaRadius`
  - `triggerAreaWidth`
  - `triggerAreaHeight`
  - `explicitHintText`
  - `shopUnlockMechanism`
- Confirmed the updated `workbench-ui` still builds successfully after the contract change.
- Generated a schema-tightened artifact and handoff bundle:
  - `tmp/war3-schema-tightened-artifact.json`
  - `tmp/war3-schema-tightened-bundle.json`
- Ran a new grounded probe against the tightened schema:
  - input: `tmp/war3-grounded-probe-schema-tightened-v2.md`
  - result: `tmp/kimi-war3/grounded-schema-v2-2026-04-13T13-31-06-577Z.final.txt`
  - summary status: `model-output-success`
- Confirmed the new grounded probe no longer guesses how `mid_trigger_zone` becomes a real trigger area.
  - it grounded the trigger area directly as:
    - `Rect(...)`
    - `CreateRegion()`
    - `RegionAddRect(...)`
    - `TriggerRegisterEnterRegion(...)`
- Confirmed the remaining unresolved part stayed explicit:
  - `// UNSPECIFIED IN PROMPT: shop unlock mechanism`

# Next intended moves

- Keep the current contract shape and decide whether to:
  1. add one more explicit schema field or enum around `shopUnlockMechanism`, or
  2. run a narrower implementation-draft validation that treats shop unlock as explicitly unknown while validating the rest of the grounded runtime path
- Prefer host-binding tightening over further broad prompt experimentation.
- Keep probe outputs as schema / boundary evidence only.

# Current blockers / risks

Primary blocker:

- the War3 intake / handoff contract still lacks a grounded host-binding choice for `shopUnlockMechanism`, so the downstream runtime path is not yet fully specified

Secondary blockers / risks:

- the repo-level `core/llm` provider path is still blocked by `401 Invalid Authentication`, so in-repo real execution remains unavailable
- `triggerAreaSourceId` remains relevant for `existing-region` mode, but that mode is not yet proven through a grounded probe
- the current panel uses free-text inputs for the new fields, so invalid values are still possible until a narrower enum/select treatment is added
- manual anchor ordering is still assembled as `anchor -> doodad -> manual`, not a true cross-type global order

# Notes for other sessions

- The important correction is that `401 Invalid Authentication` should no longer be treated as the main War3 blocker.
- The main blocker is now schema / host-binding incompleteness, with the clearest remaining item being `shopUnlockMechanism`.
- `trigger-area realization / region-handle sourcing` has materially improved:
  - once encoded as `generated-radius` plus radius, the grounded probe can realize the trigger area directly from native vocabulary instead of guessing a symbolic region handle source
- The current best evidence chain is now:
  - tightened intake contract -> tightened handoff bundle -> grounded native probe -> explicit residual unknown on shop unlock only
- Useful follow-up files:
  - `docs/session-sync/war3-mainline-20260413-2115.md`
  - `docs/war3-probes/grounded-native-whitelist-v1-2026-04-13.md`
  - `tmp/war3-schema-tightened-artifact.json`
  - `tmp/war3-schema-tightened-bundle.json`
  - `tmp/kimi-war3/grounded-schema-v2-2026-04-13T13-31-06-577Z.final.txt`
