# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update turned the newly explicit `hostBindingDraft` into a real downstream consumer artifact instead of leaving it as review-only structure with no next-stage consumer.

# Done since last update

- Added:
  - `adapters/war3/intent/implementation-draft-plan.ts`

- Updated:
  - `adapters/war3/intent/index.ts`
  - `adapters/war3/intent/review-package.ts`
  - `scripts/war3-export-review-package.ts`
  - `scripts/war3-validate-review-package.ts`

- Added a War3-local implementation-draft consumer artifact:
  - `war3-implementation-draft-plan/current-slice-v1`

- This new artifact is derived from:
  - exported `tstlHostDraft`
  - `hostBindingDraft`
  - the current review-package validation snapshot

- The implementation-draft plan currently materializes three review-only entries:
  - `runtime-hook-bootstrap`
  - `mid-zone-shop-feature`
  - `host-binding-review-json`

- Each entry now records:
  - target path hint
  - content type
  - content
  - consumed binding slots
  - source evidence
  - review-only notes

- `exportWar3ReviewPackage(...)` now writes:
  - `implementation-draft-plan.json`

- `readWar3ReviewPackageFromDir(...)` now reads that file back when present.

- Re-exported and re-validated a fresh package:
  - `tmp/war3-review/micro_feature_rhj0-2026-04-13T19-17-42-397Z`

- Current validated result for that package:
  - `workspace flavor = tstl-skeleton`
  - `workspace readiness = recognized`
  - `hostTargetValidation = aligned-with-skeleton`
  - `tstlDraftValidation = draft-slot-semantics-present`
  - `implementationDraftPlan = 3 entries, ready=no, evidenceLevel=binding-draft`
  - `runtimeHook = host-evidence-found`
  - `shopTarget = declaration-shape-only`
  - `triggerArea = rect-shape-only`

- `npm run check-types` passes after these changes.

# Next intended moves

- Primary next move:
  - decide whether the next honest step is:
    - slot-specific proof tightening against a more real TSTL host project, or
    - consumption of `implementation-draft-plan.json` by a narrower write-plan / review UI path

- The most grounded near-term move is:
  - tighten `shop-target` declaration-site evidence first
  - because `runtime-hook` already has host evidence and `trigger-area` already has at least rect-shape evidence

- A still-safe follow-up is:
  - add trigger-area realization-site proof in a real host project once a concrete region/rect declaration site exists beyond the skeleton

# Current blockers / risks

Primary blocker:

- War3 now has a narrow implementation-draft consumer artifact, but the real host workspace still does not prove:
  - shop-target declaration site
  - trigger-area generated region handle

Secondary blockers / risks:

- `implementation-draft-plan.json` is intentionally review-only and must not be mistaken for a host-write-ready plan.
- The current TSTL skeleton remains workspace-shape evidence, not KK runtime proof.
- `readyForImplementationDraft` is still `no` because three bindings remain unresolved at host-project level:
  - `trigger-area`
  - `shop-target`
  - `runtime-hook`

# Notes for other sessions

- War3 should continue using the new split:
  - host workspace validation stays conservative
  - exported draft validation checks slot semantics
  - implementation-draft plan is now the narrow downstream consumer artifact

- The freshest package to inspect is:
  - `tmp/war3-review/micro_feature_rhj0-2026-04-13T19-17-42-397Z`

- The active seam is now:
  - whether to tighten declaration-site / realization-site proof next, or start consuming `implementation-draft-plan.json` in a downstream review/write-planning path
