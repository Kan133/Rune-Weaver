# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the Dota2 V2 mainline still has the same honest blocker around dependency revalidation refresh, grounding hardening, and exploratory graduation, but the `IntentSchema` layer itself has now landed a second-round internal refactor so the recently-stabilized create-side governance core is maintainable instead of living inside another monolith. `raw facts`, `governance decisions`, and `open semantic residue` are now first-class internal layers, and `normalize / fallback / canonicalization` have been reduced to orchestration seams instead of mixed policy piles.

# Done since last update

- Landed a second-round split under `core/wizard/intent-schema/`:
  - added first-class internal semantic-layer analysis with `rawFacts`, `governanceDecisions`, and `openSemanticResidue`
  - added stable helper export `analyzeIntentSemanticLayers(...)`
  - kept the public `IntentSchema` contract unchanged
- Rebuilt the normalization flow around those layers:
  - `normalize.ts` now orchestrates candidate coercion, section normalization, semantic-layer analysis, canonicalization, and final projection
  - `fallback.ts` is now an orchestrator and consumes the raw-fact layer through `fallback-builders.ts`
  - `canonicalization.ts` is now only the pass runner, with the candidate-draw pass moved into `canonicalization-pass-candidate-draw.ts`
- Split the old oversized `intent-schema` implementation into bounded modules:
  - `normalize.ts` is now 135 lines
  - `fallback.ts` is now 27 lines
  - `canonicalization.ts` is now 52 lines
  - the remaining overweight feature normalizer was further split into `normalize-feature-sections.ts` and `normalize-selection-sections.ts`
- Preserved the create-side stability work while tightening generic prompt semantics instead of adding `talent_draw` hacks:
  - raw-fact extraction now recognizes trigger phrasing like `F4触发`
  - candidate-draw governance now accepts strong implied selection UI from multi-candidate player choice, not just literal `modal/UI` wording
  - prompt semantic hints now recognize broader Chinese phrasing for future-eligibility removal and return-to-pool semantics
- Added layer-focused tests proving:
  - same-prompt raw facts stay stable
  - paraphrase-level governance decisions stay aligned for bounded local weighted candidate-draw asks
  - explicit persistence still stays visible as governance truth
  - fallback and structured paths converge on the same governance core

# Next intended moves

1. Return to the mainline blocker and refresh dependency-driven provider/consumer revalidation evidence on top of the cleaner `IntentSchema` seam.
2. Keep reducing exploratory grounding warnings with harder exact-symbol backing instead of reopening prompt overfitting.
3. If more `IntentSchema` work is needed later, focus on the remaining shared governance-control seams across family/pattern admission rather than putting new policy back into the wizard facade.

# Current blockers / risks

- The primary blocker is still not `talent_draw` create/update drift, host bootstrap, or create-side `IntentSchema` persistence/external drift for bounded local weighted selection.
- The main blocker remains:
  - dependency-driven revalidation still needs a fresh proof pass on the cleaner planning/update/intent surface
  - exploratory grounding still needs harder exact-symbol evidence on non-family asks
  - exploratory outputs are still `requiresReview=true` until they accumulate enough repeated evidence to stabilize
- Local implementation risk after this refactor:
  - the three-layer split now exists, but governance decisions are still only consumed inside the wizard layer; the broader shared control-plane seam across family/pattern admission is not solved yet
  - `core/validation/index.ts` still re-exports test modules, which is why unrelated wizard test runs print `schema-validator.test.ts passed`; this is noisy but not a blocker for the current slice

# Notes for other sessions

- New internal seam to inspect when debugging wizard behavior:
  - `core/wizard/intent-schema/semantic-analysis.ts`
  - `core/wizard/intent-schema/raw-facts.ts`
  - `core/wizard/intent-schema/governance-decisions.ts`
  - `core/wizard/intent-schema/semantic-residue.ts`
- The three layers are now implemented as first-class internal structures:
  - `raw facts`: implemented
  - `governance decisions`: implemented
  - `open semantic residue`: implemented
- They are still internal-only this round:
  - not added to public `IntentSchema`
  - not written into CLI artifacts
  - not consumed directly by `update-intent.ts`
- Targeted validation that passed after the refactor:
  - `npm run check-types`
  - `npx tsx core/wizard/intent-schema.test.ts`
  - `npx tsx core/wizard/intent-schema-layers.test.ts`
  - `npx tsx core/wizard/update-intent.test.ts`
  - `npx tsx core/wizard/stability-harness.test.ts`
  - `npx tsx apps/cli/wizard-cli.smoke.test.ts`
