# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow seam.

This round did not widen scope. It added a War3-local pre-Blueprint normalization layer so the current slice can enter the generic Blueprint stage without immediately drifting into a broader Dota2-shaped UI interpretation.

# Done since last update

- Added a narrow adapter-local normalization module:
  - `adapters/war3/intent/pre-blueprint-normalization.ts`
- Updated:
  - `adapters/war3/intent/blueprint-trial.ts`
  - `adapters/war3/intent/index.ts`
- The normalization now does two specific things for the current validated War3 slice:
  1. if the slice only has lightweight `hint` feedback and does not claim `uiModal`, it disables the generic UI branch before BlueprintBuilder runs
  2. it copies unresolved War3 host-binding facts into Blueprint-facing assumptions
- Re-ran the current slice Blueprint trial with the explicit supported sample inputs.

Observed result after normalization:

- Blueprint is still produced successfully
- the `selection_modal` drift disappears
- current Blueprint output becomes:
  - one trigger module
  - one effect module
  - no generic UI module
- unresolved host binding for `central_shop_proxy` is now visible in Blueprint assumptions
- drift signals for the current slice drop to empty

- Re-verified `apps/workbench-ui` build and it still passes.

# Next intended moves

- The next useful move is not broad generalization.
- The next useful move is to decide where this normalized War3 Blueprint trial should be surfaced:
  1. workbench-side preview of Blueprint trial result
  2. a narrow write-preview / assembly trial downstream of this normalized Blueprint
  3. a second War3 slice only if it exercises a meaning shape different from the current one

- Keep the current interpretation honest:
  - this is a War3-local narrowing seam
  - not a claim that generic core Blueprint semantics are now fully War3-ready

# Current blockers / risks

Primary blocker:

- downstream War3 realization still depends on real host-binding truth, especially anchored shop-handle sourcing

Secondary blockers / risks:

- the current normalization is intentionally slice-specific
- the generic BlueprintBuilder itself still has no native concept of lightweight hint-only feedback modules
- this round removes current drift, but only because War3 now pre-normalizes before entering generic Blueprint

# Notes for other sessions

- Important strategic result:
  - adapter-local narrowing was enough
  - no broad core surgery was needed for this round
- Current honest chain is now:
  - intake artifact
  - current-slice bridge
  - War3 pre-Blueprint normalization
  - Blueprint trial
- This means War3 has an increasingly credible path toward core mainline, but still through a narrow, explicit seam rather than a broad claim of compatibility.
