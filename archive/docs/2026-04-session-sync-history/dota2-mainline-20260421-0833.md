> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-21
> Read when: resuming same-day Dota2 mainline work or checking whether the current blocker is still evidence closure vs structural governance drift
> Do not use for: claiming provider/consumer evidence is closed or that exploratory outputs are now stabilized

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the Dota2 V2 mainline has now upgraded `update` to the same three-layer governance shape as `create`: `raw facts -> governance decisions -> open semantic residue`, with `CurrentFeatureTruth`, `UpdateSemanticAnalysis`, and `GovernedUpdateSchema` now forming the only intended update authority. The key downstream consumers that can change execution outcome now prefer governed authority over raw candidate semantics, including `selection_pool` update merge, cross-feature grant gating, write-plan preservation, blueprint enrichment, core update-preservation authority helpers, and CLI update posture fallback. Fresh `rw-test6` dry-run evidence also shows that three bounded inventory phrasings (`存储面板`, `仓库`, `库存面板`) all converge to the same `bounded_update` inventory contract with shared preservation backbone and the same `governedChange/semanticAnalysis` seam in review artifacts. The active slice is still evidence closure rather than another structural rewrite: provider/consumer revalidation remains open, and create-side exploratory grounding for the same weighted-selection cluster still drifts enough that the fresh `rw-test6` create prompt can fail early on semantic trigger grounding before the host-level `F4` conflict is even the dominant stop.

# Done since last update

- Landed the update-intent three-layer governance upgrade:
  - added explicit update-side seams for `CurrentFeatureTruth`, prompt/current-truth raw facts, governance decisions, semantic residue, and governed delta projection
  - `runWizardToUpdateIntent` now treats LLM output as a semantic candidate only; downstream authority comes from `governedChange` and governed `delta`
  - review artifacts now carry `governedChange` and `semanticAnalysis` alongside compatibility `requestedChange`
- Migrated the most important Dota2 update consumers to governed authority:
  - `selection_pool` update merge no longer reparses prompt text to recover inventory / trigger / count semantics
  - grant-seam and write-plan preservation now gate on update governance instead of re-interpreting prose
  - generic update blueprint enrichment now consumes governed authority
  - core blueprint update-preservation helpers now prefer governed authority for trigger/choice/removal/projection checks
  - CLI update blueprint fallback posture now reads `semanticAnalysis`, not only candidate uncertainties
- Tightened the remaining write-plan preservation leak:
  - the main path no longer treats any raw prompt mention of `Lua/KV/TypeScript` as rewrite authority when update governance already says `bounded_update`
  - added regression coverage so a bounded trigger rebind that merely mentions keeping the Lua/KV shell still carries the preserved gameplay shell forward
- Revalidated the seam locally:
  - `npx tsx core/wizard/update-intent.test.ts`
  - `npx tsx adapters/dota2/update/write-plan-preservation.test.ts`
  - `npx tsx apps/cli/dota2/planning.test.ts`
  - `npx tsx apps/cli/dota2/semantic-artifacts.test.ts`
  - `npm run check-types`
- Refreshed live `rw-test6` evidence after the update-governance round:
  - update dry-run with `为该系统创建一个仓库用于存放抽取的奖励，最高16个，存放满了后无法再进行抽取。` still reaches `bounded_update`, `Can Update = true`, `Requires Regenerate = No`, and emits governed review artifacts
  - existing `rw-test6` review artifacts for `存储面板 / 仓库 / 库存面板` all show the same inventory contract seam, with the only semantic difference being `blockDrawWhenFull` when the wording explicitly asks for it
  - the same host's create dry-run remains unstable on the exploratory prompt cluster; a fresh run reached `wizard.create` LLM output but then blocked on missing reliable trigger/passive boundary instead of cleanly advancing to the host-level `F4` conflict

# Next intended moves

1. Re-tighten create-side exploratory grounding for the weighted-selection / talent-draw cluster so the same `F4 + three choices + rarity` ask stops drifting between valid local-shell intent and early trigger-boundary failure.
2. Re-run the real provider/consumer proof on a fresh host on top of the stricter provider identity, KV fragment ownership, and upgraded update seam.
3. After that evidence pass, decide whether the remaining compatibility fallbacks around `requestedChange` can be retired without breaking fixture-driven tests.

# Current blockers / risks

- The primary blocker is no longer update-side raw-schema drift where downstream Dota2 consumers silently reparsed prompt text to recover execution authority.
- The primary blocker is still evidence closure:
  - provider/consumer revalidation still needs one fresh resolved-target proof on the converged create/update surface
  - create-side exploratory grounding for the same weighted-selection cluster is still not stable enough, as shown by the fresh `rw-test6` create dry-run falling into a trigger-boundary semantic miss before the host conflict check becomes the decisive blocker
  - exploratory outputs are still `requiresReview=true` and not yet mature reusable assets
- Residual implementation risk:
  - a few `requestedChange` fallbacks still remain as compatibility shims for legacy fixtures, although the main execution path now prefers governed update authority
  - update review artifacts are cleaner, but the create lane still needs another grounding pass before it is honest to call the broader `talent_draw` exploratory cluster converged

# Notes for other sessions

- Update authority rule after this round:
  - `governedChange` is the intended execution authority
  - `semanticAnalysis` is the intended governance/readiness authority
  - `requestedChange` remains for compatibility, review, and diagnostics
- Useful live evidence paths:
  - `D:\\rw-test6\\_output\\test6-update-storage-panel-after-governance.json`
  - `D:\\rw-test6\\_output\\test6-update-warehouse-after-governance.json`
  - `D:\\rw-test6\\_output\\test6-update-inventory-after-governance.json`
  - `D:\\rw-test6\\_output\\test6-update-warehouse-after-update-intent-v2.json`
  - `D:\\rw-test6\\_output\\test6-create-dryrun-after-update-intent-v2.json`
- Current honest Dota2 mainline judgment:
  - update-side authority cleanup is materially healthier and the bounded inventory phrasing cluster is now stable through governed update seams
  - the create-side weighted-selection prompt cluster is still the narrower live blocker to watch before claiming the broader exploratory flow is stable again
