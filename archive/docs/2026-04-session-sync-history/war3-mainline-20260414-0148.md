# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update moved the current slice closer to the TSTL host path by adding explicit TSTL workspace target hints into the War3 sidecar / write-preview path.

# Done since last update

- Updated:
  - `adapters/war3/intent/war3-assembly-sidecar.ts`
  - `adapters/war3/intent/write-preview-artifact.ts`

- Added `hostTargetHints` so the current slice no longer points only at generic review path hints.

- Current TSTL-oriented target hints are now:
  - runtime hook -> `src/host/bootstrap.ts`
  - feature draft -> `src/features/setupMidZoneShop.ts`
  - host-binding review -> `rune_weaver/generated/host-binding/current-slice.json`

- Re-exported a fresh review package:
  - `tmp/war3-review/micro_feature_48l9-2026-04-13T18-31-38-110Z`

- Verified that both:
  - `sidecar.json`
  - `write-preview.json`
  now carry the TSTL host target hints

# Next intended moves

- Primary next move:
  - turn these TSTL host target hints into a shared host-target contract/schema that validator and future generation steps can both consume

- After that:
  - align current host-binding slots more explicitly against:
    - `bootstrap` seam
    - generated binding seam
    - generated rect/region seam

# Current blockers / risks

Primary blocker:

- target hints now point at the right TSTL workspace locations, but they are still advisory review artifacts rather than validated write contracts

Secondary blockers / risks:

- current skeleton module is still map-script-shaped draft content even though host target hints now point at TSTL workspace locations

# Notes for other sessions

- War3 has now crossed an important line:
  - output is no longer only “review this vague current-slice path”
  - output can now say “this review most likely belongs near these TSTL host workspace seams”

- This is still review-only and should not be overstated as write readiness.
