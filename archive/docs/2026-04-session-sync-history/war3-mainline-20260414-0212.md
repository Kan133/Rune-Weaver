# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update completed the next important shift: the three current host-binding slot validations now have a TSTL workspace-aware path instead of only a map-script-sample path.

# Done since last update

- Updated:
  - `adapters/war3/intent/review-package.ts`

- Added TSTL-aware validation behavior for the three current slice slots:
  - runtime-hook
  - shop-target
  - trigger-area

- Current TSTL workspace behavior now looks like:
  - runtime-hook -> `host-evidence-found`
    - from `src/host/bootstrap.ts`
  - shop-target -> `declaration-shape-only`
    - from `src/features/setupMidZoneShop.ts`
  - trigger-area -> `rect-shape-only`
    - from `src/features/setupMidZoneShop.ts` and `config.json`

- Re-validated the TSTL-skeleton-based review package:
  - `tmp/war3-review/micro_feature_d20d-2026-04-13T18-38-29-485Z`

- Current result is now coherent:
  - `workspace flavor = tstl-skeleton`
  - `workspace readiness = recognized`
  - `hostTargetValidation = aligned-with-skeleton`
  - `runtimeHook = host-evidence-found`
  - `shopTarget = declaration-shape-only`
  - `triggerArea = rect-shape-only`

# Next intended moves

- Primary next move:
  - start a narrow implementation-draft path for one current-slice feature against the TSTL skeleton shape

- The likely first step is:
  - emit or stage a TSTL host draft that lives beside:
    - `src/host/bootstrap.ts`
    - `src/features/setupMidZoneShop.ts`
    - RW-owned host-binding review output

# Current blockers / risks

Primary blocker:

- the skeleton and slot validations are now aligned, but current output is still review-oriented rather than a validated write contract

Secondary blockers / risks:

- `shop-target` and `trigger-area` are still only shape-level evidence inside the TSTL skeleton
- they are better grounded now, but still not proven as actual generated runtime bindings

# Notes for other sessions

- War3 is now meaningfully past the host-shape discovery phase.
- The active seam is no longer “what host should we target?”
- The active seam is now “how do we turn this aligned review path into a narrow implementation-draft path without overstating write readiness?”
