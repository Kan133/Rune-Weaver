# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow seam.

This round added a War3-local post-Blueprint / pre-Assembly sidecar so the current slice can keep moving without pretending the generic AssemblyPlan stage is already host-correct.

# Done since last update

- Consolidated around a single War3-local sidecar implementation in:
  - `adapters/war3/intent/war3-assembly-sidecar.ts`
- Exported the sidecar and its trial helper from:
  - `adapters/war3/intent/index.ts`
- Expanded the sidecar to carry:
  - trigger semantics
  - effect semantics
  - War3-local review-only write target hints
  - War3-local review-only bridge-update hints
  - unresolved host bindings
- Added a trial helper:
  - `buildWar3CurrentSliceAssemblySidecarTrial(...)`

- Ran a direct comparison between:
  1. generic Assembly trial
  2. War3-local assembly sidecar trial

Observed result:

- generic Assembly still drifts in multiple ways:
  - selects `dota2.*` patterns for a War3 slice
  - widens trigger semantics into `input.key_binding`
  - loses an honest current-slice effect/rule path
  - proposes generic `server/<id>.ts` write targets
  - emits Dota2-oriented bridge updates
- War3-local sidecar trial stays clean:
  - drift signals are empty
  - trigger stays `area-enter` / `generated-radius`
  - effect stays `neutral-shop-order-dispatch`
  - write targets remain War3-local review hints
  - bridge updates remain War3-local review hints
  - unresolved host binding remains visible

# Next intended moves

- The next useful move is not more generic Assembly experimentation.
- The next useful move should be one of:
  1. surface the War3 assembly sidecar in workbench preview
  2. connect the sidecar to the current skeleton generator / write-preview seam
  3. derive a tiny War3 host-binding manifest artifact from the sidecar for downstream runtime review

- The strongest next move is probably:
  - sidecar -> generator/write-preview seam
  - still review-oriented
  - still no claim of host-write readiness

# Current blockers / risks

Primary blocker:

- real host binding is still unresolved, especially runtime handle sourcing for anchored shop target

Secondary blockers / risks:

- current shared generic AssemblyPlan path remains too Dota2-shaped for War3
- War3 progress now depends on continuing through local seams rather than assuming immediate shared-pipeline compatibility

# Notes for other sessions

- This round clarified the architecture honestly:
  - normalized Blueprint is good enough
  - generic Assembly is still too broad
  - War3-local sidecar is the current honest downstream continuation
- Current best chain is now:
  - intake artifact
  - current-slice bridge
  - pre-Blueprint normalization
  - Blueprint trial
  - War3-local assembly sidecar
