> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-21
> Read when: resuming same-day Dota2 mainline work after the selection-outcome host proof and legacy talent-draw demo-harness realignment
> Do not use for: claiming generic outcome realization is fully stabilized beyond the bounded selection outcome seam

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: host-proof the new selection outcome layer on a fresh x-template host and realign the legacy `talent_draw` demo/evidence path to the same `selection_pool + selection_outcome` family skeleton. Fresh `D:\rw-test-1` evidence now shows two honest closures in the same wave: the real CLI create path for the old F4 talent draw prompt reaches the 5-module family skeleton, host write, runtime validation, `validate` PASS, and `doctor` PASS; and the legacy `demo:talent-draw` harness no longer falls back to the old 3-module pattern-only path and now re-proves `talent_draw_demo` on the new source-backed `selection-pool.source.json` artifact with per-object `outcome`.

# Done since last update

- Reproved the real create path for the previous F4 talent draw prompt on a fresh host:
  - seeded and initialized `D:\rw-test-1`
  - ran the old talent draw prompt through the CLI create path
  - confirmed `selection_input / selection_pool / selection_flow / selection_outcome / selection_modal`
  - confirmed host write success, runtime validation pass, workspace update, and source-backed `selection_pool` artifact write
- Confirmed the fresh host can be brought to a clean runnable state:
  - `npm run cli -- dota2 validate --host D:\rw-test-1` passes
  - `npm run cli -- dota2 doctor --host D:\rw-test-1` passes after Panorama build output is generated
- Found and fixed the real regression in the legacy demo evidence lane:
  - `scripts/talent-draw-demo-evidence` was still constructing a hand-rolled legacy `IntentSchema`, so it silently fell back to the old 3-module path
  - the demo harness now reuses the CLI Dota2 planning flow for `IntentSchema` + `Blueprint`, then overlays the source-backed fixture authoring parameters
  - smoke expectations were updated to the 5-module `selection_pool` skeleton and the new outcome-aware source artifact
- Reproved the legacy case end to end on a fresh host after the fix:
  - `npm run demo:talent-draw -- --host D:\rw-test-1 --write --force`
  - result: success
  - smoke: `24/24` passed
  - write executor: 8 files created, 0 failed
  - generated plan: 10 entries with 2 intentionally deferred `selection_outcome` entries
- Revalidated locally after the harness fix:
  - `npm run check-types`
  - `npm run cli -- dota2 validate --host D:\rw-test-1`
  - `npm run cli -- dota2 doctor --host D:\rw-test-1`

# Next intended moves

1. Re-run the provider/consumer revalidation evidence pass on top of the converged selection outcome seam.
2. Decide whether the deferred `selection_outcome.realization` lane should remain one review-required bounded seam or split into narrower first-class realizers as more cases accumulate.
3. Use the same host-proved seam to advance the equipment draw case now that `talent_draw` and the legacy demo harness both close on the new skeleton.

# Current blockers / risks

- The primary blocker is no longer the old `talent_draw` host path or its demo harness; both are now re-proved on a fresh host.
- The active Step 6 blocker remains broader evidence closure:
  - provider/consumer revalidation still needs a fresh proof pass on the converged planning/update/outcome surface
  - reveal-only weighted-card prompts still lack a proven non-family exploratory realization path
- `selection_outcome` remains review-required in workspace truth because broad generic modifier/KV synthesis is still intentionally deferred; host/runtime validation passed, but this seam is not yet a fully stabilized generic reusable asset.

# Notes for other sessions

- Canonical fresh host for this proof: `D:\rw-test-1`
- Canonical verification commands from this round:
  - `npm run demo:talent-draw -- --host D:\rw-test-1 --write --force`
  - `npm run cli -- dota2 validate --host D:\rw-test-1`
  - `npm run cli -- dota2 doctor --host D:\rw-test-1`
- The important bug was not in generator/runtime output; it was in the demo harness bypassing the CLI planning path and constructing its own stale schema surface.
- Scratch hosts created during isolation exist and should be treated as debugging history, not the canonical acceptance host for this slice.
