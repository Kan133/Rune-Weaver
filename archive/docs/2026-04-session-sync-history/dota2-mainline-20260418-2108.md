# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the real Dota2 V2 chain is now proven on the templated `talent_draw` case through create/update/delete/recreate on a real host. The active slice is no longer talent-draw lifecycle breakage; it is evidence breadth and truthfulness hardening across exploratory/dependency paths and remaining manual runtime evidence.

# Done since last update

- Fixed the real `talent_draw` chain bugs that were still breaking proof work:
  - demo create path now enriches `selection_pool` and persists source-backed workspace truth plus `selection-pool.source.json`
  - update workspace writeback now preserves source-backed fields and refreshes `integrationPoints`
  - `selection_pool` update merge now prefers the requested target hotkey, so source-backed authoring and source artifact update from `F4` to `F5` instead of staying stale
- Re-ran real commands on `D:\Rune Weaver\tmp\talent-draw-v2-proof-host`:
  - `npm run demo:talent-draw -- --host "D:\Rune Weaver\tmp\talent-draw-v2-proof-host" --write --force`
  - `npm run cli -- dota2 doctor --host "D:\Rune Weaver\tmp\talent-draw-v2-proof-host"`
  - `npm run cli -- dota2 validate --host "D:\Rune Weaver\tmp\talent-draw-v2-proof-host"`
  - `npm run cli -- dota2 lifecycle prove --host "D:\Rune Weaver\tmp\talent-draw-v2-proof-host" --addon-name talent_draw_demo --map temp --write`
- The lifecycle proof now passes end-to-end for the required steps:
  - create
  - doctor after create
  - validate after create
  - owned-scope update
  - doctor after update
  - validate after update
  - delete
  - doctor after delete
  - validate after delete
  - recreate
  - doctor after recreate
  - validate after recreate
- Verified separately that after a hotkey update:
  - workspace `integrationPoints` changes to `input.key_binding:F5`
  - `featureAuthoring.parameters.triggerKey` changes to `F5`
  - `selection-pool.source.json` changes to `F5`
  - doctor/validate still pass
- Restored the proof host back to canonical `F4` talent-draw state after verification.

# Next intended moves

1. Re-prove one true exploratory/guided-native feature through real LLM synthesis, bounded repair, validation, and `requiresReview=true` commit.
2. Re-prove one dependency provider/consumer pair and confirm dependency-driven revalidation truth on update/delete.
3. Decide whether bounded same-feature source-backed updates should preserve templated/family lifecycle classification instead of drifting to `guided_native` / `exploratory` metadata on some prompts.
4. If canonical demo evidence completeness matters this round, run `yarn dev` in the proof host and capture the missing screenshots/video before rerunning evidence refresh.

# Current blockers / risks

- The main blocker is no longer talent-draw lifecycle breakage.
- The remaining blocker is evidence breadth:
  - no fresh exploratory/guided-native proof case yet
  - no fresh dependency-revalidation proof case yet
- Canonical evidence refresh is still incomplete because:
  - the host is missing built Panorama artifacts until `yarn dev`
  - the screenshot/video pack is still manual and absent
- Some bounded same-feature updates can still land `maturity=exploratory` / `implementationStrategy=guided_native` depending on planning phrasing even when source-backed truth and owned-scope lifecycle remain preserved. This is now a truthfulness/classification issue, not a broken write path.

# Notes for other sessions

- Proof host used for the real run: `D:\Rune Weaver\tmp\talent-draw-v2-proof-host`
- Current host state after this session is restored to canonical talent-draw `F4`, with source-backed workspace truth present.
- Do not confuse the optional evidence-refresh failure with lifecycle failure:
  - `lifecycle prove` passed
  - the optional refresh step still reports incomplete because it expects built host assets plus manual screenshots/video
- The truthful talent-draw status is now:
  - templated create path writes source-backed truth
  - update preserves source-backed truth and updates it when bounded fields change
  - doctor/validate are green except for the expected host build-artifact warning before `yarn dev`
