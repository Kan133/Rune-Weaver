# Current focus

Active mainline step: 7. Productization / UX bridge

Current slice: keep Talent Draw frozen as the only canonical acceptance case, but stop treating Talent Draw replay as the day-to-day blocker. The live Dota2 mainline has now opened a second bounded mechanism case on the same real `gap-fill` chain, and the current work is to tighten that case inside its existing boundary rather than to broaden surface area, reopen architecture planning, or spend more time on Workbench polish.

# Done since last update

- Kept the real `gap-fill` path stable on `D:\test3`:
  - Workbench feature selection no longer black-screens
  - Workbench `gap-fill` uses the current UI host instead of a sample host
  - workflow-level LLM settings are centralized into repo-root `.env`
  - `gap-fill` artifacts now expose non-sensitive LLM diagnostics
- Hardened the real Dota2 chain so it can be used for bounded case progression instead of only first-case closure:
  - moved the `weighted_pool.selection_policy` anchor to the actual editable selection-policy logic in [weighted-pool.ts](/D:/Rune%20Weaver/adapters/dota2/generator/server/weighted-pool.ts)
  - changed `validate-applied` to validate approved patch presence instead of rejecting any post-apply file change
  - aligned `doctor` host-build checks with the real launch surface on `D:\test3`
  - added a narrow malformed-JSON recovery path for the OpenAI-compatible provider in [openai-compatible.ts](/D:/Rune%20Weaver/core/llm/providers/openai-compatible.ts)
  - strengthened generator-template patch guidance in [runner.ts](/D:/Rune%20Weaver/core/gap-fill/runner.ts)
- Opened the second bounded Dota2 mechanism case on the same real chain:
  - host: `D:\test3`
  - feature: `standalone_system_qdkw`
  - boundary: `weighted_pool.selection_policy`
  - prompt: tier-diverse selection with session duplicate filtering
  - real chain result: `review -> apply -> validate-applied -> doctor` all pass
- Fixed a validation false negative exposed by this second case:
  - [validation.ts](/D:/Rune%20Weaver/core/gap-fill/validation.ts) now checks `replace` operations against the approved replacement length rather than the original target range length
  - [validation.test.ts](/D:/Rune%20Weaver/core/gap-fill/validation.test.ts) now covers longer approved replacements
- Re-ran repo verification after the second-case fixes:
  - `npm run check-types`
  - `npm test`

# Next intended moves

1. Keep the second bounded case on the same boundary and tighten the generator logic so the applied strategy fully matches the instruction:
   - current good part: diversity-first tier selection and session-aware filtering
   - current residual seam: the fallback fill phase is still not explicit enough
2. Re-run the same real chain for the tightened second-case prompt:
   - `review`
   - `apply`
   - `validate-applied`
   - `doctor`
3. Only after the second case is both chain-stable and behaviorally honest should Dota2 reopen:
   - a broader second mechanism family
   - or wider gap-fill surface expansion
4. Keep Talent Draw frozen as canonical acceptance evidence, but do not spend mainline budget replaying it unless canonical closure truth changes again.

# Current blockers / risks

- The main blocker is no longer "can Dota2 `gap-fill` run on a real host?" It can.
- The current honest blocker is a bounded second-case seam inside `weighted_pool.selection_policy`:
  - the applied logic does express diversity-first selection
  - but it still underspecifies or omits the fallback "fill the remaining slots by weight" portion of the prompt
- That means the active seam is best classified as:
  - `generator/assembler capability`, with some prompt-to-plan fidelity pressure
- Workbench remains non-authoritative and is not the blocker here.

# Notes for other sessions

- Dota2 remains on step 7, but the active slice has shifted again:
  - Talent Draw stays the only canonical acceptance case
  - the day-to-day mainline blocker is now second-case bounded generalization on the real chain
- Treat the current second case as a real proof point already opened, not as planning-only work:
  - boundary: `weighted_pool.selection_policy`
  - host: `D:\test3`
  - status: chain-stable, behavior still needs one more bounded tightening pass
- Keep the architecture boundary intact:
  - structure = blueprint/pattern/generator/write
  - business logic = gap fill
  - host lifecycle = downstream CLI/adapter actions
- Do not move Dota2 host/build/launch logic into `core/gap-fill`.
- Do not reopen broad architecture/doc planning as a substitute for progressing this second bounded case.
