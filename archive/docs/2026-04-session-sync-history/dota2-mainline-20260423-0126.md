> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after the definition-only provider create-boundary fix and fresh `rw-test-3` CLI repro
> Do not use for: claiming definition-only provider shells are already write-ready end to end

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: keep the provider/consumer seam proof intact, but move the active Dota2 blocker one stage deeper. The definition-only provider create path is no longer stuck in clarification: canonicalization now recognizes definition-only provider boundaries without requiring prior consumer drift, stale open semantic residue is reconciled at ingress, and `clarification-plan` stops asking trigger/conflict questions for this bounded provider shape. Fresh `D:\rw-test-3` dry-runs for both the original `gameplay ability shell only` prompt and the stricter `grant-only provider shell` variant now pass Stage 1 with `Intent Kind: micro-feature`, `Semantic Posture: ready`, `Uncertainties: 0`, and `clarificationAuthority.blocksBlueprint = false`. The honest remaining blocker is now Blueprint synthesis for this provider shell shape, not prompt-side clarification drift.

# Done since last update

- Fixed the definition-only provider create boundary in the canonical seams instead of patching prompts:
  - `core/wizard/intent-schema/canonicalization-pass-definition-only-provider.ts` now canonicalizes any matched definition-only provider boundary, even when no consumer-side drift was present yet
  - `core/wizard/intent-schema/definition-only-provider.ts` now treats explicit negated selection/UI wording as authoritative opt-out evidence, clears stale derived `normalizedMechanics`, and exposes provider-boundary residue reconciliation helpers
  - `core/wizard/intent-schema/canonicalization.ts` now rebuilds raw facts after each canonicalization pass so post-pass semantic analysis reads current candidate truth instead of stale pre-pass facts
  - `core/wizard/clarification-plan.ts` now suppresses trigger/conflict clarification for canonical definition-only provider boundaries and ignores already-resolved activation/selection/composition residue
- Added regression coverage for the repaired boundary:
  - `core/wizard/intent-schema.test.ts` now covers definition-only canonicalization without consumer drift, stale `normalizedMechanics` cleanup, legacy clarification residue cleanup, and create-path no-clarification behavior
  - `core/wizard/update-intent.test.ts`, `core/wizard/intent-schema-layers.test.ts`, `apps/cli/dota2/planning.test.ts`, and `npm run check-types` all passed after the fix
- Re-ran the real CLI create repros on `D:\rw-test-3` with new feature ids:
  - `npm run cli -- dota2 run "Create one gameplay ability shell only..." --host D:\rw-test-3 --feature skill_provider_shell_demo`
  - `npm run cli -- dota2 run "Create one grant-only provider shell..." --host D:\rw-test-3 --feature skill_provider_shell_demo_alt`
  - both now advance past Stage 1 and stop honestly at Stage 2 Blueprint with one remaining module need instead of clarification blockers

# Next intended moves

1. Close the Stage 2 Blueprint blocker for definition-only provider shells so the create path becomes assemblable/writeable instead of stopping at one synthesized module need.
2. Decide whether the provider-shell shape should gain a first-class micro-feature realization path or stay as a bounded exploratory/module-need shape until a narrower contract lands.
3. Return to the reveal-only vs choose-one weighted-card boundary and decide whether it remains clarification-only or graduates into a separate exploratory realization lane.

# Current blockers / risks

- The old blocker is gone:
  - definition-only provider creation is no longer clarification-blocked in the normal create path
- The new blocker is:
  - Stage 2 Blueprint still stops with `FINAL_BLUEPRINT_SEMANTIC_WARNING: Standalone entity/session state semantics require synthesis via feature-owned session state.`
  - no write proof exists yet for a fresh definition-only provider feature because the current path is still `IntentSchema ready -> Blueprint blocked`, not `readyForHostWrite`
- Reveal-only weighted-card asks still do not have a proven non-`selection_pool` exploratory realization path.
- `doctor` on `D:\rw-test-3` still reports missing built HUD artifacts until `yarn dev` is run inside the host; this remains a host build warning, not a contract failure.

# Notes for other sessions

- Fresh definition-only provider review artifacts:
  - `D:\Rune Weaver\tmp\cli-review\dota2-review-1776878651320.json`
  - `D:\Rune Weaver\tmp\cli-review\dota2-review-1776878728995.json`
- Honest current claim after this round:
  - provider/consumer seam remains host-proven on `D:\rw-test-3`
  - definition-only provider create prompts now close through Stage 1 honestly and no longer ask trigger/conflict clarification
  - the next real work item is Blueprint/module synthesis for provider shells, not more prompt-side clarification patching
