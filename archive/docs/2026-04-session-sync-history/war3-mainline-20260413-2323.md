# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow seam.

This round connected the War3-local assembly sidecar to the existing generator/write-preview seam, so the current slice now has a real downstream consumer beyond explanation artifacts.

# Done since last update

- Extended `adapters/war3/intent/war3-assembly-sidecar.ts` so the sidecar now carries enough host-facing detail to drive the current skeleton generator directly:
  - trigger center coordinates
  - target binding symbol
  - effect semantics for order dispatch and hint timing
- Added:
  - `createMidZoneShopSkeletonInputFromAssemblySidecar(...)`
- Exported the new adapter from:
  - `adapters/war3/intent/index.ts`

- Updated `apps/workbench-ui/vite.config.ts`:
  - `/api/war3/skeleton-preview` no longer maps generator input directly from raw bridge fields
  - current chain is now:
    - artifact
    - bridge
    - normalized Blueprint trial
    - War3 assembly sidecar trial
    - sidecar -> skeleton generator input
    - skeleton module draft
  - response now also includes `sidecar`

- Updated `apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx`:
  - the War3 panel now shows the assembly sidecar preview in addition to bridge preview and skeleton preview

- Verified:
  - `apps/workbench-ui` build passes
  - direct sidecar-to-generator invocation succeeds
  - the resulting generator input remains the same current-slice payload:
    - trigger center `(384, 384)`
    - trigger radius `192`
    - hint text and duration
    - order id `852566`
    - target binding symbol `central_shop_proxy`

# Next intended moves

- The next best move is likely one of:
  1. expose a War3 write-preview artifact derived from the sidecar, not just a raw code textarea
  2. derive a host-binding manifest artifact beside the skeleton preview
  3. start validating one more War3 slice only if it exercises a meaning shape different from the current mid-zone shop path

- The strongest immediate move is probably:
  - sidecar -> host-binding manifest / write-preview artifact
  - still review-oriented
  - still no real host-write claim

# Current blockers / risks

Primary blocker:

- runtime host binding remains unresolved, especially anchored shop-handle sourcing and final declaration site

Secondary blockers / risks:

- the current shared generic AssemblyPlan path is still not War3-ready
- War3 progress remains honest because current downstream flow is built on local seams, not because core downstream stages are already shared-compatible

# Notes for other sessions

- Important change in truth status:
  - War3 sidecar is no longer just a review note
  - it is now a real consumed intermediate artifact in the current preview chain
- Current honest chain:
  - intake artifact
  - current-slice bridge
  - pre-Blueprint normalization
  - Blueprint trial
  - War3 assembly sidecar
  - sidecar-driven skeleton preview
