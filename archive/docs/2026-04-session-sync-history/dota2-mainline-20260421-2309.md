> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-21
> Read when: resuming same-day Dota2 mainline work after the shared selection-case runner extraction and fresh equipment draw host proof
> Do not use for: claiming generic selection-case generalization or non-selection exploratory realization is fully stabilized beyond the bounded `selection_pool + selection_outcome` seam

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: turn the new selection outcome seam into an honest reusable case lane instead of leaving `talent_draw` as a one-off proof. The Dota2 mainline now has a shared selection-case demo/evidence runner that feeds both `demo:talent-draw` and `demo:equipment-draw`, while keeping the runtime skeleton fixed at `selection_input / selection_pool / selection_flow / selection_outcome / selection_modal`. Fresh `D:\rw-test-2` evidence now proves the second sibling case on the same family: `equipment_draw_demo` writes through `selection_pool`, keeps UI unchanged, and realizes `native_item_delivery` through `selection_outcome` on a clean host with `validate` PASS and `doctor` PASS.

# Done since last update

- Extracted the old talent-only demo/evidence lane into a shared case-driven runner:
  - new shared runner lives under `scripts/selection-case-demo-evidence/`
  - `demo:talent-draw` is now a thin compatibility wrapper instead of a separate harness
  - new `demo:equipment-draw` and matching workbench fixture reuse the same case spec contract
- Kept equipment case truth in one place instead of cloning another fixture/demo data source:
  - `EQUIPMENT_DRAW_EXAMPLE` remains the only authored equipment pool truth
  - equipment fixture/demo/smoke now consume that truth instead of maintaining their own item lists
- Fixed the real pattern-resolution mismatch in the new outcome seam:
  - `selection_outcome` now resolves to `effect.outcome_realizer`
  - smoke expectations were corrected so `selection_flow` is judged honestly as the current kv+lua shell lane instead of a falsely-required resolved pattern
- Refreshed both proof lanes locally:
  - `npm run demo:talent-draw`
  - `npm run demo:equipment-draw`
  - evidence artifacts now exist for both talent and equipment demo lanes
- Reproved the equipment case end to end on a fresh host:
  - seeded and initialized `D:\rw-test-2`
  - ran `npm run demo:equipment-draw -- --host D:\rw-test-2 --write --force`
  - ran `npm run cli -- dota2 validate --host D:\rw-test-2`
  - built Panorama output for the host
  - ran `npm run cli -- dota2 doctor --host D:\rw-test-2`
  - result: fresh-host equipment create/write proof is green end to end

# Next intended moves

1. Re-run the provider/consumer revalidation evidence pass on top of the converged planning/update/outcome surface.
2. Decide whether reveal-only weighted-card prompts should stay clarification-only or gain a separate non-`selection_pool` exploratory realization path.
3. Keep observing the bounded `selection_outcome` seam and split it only if repeated proven cases show a stable first-class contract boundary instead of case-by-case pressure.

# Current blockers / risks

- The blocker is no longer whether `selection_pool` can honestly reuse the same family skeleton for native equipment outcomes; that proof now exists on `D:\rw-test-2`.
- The active Step 6 blocker remains broader evidence closure:
  - provider/consumer revalidation still needs a fresh proof pass on the converged planning/update/outcome surface
  - reveal-only weighted-card prompts still lack a proven non-family exploratory realization path
- `selection_outcome` is healthier, but it is still a bounded outcome-realization seam rather than a fully generalized downstream realization system:
  - current proof covers `attribute_bonus` and `native_item_delivery` inside the selection family
  - broader exploratory outputs still remain review-required and should not yet be treated as reusable stabilized assets

# Notes for other sessions

- Canonical fresh hosts from this wave:
  - `D:\rw-test-1` for the re-proved talent draw path and legacy demo harness
  - `D:\rw-test-2` for the new equipment draw sibling proof
- Canonical verification commands from this wave:
  - `npm run demo:talent-draw -- --host D:\rw-test-1 --write --force`
  - `npm run demo:equipment-draw -- --host D:\rw-test-2 --write --force`
  - `npm run cli -- dota2 validate --host D:\rw-test-2`
  - `npm run cli -- dota2 doctor --host D:\rw-test-2`
- The important architectural truth after this round:
  - `selection_flow` still owns draw/select/confirm/pool-commit only
  - concrete equipment delivery stays in `selection_outcome`
  - the new shared runner is proof infrastructure, not a second business-logic lane
