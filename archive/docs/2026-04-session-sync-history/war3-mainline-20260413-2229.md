# Current focus

War3 mainline is currently at Step 7: Product-facing integration, but only for a very narrow validated write-preview seam.

Exact slice:

- connect the validated War3 generator slice to a real workbench-side preview consumer
- keep the preview consumer limited to the already-validated mid-zone shop skeleton path
- avoid broad product shell expansion or claims beyond the current slice

# Done since last update

- Kept the narrow War3 generator surface in place:
  - `adapters/war3/generator/mid-zone-shop-skeleton.ts`
  - `adapters/war3/generator/index.ts`
- Added a workbench-side preview consumer for that generator.
- Landed a new dev-server endpoint in:
  - `apps/workbench-ui/vite.config.ts`
  - endpoint: `POST /api/war3/skeleton-preview`
- The endpoint conservatively maps the current War3 intake artifact into the generator input and rejects unsupported shapes instead of inventing values.
- Current supported slice for preview is intentionally narrow:
  - one confirmed trigger anchor
  - `triggerAreaMode = generated-radius`
  - non-null `triggerAreaRadius`
  - non-empty `explicitHintText`
  - non-null `hintDurationSeconds`
  - `shopTargetMode = existing-anchor`
  - non-empty `shopTargetSourceId`
  - numeric `shopOrderId`
- Added matching panel state and affordances in:
  - `apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx`
- The panel can now:
  - request skeleton preview generation
  - display the returned skeleton module draft
  - copy the skeleton preview text
- Verified `apps/workbench-ui` still builds successfully after the preview-consumer wiring.
- Verified the artifact-to-generator mapping directly with a sample artifact path and confirmed it produces the expected War3 skeleton preview string.

# Next intended moves

- Use the new skeleton preview consumer as the honest War3 product-facing seam for the current validated slice.
- Decide the next narrow integration move:
  1. tighten the workbench inputs from free-text toward more explicit enums/selects for the supported slice
  2. add one more similarly narrow generator slice
  3. begin wiring a real write-preview / file-target path once the consumer is stable enough
- Keep the current preview surface honest:
  - no unsupported host shapes
  - no invented fallback paths
  - explicit errors when the artifact does not satisfy the supported slice

# Current blockers / risks

Primary blocker:

- the current War3 preview consumer still depends on unresolved real host bindings:
  - where `central_shop_proxy` is declared or bound in a real emitted host file
  - how this preview should eventually connect to a real file target or host workspace write path

Secondary blockers / risks:

- the preview consumer only supports one narrow slice right now
- the current panel still represents the newer War3 schema fields as free-text inputs
- the repo-level `core/llm` provider path is still blocked by `401 Invalid Authentication`

# Notes for other sessions

- This round moved War3 from “generator exists” to “generator has a real workbench consumer”.
- The current honest War3 chain is now:
  - map workspace -> derived workspace -> intake artifact -> skeleton preview consumer -> War3 module draft string
- This is still slice-specific, not broad War3 host productization.
- Useful files for the next session:
  - `adapters/war3/generator/mid-zone-shop-skeleton.ts`
  - `apps/workbench-ui/vite.config.ts`
  - `apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx`
