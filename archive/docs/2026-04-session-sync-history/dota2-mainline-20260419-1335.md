# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: close the `update authority first, then planning convergence` refactor and turn it into honest initialized-host evidence. This session made source-backed module truth authoritative for update preservation, landed a real backbone-plus-facet planning model for single-ability exploratory asks, and re-proved the resulting convergence on `D:\test3`.

# Done since last update

- Reduced remaining grammar-era bias across planning/resolver/update wording:
  - `core/blueprint/seam-authority.ts`
  - `core/blueprint/blueprint-semantic-shaping.ts`
  - `core/patterns/resolver.ts`
  - `core/wizard/update-intent.ts`
  - `core/llm/prompt-packages.ts`
  - `core/llm/prompt-constraints.ts`
  - `core/retrieval/dota2-bundles.ts`
  - `core/schema/types.ts`
  - `core/blueprint/blueprint-status-policy.ts`
  - `core/blueprint/builder.ts`
- Shifted the semantics from old `admitted/current seam` wording toward:
  - governance-blocked only for real ownership/safety/host impossibility
  - safe unknown mechanics continue as `weak` plus synthesis-required
  - update preservation now prefers persisted module truth over old pattern-only hints
- Landed update-authority correctness for source-backed families:
  - `moduleRecords` plus source-backed invariant roles are now authoritative preservation truth
  - `admittedSkeleton` and legacy `selectedPatterns` are fallback-only when authoritative module truth is absent
  - `selection_pool` invariant roles now block destructive update attempts through `UPDATE_INVARIANT_CONFLICT` rather than silently washing them away
- Landed real planning convergence for eligible exploratory single-ability asks:
  - planner now creates one top-level `gameplay_ability` backbone instead of many top-level trigger/state/timing/spawn/effect modules
  - local ability semantics are attached as `moduleFacets`
  - resolver now sees one unresolved gameplay backbone need
  - synthesis now stays aligned to that planner truth instead of reconstructing convergence from scattered top-level modules
- Fixed a real product-facing bug discovered on initialized-host proof:
  - Wizard emitted negative constraints as English sentences like `The feature must not add UI.`
  - builder only filtered `Do not / no / without ...`, so the negative line was being mistaken for a positive UI requirement
  - builder now recognizes `must not / should not / feature must not ...` negative constraints, preventing accidental UI module injection
- Hardened negative-constraint handling without reintroducing grammar-era denial:
  - no-UI/no-inventory/no-persistence asks no longer auto-inject `ui.selection_modal`
  - missing reusable pattern coverage no longer forces multi-module exploratory fragmentation by itself
- Updated the affected tests to match V2 truth instead of old grammar-era assumptions:
  - `core/blueprint/builder.test.ts`
  - `core/blueprint/package6-current-seam.test.ts`
  - `core/blueprint/seam-authority.test.ts`
  - `core/patterns/resolver.test.ts`
  - `core/wizard/update-intent.test.ts`
  - `apps/cli/dota2/planning.test.ts`
- Validation that passed this round:
  - `npm run check-types`
  - `npx tsx -e "import('./core/blueprint/builder.test.ts').then((m) => m.runTests())"`
  - `npx tsx -e "import('./core/blueprint/package6-current-seam.test.ts').then((m) => m.runTests())"`
  - `npx tsx core/blueprint/seam-authority.test.ts`
  - `npx tsx core/patterns/resolver.test.ts`
  - `npx tsx core/wizard/update-intent.test.ts`
  - `npx tsx apps/cli/dota2/planning.test.ts`
- Fresh initialized-host proof on `D:\test3`:
  - proof prompt: `做一个主动技能：施放后在英雄身边召唤一个跟随自己的火球，持续5秒，每秒灼烧附近敌人。不要 UI，不要 inventory，不要 persistence，不要跨 feature，只需要一个技能。`
  - review dry-run now shows:
    - `Modules: 1`
    - `Gameplay Backbones: 1`
    - `Unresolved module needs: 1`
    - `Synthesized Bundles: 1`
    - `WritePlan entries: 2`
    - `UI routes: 0`
  - `npx tsx apps/cli/index.ts dota2 run "<proof prompt>" --host D:\test3 --write -o tmp\\final-exploratory-proof.json` passed
  - final initialized-host result:
    - host validation passed
    - runtime validation passed
    - final commit decision: `exploratory`
    - `requiresReview=true`
    - workspace state updated with feature `micro_feature_3kde`
- Latest proof artifact:
  - `D:\Rune Weaver\tmp\phase-update-authority-backbone-proof.json`
  - `D:\Rune Weaver\tmp\final-exploratory-proof.json`

# Next intended moves

1. Make must-requirement matching facet-aware so backbone facets count as authoritative coverage instead of leaving stray `Missing canonical ModuleNeed` warnings.
2. Tighten source-backed family update purity so bounded `selection_pool` updates preserve live source-backed truth without drifting into unnecessary synthesized trigger/flow pieces.
3. Continue reducing exploratory grounding warnings and then re-run provider/consumer dependency evidence on top of the cleaner planning surface.

# Current blockers / risks

- The blocker is no longer grammar gating, no-UI leakage, or single-skill bundle explosion.
- The current blocker is exploratory planning/synthesis quality on top of the converged backbone model:
  - facet/backbone requirement matching still leaves warning noise where facet coverage should count as fulfilled
  - source-backed family updates still need a cleaner preservation path so bounded updates do not over-synthesize preserved structure
  - exploratory outputs are write-capable and lifecycle-truthful now, but they remain review-required and still need further grounding/stabilization before graduation
- Do not regress by reintroducing `grammar-v1` denial, `resolver-as-gate`, or pattern-miss fragmentation just to hide synthesis uncertainty.

# Notes for other sessions

- `D:\test3` is now the freshest initialized-host proof surface for Dota2 V2 backbone convergence on a no-UI exploratory single-skill ask.
- The real lesson from this round is that the remaining risk moved from “can the system attempt this mechanic at all?” to “does the planner converge the mechanic honestly and does synthesis stay grounded on that converged plan?”
- If another session continues this lane, the highest-value next step is facet-aware requirement cleanup plus source-backed update cleanliness, then grounding/dependency evidence on top of that cleaner control plane.
