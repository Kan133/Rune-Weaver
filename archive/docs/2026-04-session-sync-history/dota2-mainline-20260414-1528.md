# Current focus

Active mainline step: 7. Productization / UX bridge

Current slice: move Dota2 from "canonical case can write and run" to "generated feature can expose a truthful post-write business-logic fill path." The specific focus of this session was not broad generator expansion. It was to make Gap Fill usable as the next layer after blueprint/pattern/generator skeleton output, while keeping that boundary out of generic core host logic.

# Done since last update

- Kept the Dota2 mainline honest after the recent review-surface pass:
  - review surface now classifies create results as success / warning / failure instead of treating strict `pipelineComplete` as the only success signal
  - this removed the false "create failed" feeling when write succeeded but follow-up host work was still recommended
- Added Dota2-side gap-fill boundary resolution from selected patterns in [boundaries.ts](/D:/Rune%20Weaver/adapters/dota2/gap-fill/boundaries.ts).
  - `rule.selection_flow` -> `selection_flow.effect_mapping`
  - `data.weighted_pool` -> `weighted_pool.selection_policy`
  - `ui.selection_modal` -> `ui.selection_modal.payload_adapter`
- Extended workspace persistence so written features can record which gap-fill boundaries they expose:
  - [types.ts](/D:/Rune%20Weaver/core/workspace/types.ts)
  - [manager.ts](/D:/Rune%20Weaver/core/workspace/manager.ts)
  - [workspace-integration.ts](/D:/Rune%20Weaver/apps/cli/helpers/workspace-integration.ts)
- Made `dota2 gap-fill` feature-aware instead of boundary-only:
  - [index.ts](/D:/Rune%20Weaver/apps/cli/index.ts)
  - [dota2-cli.ts](/D:/Rune%20Weaver/apps/cli/dota2-cli.ts)
  - the Dota2 CLI gap-fill entry flow now resolves feature context before dispatch
- Added backward compatibility for older hosts/workspaces:
  - if `gapFillBoundaries` is not yet stored in workspace, the command now derives candidates from `selectedPatterns`
  - this was verified against existing host `D:\AA`
- Added a focused Dota2 gap-fill mapping test:
  - [boundaries.test.ts](/D:/Rune%20Weaver/adapters/dota2/gap-fill/boundaries.test.ts)
- Productized the Workbench entry for feature-scoped gap fill:
  - front-end feature/workspace models now carry `gapFillBoundaries` and `integrationPoints`
  - [workspace.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/types/workspace.ts)
  - [feature.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/types/feature.ts)
  - [workspaceAdapter.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/data/workspaceAdapter.ts)
  - [useCLIExecutor.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/hooks/useCLIExecutor.ts)
  - [vite.config.ts](/D:/Rune%20Weaver/apps/workbench-ui/vite.config.ts)
  - [FeatureDetail.tsx](/D:/Rune%20Weaver/apps/workbench-ui/src/components/feature/FeatureDetail.tsx)
- The Workbench feature detail now has a Gap Fill panel that can:
  - list fillable boundaries for the selected feature
  - accept a business-logic instruction
  - execute the CLI-backed `gap-fill` path
  - reuse the existing execution/review result panel instead of inventing a second result system

# Next intended moves

1. Tighten the Workbench Gap Fill panel from MVP to usable product flow:
   - show clearer boundary labels instead of raw ids only
   - preview the exact CLI command that will run
   - explain when the user must choose a boundary versus when the system can infer one
2. Extend the path from plan-only into approval/apply UI:
   - if the result requires confirmation, surface the approval record and next step directly in Workbench
   - if auto-apply becomes acceptable for some low-risk boundaries, expose that path explicitly
3. Keep the Dota2 mainline pointed at "skeleton + muscle" rather than broad pattern inflation:
   - blueprint/pattern/generator remain structure
   - gap fill becomes business logic / muscle layer
4. After this UX slice stabilizes, choose the next Dota2 proof point:
   - either use Gap Fill to refine Talent Draw logic honestly
   - or pick a second mechanism case that exercises the same skeleton+fill model

# Current blockers / risks

- Gap Fill now has a usable feature-scoped entry, but the Workbench surface is still MVP-level:
  - boundary ids are raw
  - approval/apply is not yet a first-class UI path
  - the result panel is technically correct but still engineering-heavy
- Some existing hosts/workspaces were created before `gapFillBoundaries` was recorded. Backward compatibility exists via `selectedPatterns`, but old hosts still may present multiple valid boundaries and require manual choice.
- The current Dota2 gap-fill model still assumes generator-owned anchor files and repo-side patching, not arbitrary host-side runtime patching. That is intentional, but other sessions should not describe it as a generic full-feature code edit system yet.
- The second explorer subagent did not complete a useful audit because it was constrained from reading files. Do not rely on that result; rely on the concrete file changes above.

# Notes for other sessions

- The Dota2 mainline has moved past "one canonical case writes and runs" into "a written feature can expose business-logic fill boundaries."
- Do not move Talent Draw-specific logic back into `core/wizard`. The current direction is:
  - structure in blueprint/pattern/generator
  - business logic in gap fill
- If another session touches gap fill, the current critical files are:
  - [boundaries.ts](/D:/Rune%20Weaver/adapters/dota2/gap-fill/boundaries.ts)
  - [workspace-integration.ts](/D:/Rune%20Weaver/apps/cli/helpers/workspace-integration.ts)
  - [FeatureDetail.tsx](/D:/Rune%20Weaver/apps/workbench-ui/src/components/feature/FeatureDetail.tsx)
  - [index.ts](/D:/Rune%20Weaver/apps/cli/index.ts)
  - [dota2-cli.ts](/D:/Rune%20Weaver/apps/cli/dota2-cli.ts)
- The honest product claim right now is:
  - Workbench can write a Dota2 feature
  - workspace can expose fillable business-logic boundaries
  - Workbench can launch a CLI-backed gap-fill planning flow from a selected feature
  - approval/apply productization is the next step, not already complete
