# Current focus

War3 mainline is currently at Step 6: Validation of assumptions, but the active slice has now moved into a narrow real host skeleton landing.

Exact slice:

- connect the validated War3 contract to a real generator surface
- keep the landed skeleton tightly scoped to the already-validated mid-zone shop slice
- avoid broad product shell expansion or broad runtime claims

# Done since last update

- Kept the recent War3 contract tightening in place across:
  - trigger-area realization
  - shop target realization
  - shop order mode / order id
- Ran a minimal module-draft validation prompt and confirmed the current contract can support a narrow host-shaped output:
  - input: `tmp/war3_module_draft_prompt_short.txt`
  - result: `tmp/kimi-war3/module-draft-short-v1-2026-04-13T14-09-24-113Z.final.txt`
  - summary status: `model-output-success`
- Landed a real War3 generator surface under:
  - `adapters/war3/generator/mid-zone-shop-skeleton.ts`
  - `adapters/war3/generator/index.ts`
- The new generator owns a narrow explicit input contract:
  - `moduleName`
  - `triggerCenterX`
  - `triggerCenterY`
  - `triggerRadius`
  - `hintText`
  - `hintDurationSeconds`
  - `shopOrderId`
  - `shopTargetSymbol`
  - optional `rectBoundsOverride`
  - optional `emitExplicitUnresolvedBounds`
- The generator now emits a real TypeScript-to-Lua module draft string for the validated War3 slice rather than another free-form probe prompt.
- Corrected an initial subagent draft that had drifted toward direct Lua output and unvalidated runtime names.
- Verified the landed generator by executing it directly and inspecting the emitted skeleton string.
- Verified the new generator files pass standalone TypeScript checking:
  - `npx tsc --noEmit adapters/war3/generator/index.ts adapters/war3/generator/mid-zone-shop-skeleton.ts`

# Next intended moves

- Use the new generator surface as the narrow write target for the current War3 slice instead of continuing to expand the probe family.
- Decide where the generator should be consumed next:
  1. a War3 adapter-level write preview / artifact path
  2. a workbench-side preview bridge for the existing intake artifact
  3. a deeper host write path only if a real consumer is ready
- Keep the current slice narrow:
  - generated-radius trigger area
  - explicit hint text / duration
  - existing anchor-backed shop target
  - `IssueNeutralTargetOrderById`
- Preserve unresolved host-binding seams explicitly instead of inventing helper layers.

# Current blockers / risks

Primary blocker:

- the remaining uncertainty is now mainly real host-binding integration, not contract design:
  - where `central_shop_proxy` is actually declared or bound in a real War3 host output
  - how rect bounds are sourced when the generator is driven by real map-space data instead of hand-entered numbers

Secondary blockers / risks:

- the repo-level `core/llm` path is still blocked by `401 Invalid Authentication`
- the current War3 UI still represents the newer schema fields as free-text rather than tighter enum/select controls
- the new generator is currently a narrow standalone surface and is not yet wired into a real write or preview consumer

# Notes for other sessions

- This round is the first one where War3 mainline landed an actual generator/skeleton surface rather than only parser/workbench/probe artifacts.
- The generator is intentionally narrow and should be treated as:
  - evidence-backed
  - slice-specific
  - not yet a general War3 host generator
- The best current handoff from validation into real code is:
  - `adapters/war3/generator/mid-zone-shop-skeleton.ts`
- The next useful work should connect this generator to one real consumer, not reopen broad schema or prompt exploration.
