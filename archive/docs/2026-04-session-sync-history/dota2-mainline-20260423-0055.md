> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after the fresh `rw-test-3` provider/consumer proof and CLI evidence refresh pass
> Do not use for: claiming the definition-only provider create boundary or reveal-only weighted-card boundary is fully closed

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: close the provider/consumer seam with fresh host proof instead of leaving it as test-only confidence. Fresh `D:\rw-test-3` proof now shows three things on top of the converged `selection_pool + selection_outcome` path: the canonical CLI-backed talent evidence refresh path still works on a clean host after the new selection outcome seam, a real provider export plus consumer binding can be landed on host truth without mutating `selection_pool` source authoring, and a local-only consumer update preserves the cross-feature sidecars and dependency edge instead of dropping them.

# Done since last update

- Created a fresh x-template host at `D:\rw-test-3`, initialized it, and corrected the real host order dependency:
  - `demo:talent-draw` before `dota2 init` produced files but left the fresh workspace uninitialized for later CLI refresh
  - rerunning in the honest order `init -> demo write -> refresh` closed the CLI evidence path cleanly
- Re-proved the canonical talent evidence lane on the fresh host without redoing screenshots or runtime video:
  - `npm run cli -- dota2 demo prepare --host D:\rw-test-3 --addon-name talent_draw_demo --map temp --write`
  - `npm run demo:talent-draw -- --host D:\rw-test-3 --write --force`
  - `npm run demo:talent-draw:refresh -- --host D:\rw-test-3`
  - `npm run cli -- dota2 doctor --host D:\rw-test-3`
  - `npm run cli -- dota2 validate --host D:\rw-test-3`
- Landed a real provider/consumer seam on the fresh host and refreshed bridge truth:
  - added provider export artifact for `skill_provider_demo`
  - added matching KV/Lua ability identity for `rw_skill_provider_demo`
  - added `selection-grant-contract.json` and `selection-grant-bindings.json` for `talent_draw_demo`
  - updated workspace dependency truth and refreshed bridge wiring
- Re-proved the seam through validation instead of only internal tests:
  - `validate` now passes with `provider_ability_exports: All 1 provider export(s) align with KV/Lua identity`
  - `doctor` sees a 2-feature workspace with bridge wiring intact
- Re-proved consumer-side preservation on the fresh host:
  - ran `npm run cli -- dota2 update "把天赋抽取的触发键从 F4 改成 F5" --host D:\rw-test-3 --feature talent_draw_demo --write`
  - CLI update recreated/preserved `selection-grant-bindings.json` and `selection-grant-contract.json`
  - workspace now keeps `dependsOn`, `featureContract.consumes(grantable_primary_hero_ability)`, and required `dependencyEdges`
- Re-ran delete-style dependency revalidation analysis against the fresh workspace:
  - simulated provider removal for `skill_provider_demo`
  - result blocks because `talent_draw_demo` has a required dependency on `grantable_primary_hero_ability`

# Next intended moves

1. Decide whether to productize this proof as a first-class provider/consumer demo or lifecycle harness instead of relying on host seeding plus validation commands.
2. Fix the definition-only provider create boundary so a `grant_only` provider shell can be created through the normal CLI create path without trigger/passive-boundary clarification drift.
3. Return to the remaining reveal-only vs choose-one weighted-card boundary and decide whether it stays clarification-only or graduates into a separate exploratory realization lane.

# Current blockers / risks

- The provider/consumer seam itself is in better shape now; the fresh-host proof is no longer missing for the current selection consumer path.
- The active blocker has shifted to prompt-side creation boundaries:
  - definition-only `grant_only` provider prompts still trigger clarification instead of closing honestly through the create chain
  - reveal-only weighted-card asks still do not have a proven non-`selection_pool` exploratory realization path
- `doctor` still reports missing built HUD artifacts on the fresh host until `cd D:\rw-test-3 && yarn dev` is run; this is a host build warning, not a selection/cross-feature contract failure.
- The current provider/consumer host proof is honest, but part of it still relies on explicit host-side contract seeding rather than a fully stabilized end-user-facing CLI scenario.

# Notes for other sessions

- Fresh host for this wave: `D:\rw-test-3`
- Canonical commands actually run this wave:
  - `npm run cli -- dota2 init --host D:\rw-test-3 --addon-name talent_draw_demo`
  - `npm run demo:talent-draw -- --host D:\rw-test-3 --write --force`
  - `npm run demo:talent-draw:refresh -- --host D:\rw-test-3`
  - `npm run cli -- dota2 doctor --host D:\rw-test-3`
  - `npm run cli -- dota2 validate --host D:\rw-test-3`
  - `npm run cli -- dota2 update "把天赋抽取的触发键从 F4 改成 F5" --host D:\rw-test-3 --feature talent_draw_demo --write`
- Honest current claim after this round:
  - selection-case CLI evidence refresh is still healthy on a fresh host
  - provider export identity, consumer binding sidecars, bridge wiring, and dependency revalidation now have fresh host-backed proof on the current `selection_pool` consumer path
  - definition-only provider creation through the normal create prompt is still not closed and should remain an explicit next blocker instead of being hand-waved as solved
