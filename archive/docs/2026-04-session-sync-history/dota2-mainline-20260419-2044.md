# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the `update authority first, then planning convergence` refactor is now closed with real initialized-host evidence. The active slice has moved to dependency-driven revalidation evidence refresh, synthesis grounding hardening, and exploratory graduation triage on top of the cleaner backbone/update surface.

# Done since last update

- Closed the remaining update-authority correctness work:
  - source-backed invariant roles now outrank generic projected module roles in update preservation
  - bounded source-backed updates can use an authoritative projection path instead of re-planning from noisy preserve echoes
  - preserved `stateModel` / `composition` / `integrations` restatements no longer wash bounded `selection_pool` updates into unnecessary synthesized structure
- Closed the remaining backbone/facet convergence cleanup:
  - must-requirement matching is now facet-aware, so backbone modules satisfy canonical coverage through `moduleFacets`
  - the fireball no-UI exploratory proof no longer emits false `Missing canonical ModuleNeed` warning noise
- Fixed the Talent Draw canonical create-path drift that was still poisoning update safety:
  - `scripts/talent-draw-demo-evidence/pipeline.ts` now derives the fixture schema from mechanics rather than noisy functional prose
  - the canonical create path no longer injects an extra synthesized rule artifact into `talent_draw`
- Re-proved the key Dota2 V2 evidence on `D:\test3`:
  - fireball/no-UI review proof converges to `Modules: 1`, `Gameplay Backbones: 1`, `Unresolved module needs: 1`, `Synthesized Bundles: 1`, `UI routes: 0`
  - bounded `selection_pool` update proof keeps source-backed structure clean instead of exploding into exploratory rebuild noise
  - `dota2 lifecycle prove --write` passes again, including Talent Draw create/update/delete/recreate plus doctor/validate checkpoints
- Validation that passed in this closure slice:
  - `npm run check-types`
  - `npx tsx -e "import('./core/blueprint/builder.test.ts').then((m) => m.runTests())"`
  - `npx tsx core/wizard/update-intent.test.ts`
  - `npx tsx core/blueprint/seam-authority.test.ts`
  - `npx tsx core/patterns/resolver.test.ts`
  - `npx tsx adapters/dota2/synthesis/index.test.ts`
  - `npx tsx apps/cli/dota2/planning.test.ts`

# Next intended moves

1. Refresh provider/consumer dependency evidence on the cleaned control plane so contract-driven revalidation is re-proved on the post-convergence surface.
2. Reduce the remaining exploratory host grounding and deferred warning noise with harder exact-symbol evidence, then re-run the same initialized-host proof prompt.
3. Decide which repeated exploratory outputs are mature enough to stabilize or graduate into reusable assets and which should remain review-required only.

# Current blockers / risks

- The blocker is no longer grammar gating, no-UI leakage, single-skill planning explosion, facet-warning noise, source-backed update purity, or Talent Draw lifecycle instability.
- The current blocker is exploratory quality and stabilization:
  - dependency-driven revalidation evidence needs a fresh proof pass on the converged backbone/update surface
  - synthesis grounding still leaves some host evidence and deferred warning noise on exploratory artifacts
  - exploratory outputs remain `requiresReview=true` and are not yet graduated into stabilized reusable assets
- Do not reopen old grammar-era denial or legacy pattern-id preservation shortcuts just to suppress uncertainty; the remaining work is evidence quality, not reintroducing harder gates.

# Notes for other sessions

- `D:\test3` is still the freshest initialized-host proof surface for Dota2 V2.
- The freshest evidence artifacts for this closed slice are:
  - `D:\Rune Weaver\tmp\phase-facet-warning-proof.json`
  - `D:\Rune Weaver\tmp\phase-source-backed-update-proof.json`
  - `D:\Rune Weaver\tmp\final-exploratory-proof.json`
  - `D:\Rune Weaver\tmp\cli-review\lifecycle-proof-1776602504510.json`
- Parallel knowledge cleanup should stay in the additive staging lane until the mainline explicitly enters retrieval/corpus cutover work.
- If another session picks up Dota2 mainline next, it should start with dependency evidence plus grounding hardening, not with another round of grammar/update-authority rewrites.
