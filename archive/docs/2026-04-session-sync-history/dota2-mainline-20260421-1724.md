> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-21
> Read when: resuming same-day Dota2 mainline work after the latest create-side weighted-card clarification cleanup
> Do not use for: claiming reveal-only gacha semantics are now fully realized without clarification or that exploratory create outputs are generally stabilized

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the Dota2 V2 create lane has been narrowed further on the weighted-selection / card-draw cluster. Fresh `rw-test11` dry-run evidence now shows that the ambiguous `抽卡 / 三张卡片 / 稀有度影响概率和外观` prompt no longer gets polluted by weight-detail clarification noise or by `selection_pool` over-escalating an under-specified prompt into family-level governance blocking. The same prompt now collapses to one honest `selection_flow` clarification about whether the shown cards are selectable/committable or only displayed. On the same host, the explicit choose-one variant (`从三张里选一张，未选回池，已选后续不再出现`) reaches `selection_pool` `admitted_explicit`, `FinalBlueprint ready`, `AssemblyPlan ready`, and dry-run `committable`.

# Done since last update

- Cleaned up create-side semantic residue classification for the weighted-card cluster:
  - exact rarity-weight / exact visual-treatment gaps now classify as `bounded_detail_only` instead of blocking clarification
  - display-vs-choice ambiguity now maps to `selection_flow` instead of a vague UI/presentation bucket
- Tightened the create clarification surface:
  - the old generic “semantic boundary open” question is replaced with a direct `clarify-selection-flow` question about whether the shown candidates should be chosen/committed or just resolve without follow-up choice
- Tightened `selection_pool` prompt-shape detection without hardcoding the specific case:
  - generic detection now recognizes `抽卡`, `卡片`, and `三张卡片`-style count/UI phrasing
- Separated family decline from family governance blocking:
  - `selection_pool` no longer turns a merely under-specified create prompt into `governance_blocked`
  - `declined` family admission no longer surfaces as a hard blocker by itself
- Revalidated locally:
  - `npx tsx core/wizard/intent-schema-layers.test.ts`
  - `npx tsx adapters/dota2/families/selection-pool/authoring.test.ts`
  - `npm run check-types`
- Reproved the live case on `D:\rw-test11`:
  - ambiguous prompt dry-run now stops at one honest clarification only
  - explicit choose-one / return-to-pool / remove-selected variant runs through Stage 10 in dry-run and finishes `committable`

# Next intended moves

1. Decide whether reveal-only weighted card prompts should remain clarification-only, or whether Rune Weaver needs a separate non-`selection_pool` exploratory realization path for true gacha-style “show results without choose-one commit” semantics.
2. Re-run the provider/consumer revalidation evidence pass on top of the cleaner create/update seams.
3. If Workbench UX still feels rough here, tighten how the single `clarify-selection-flow` question is surfaced so the user sees the real semantic choice instead of a generic create failure.

# Current blockers / risks

- The blocker is no longer create-side weight-detail clarification noise.
- The blocker is no longer `selection_pool` misclassifying an under-specified prompt as a family governance violation.
- The current create-side blocker is narrower and more honest:
  - reveal-vs-choose semantics for weighted card-draw prompts are still under-specified in prompts like `弹出三张卡片`
  - Rune Weaver does not yet have a proven non-family exploratory realization path for reveal-only gacha semantics once the prompt intentionally does not mean `choose 1`
- The broader Dota2 Step 6 blocker still remains evidence closure:
  - provider/consumer revalidation still needs a fresh proof pass on top of the converged seams
  - exploratory outputs outside the explicit `selection_pool` contract are still not mature reusable assets

# Notes for other sessions

- Useful fresh evidence:
  - ambiguous prompt review artifact: `D:\Rune Weaver\tmp\cli-review\dota2-review-1776763348517.json`
  - explicit choose-one variant review artifact: `D:\Rune Weaver\tmp\cli-review\dota2-review-1776763436209.json`
- Honest judgment after this round:
  - create-side weighted-card prompts are healthier because the failure surface now reflects the real semantic ambiguity
  - explicit local choose-one weighted-card systems are re-proved on `rw-test11`
  - reveal-only / informational-card gacha semantics are still an open design-and-realization question, not a finished capability
