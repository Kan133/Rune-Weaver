# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update completed the first workspace-level validation bridge between:

- classic map-script sample shape
- TSTL skeleton workspace shape

# Done since last update

- Extended War3 validator result with workspace-level shape recognition:
  - `map-script-sample`
  - `tstl-skeleton`
  - `unknown`

- Added direct workspace-shape validation entrypoint so War3 host shape can be checked by path without first creating a review package.

- Added script:
  - `scripts/war3-validate-workspace-shape.ts`

- Added npm script:
  - `war3:validate-workspace`

- Verified current sample results:
  - `tmp/war3-samples/PyWC3/maps/test.w3x` -> `map-script-sample`, `recognized`
  - `tmp/war3-tstl-skeleton/maps/demo.w3x` -> `tstl-skeleton`, `recognized`

- The TSTL skeleton result is intentionally conservative:
  - recognized as workspace shape
  - not treated as proven runnable KK runtime integration

# Next intended moves

- Primary next move:
  - define a minimal TSTL host workspace contract schema that both validator and future host-target mapping can consume

- After that:
  - align current host-binding slots against TSTL workspace targets such as:
    - `src/host/bootstrap.ts`
    - generated host bindings
    - generated rect/region seam

# Current blockers / risks

Primary blocker:

- the TSTL workspace shape is now recognized, but current host-binding outputs still point mostly to review-oriented generic path hints rather than TSTL workspace targets

Secondary blockers / risks:

- workspace recognition alone must not be mistaken for runtime proof
- KK packaging and runtime specifics are still not fully validated even though the host shape is now clearer

# Notes for other sessions

- War3 now has two honest workspace lenses:
  - map-script sample lens
  - TSTL workspace lens

- This is a real step forward because future War3 work can now target the TSTL skeleton directly instead of arguing abstractly about host shape.
