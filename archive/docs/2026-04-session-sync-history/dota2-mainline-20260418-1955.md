# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: Dota2 V2 governance-first code closure and root baseline ratification are now landed. The active slice is no longer migration naming or v1 grammar cleanup; it is to prove the closed V2 chain on real evidence: templated lifecycle stability, exploratory/guided-native synthesis with review-required commit, dependency-driven revalidation, and maintenance-command truth under the same final gate semantics.

# Done since last update

- Landed the real Dota2 synthesis path for guided-native / exploratory asks instead of treating synthetic `dota2.exploratory_ability` as the main new-path mechanism.
- Repositioned old gap-fill logic into bounded `LocalRepair / muscle fill`.
- Landed dependency-driven revalidation and final commit decision as the end-of-chain lifecycle authority.
- Tightened maintenance command semantics so delete / rollback no longer bypass dependency governance and now surface final commit decision in review artifacts.
- Removed the old regenerate-side fallback workspace patch that could drift away from the main final gate.
- Ratified root baseline docs to V2 governance-first semantics so Dota2 no longer depends on a separate migration truth to explain current behavior.

# Next intended moves

1. Re-prove one stable templated feature through create/update/regenerate.
2. Re-prove one exploratory or guided-native feature through synthesis, repair, validation, and review-required commit.
3. Re-prove dependency contract behavior with one provider/consumer pair.
4. Decide which repeated exploratory outputs are ready to graduate into reusable pattern/family assets.

# Current blockers / risks

- The main blocker is fresh evidence, not missing architecture.
- Exploratory/guided-native outputs still require review and should not be described as stabilized by default.
- Baseline docs are now aligned, but older planning and historical grammar-era docs still exist and can confuse sessions that skip routing/control docs.

# Notes for other sessions

- Dota2 current mainline is Step 6 evidence closure on a ratified V2 baseline.
- The current truthful Dota2 chain is:
  - `IntentSchema -> Blueprint Stage -> Strategy Selection -> ArtifactSynthesis / Assembly -> LocalRepair -> Validation / Final CommitDecision -> Workspace Lifecycle`
- Old `grammar-v1`, `gap-fill-as-primary`, and blueprint-authority-stack wording are no longer target truth.
- Use root baseline docs first, then the Dota2 V2 host-specific doc for Dota2-specific implementation detail.
