# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update extended the validator from runtime-hook evidence into shop-target declaration-site validation.

# Done since last update

- Added structured `shop-target` validation in:
  - `adapters/war3/intent/review-package.ts`

- Added shop-target evidence classification:
  - `symbol-declared`
  - `declaration-shape-only`
  - `no-declaration-evidence`
  - `workspace-unrecognized`

- Validator now inspects host evidence files derived from the current host root, including:
  - map script entry
  - PyWC3-style map python entry when derivable
  - PyWC3-style generated `df/<map>.py` definitions file when derivable

- Re-ran validation against:
  - `tmp/war3-review/micro_feature_55l6-2026-04-13T16-12-22-795Z`

- Result now explicitly says:
  - `runtimeHookEvidence = map-script-anchor-only`
  - `shopTargetEvidence = declaration-shape-only`

- Concrete current finding:
  - `central_shop_proxy` is **not** proven as an existing declared symbol
  - but the sample host exposes declaration shapes that could carry such a symbol:
    - map-level generated globals (`gg_*`)
    - PyWC3 definitions import shape
    - handle / unit wrapper shape

# Next intended moves

- Primary next move:
  - either validate `trigger-area` with the same evidence-level discipline
  - or stop broadening validator categories and move toward defining the expected TSTL host workspace shape for KK 1.29

- My current recommendation:
  - do one narrow `trigger-area` pass if needed for parity
  - then pivot to modeling / obtaining a true TSTL-oriented host project shape, because PyWC3 is already close to its proof limit

# Current blockers / risks

Primary blocker:

- current evidence is still shape-level for `shop-target`, not actual target-symbol proof
- `central_shop_proxy` remains a semantic binding symbol, not a validated declared host symbol

Secondary blockers / risks:

- PyWC3 gives useful host-shape evidence, but it is still not the final KK + TypeScript-to-Lua project model
- if we overfit validator logic to PyWC3 assumptions, we may validate the wrong declaration-site contract

# Notes for other sessions

- Important nuance:
  - `declaration-shape-only` is a useful narrowing result
  - it must not be read as “shop target is already declared”

- Current validator posture is now more honest:
  - runtime-hook: map-script evidence only
  - shop-target: declaration shape only

- The remaining work is increasingly about validating the right target host model, not just adding more artifact-side structure.
