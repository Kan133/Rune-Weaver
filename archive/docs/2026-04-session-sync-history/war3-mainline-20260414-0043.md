# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update completed the current evidence-level validator loop for the three still-open host-binding slots:

- runtime-hook
- shop-target
- trigger-area

# Done since last update

- Added structured `trigger-area` validation in:
  - `adapters/war3/intent/review-package.ts`

- Trigger-area evidence classification now distinguishes:
  - `region-materialization-evidence`
  - `rect-shape-only`
  - `no-trigger-area-evidence`
  - `workspace-unrecognized`

- Re-ran validation against:
  - `tmp/war3-review/micro_feature_55l6-2026-04-13T16-12-22-795Z`

- Current evidence posture is now:
  - runtime-hook: `map-script-anchor-only`
  - shop-target: `declaration-shape-only`
  - trigger-area: `rect-shape-only`

- This makes the current sample’s proof ceiling explicit:
  - it contains useful host-shape evidence
  - but it still does not prove actual TSTL host-entry / declaration binding for the target model

- Also checked external references for a more realistic KK + TSTL host shape:
  - KK public developer entry exists, but no clear public TSTL workspace template was found from it
  - the strongest public reference shape remains the War3 TypeScript ecosystem (`w3ts` / `wc3-ts-template`)

# Next intended moves

- Primary next move:
  - stop deepening PyWC3-based validation unless new evidence appears

- Recommended continuation:
  - define or obtain the expected KK 1.29 + TypeScript-to-Lua host workspace shape
  - then re-run the same validator logic against that shape

- If we need to keep moving locally before that:
  - model a provisional TSTL host workspace contract based on `w3ts` / `wc3-ts-template`
  - clearly mark it as inferred, not KK-proven

# Current blockers / risks

Primary blocker:

- we still do not have a real KK 1.29 + TypeScript-to-Lua host workspace sample or authoritative public template

Secondary blockers / risks:

- PyWC3 is now near its usefulness limit for this mainline
- continuing to validate against PyWC3 risks proving the wrong host model too confidently

# Notes for other sessions

- The validator is now doing the honest thing:
  - it distinguishes shape-level evidence from true declaration / host-entry proof

- Current best summary:
  - runtime-hook: there is an entry seam, but only map-script level
  - shop-target: there are declaration shapes, but no target symbol proof
  - trigger-area: there are rect/region shapes, but no concrete generated region-handle proof

- Best next step is no longer “add more artifact semantics”.
- Best next step is “get the right host workspace shape in hand”.
