# Current focus

Active mainline step: 7. Productization / UX bridge

Current slice: keep Talent Draw as the one canonical skeleton+fill Dota2 case, but shift the immediate truth from Workbench-facing review polish to CLI-host evidence closure on a fresh host. In this slice, the mainline question became narrower: can a clean x-template host be initialized, written, refreshed, and produce an honest canonical evidence pack with no automatic blockers left.

# Done since last update

- Completed a real CLI-only host loop on a fresh host at `D:\RN`:
  - `npm run cli -- dota2 init --host D:\RN --addon-name rn`
  - `yarn install` in `D:\RN`
  - `npm run demo:talent-draw -- --host D:\RN --write --force`
  - `npm run cli -- dota2 repair --host D:\RN --safe`
  - `npm run demo:talent-draw:refresh -- --host D:\RN`
  - `npm run cli -- dota2 doctor --host D:\RN`
  - `npm run cli -- dota2 validate --host D:\RN`
- The RN host now has a real Rune Weaver workspace and a written Talent Draw feature:
  - workspace addon name is `rn`
  - feature id is `talent_draw_demo`
  - generated files count is `8`
  - entry bindings include server import and Panorama HUD mount
- The canonical evidence refresh flow is now proven against a real host, not only tests:
  - [manifest.json](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/manifest.json) is written
  - [review-artifact.json](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/review-artifact.json) is present
  - [generated-files.json](/D:/Rune%20Weaver/docs/talent-draw-case/demo-evidence/latest/generated-files.json) is present
  - `missingAutoEvidence` is now empty
- The current canonical evidence pack truth on RN is:
  - doctor exit code `0`
  - validate exit code `0`
  - review artifact present
  - canonical contract present
  - completeness is still `incomplete` only because manual screenshots are missing
- The new evidence completeness layer is now proven in real usage, not just unit/smoke tests:
  - it correctly reported `review-artifact.json` missing before the write path ran
  - it correctly reported `missingAutoEvidence: []` after the write/refresh loop succeeded
  - it still reports missing manual screenshots honestly

# Next intended moves

1. Keep Dota2 on the narrowest remaining honest blocker:
   - build the host assets on `D:\RN` with `yarn dev`
   - then do the manual runtime pass when needed
2. Use the existing root checklist as the manual acceptance queue:
   - [DOTA2-GAP-FILL-E2E-CHECKLIST.md](/D:/Rune%20Weaver/DOTA2-GAP-FILL-E2E-CHECKLIST.md)
3. When runtime verification resumes, the next exact path should be:
   - `cd D:\RN && yarn dev`
   - `cd D:\RN && yarn launch rn temp`
   - collect runtime screenshots and video
4. Only after that manual runtime pass should Dota2 decide whether the current canonical path is truly acceptance-complete.

# Current blockers / risks

- The main automatic blocker is gone; the remaining blocker is runtime-side/manual evidence closure.
- `doctor` still warns that host build artifacts are missing or stale until `yarn dev` is run in `D:\RN`.
- The Dota directory report still resolves through the Steam-side `x_template` link path while addon config and workspace both say `rn`. This did not block CLI write/refresh, but it is still a runtime-risk worth watching before launch.
- Workbench review surface is intentionally not the blocker in this slice. Do not reopen UI polish work unless it directly blocks the canonical runtime pass.

# Notes for other sessions

- Dota2 is still on step 7, but the active sub-slice is now better described as:
  - CLI/evidence closure on a clean host is proven
  - runtime/manual evidence is the remaining gap
- The honest current claim is:
  - a clean x-template host can now be initialized
  - Talent Draw can be written through the CLI path
  - refresh produces a canonical evidence pack with `review-artifact.json`
  - all automatic evidence blockers are cleared
  - only manual screenshots/runtime validation remain
- Do not say “canonical acceptance is complete” yet. Say instead:
  - canonical CLI closure is proven
  - runtime acceptance is pending
- If another session touches this lane, prioritize:
  - `D:\RN` runtime build/launch verification
  - Steam link/runtime path consistency if launch behaves strangely
  - manual evidence capture
