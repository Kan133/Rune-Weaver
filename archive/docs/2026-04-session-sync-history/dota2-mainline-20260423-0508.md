> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after the reveal-only weighted-card boundary closure
> Do not use for: claiming reveal-only runtime is already a promoted reusable asset

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the reveal-only weighted-card boundary is now honestly closed. Ambiguous weighted-card asks no longer slide into `selection_pool` or synthesized writeability through low-risk assumption; they block on one structural clarification. Explicit `reveal_batch_immediate` asks now follow a proven non-family exploratory continuation that writes one gameplay backbone plus one UI shell, stays `implementationStrategy = exploratory`, and keeps `requiresReview = true`. Fresh host proof on `D:\rw-test20` now passes create/write, validate, Panorama build, and doctor without leaking a provider-export sidecar.

# Done since last update

- Added explicit selection resolution truth so create-side semantics distinguish:
  - `player_confirm_single`
  - `reveal_batch_immediate`
- Tightened prompt and intent governance for weighted-card asks:
  - ambiguous “show 3 cards” wording now raises `clarify-selection-flow`
  - explicit “all 3 resolve immediately as one batch” wording now suppresses `playerChoice`
  - explicit choose-one prompts continue to route to `selection_pool`
- Closed the family boundary cleanly:
  - `selection_pool` no longer reports reveal-batch as a matched-then-declined near miss
  - reveal-only semantics stay outside the family instead of polluting family stats, design draft, or commit routing
- Landed the exploratory reveal continuation:
  - reusable pieces still come from `input.key_binding` and `data.weighted_pool`
  - runtime reveal/batch resolution now synthesizes as one gameplay bundle
  - UI now synthesizes as one reveal-only surface shell instead of reusing `selection_modal`
- Tightened an adjacent overreach seam while proving the path:
  - Dota2 provider export no longer emits just because a feature owns a gameplay ability
  - reveal-only/no-cross-feature-grants features therefore do not leak `dota2-provider-ability-export.json`
- Re-proved the boundary on fresh hosts:
  - negative proof on `D:\rw-test15`: ambiguous weighted-card prompt blocked before blueprint/write
  - positive proof on `D:\rw-test20`: explicit reveal-batch prompt wrote successfully, `validate` passed, Panorama build passed, `doctor` passed
- Regression coverage passed for the reveal boundary and adjacent seams:
  - `npx tsx core/wizard/intent-schema-layers.test.ts`
  - `npx tsx core/wizard/intent-schema.test.ts`
  - `npx tsx core/patterns/resolver.test.ts`
  - `npx tsx adapters/dota2/families/selection-pool/authoring.test.ts`
  - `npx tsx adapters/dota2/synthesis/index.test.ts`
  - `npx tsx adapters/dota2/bridge/bridge-wiring.test.ts`
  - `npx tsx adapters/dota2/cross-feature/grant-seam.test.ts`
  - `npx tsx adapters/dota2/ui/component-naming.test.ts`
  - `npm run check-types`

# Next intended moves

1. Reduce exploratory synthesis grounding risk on the remaining non-family write paths now that reveal-only semantics no longer blur family admission.
2. Decide which repeated exploratory outputs are honest candidates for promotion packets, and which should remain unpromoted shells.
3. Keep the generic contract layer narrow until there is real reuse pressure beyond `data + capability`; do not generalize `event/state/integration` prematurely.

# Current blockers / risks

- Primary blocker: exploratory synthesis grounding is still too loose on some non-family asks even when the routing boundary is now honest.
- Reveal-only weighted-card is now a proven exploratory continuation, but it is still not a promoted pattern or family and must not be sold as stabilized reuse.
- The current reveal-batch runtime remains intentionally narrow:
  - exactly 3 revealed weighted candidates
  - no follow-up selection
  - no inventory/persistence/cross-feature grants
- Provider/consumer typed contract governance remains healthy after this change, but that control-plane win does not reduce the review burden on exploratory synthesis outputs.

# Notes for other sessions

- Fresh proof artifacts:
  - `D:\Rune Weaver\tmp\cli-review\reveal-ambiguous-proof.json`
  - `D:\Rune Weaver\tmp\cli-review\reveal-batch-proof-rw-test20.json`
- Fresh proof host:
  - `D:\rw-test20`
- Key code seams for this closure:
  - `core/wizard/intent-schema/normalize-selection-sections.ts`
  - `core/wizard/intent-schema/semantic-analysis.ts`
  - `core/blueprint/module-planning.ts`
  - `core/patterns/resolver.ts`
  - `adapters/dota2/families/selection-pool/fallback-detection.ts`
  - `adapters/dota2/cross-feature/grant-seam.ts`
  - `adapters/dota2/ui/component-naming.ts`
- `rw-test20` workspace truth now records the feature as exploratory with `requiresReview = true`, and no `dota2-provider-ability-export.json` is present under the feature runtime artifacts.
- Mainline truth shifts back to exploratory grounding reduction and promotion judgment; reveal-vs-choose routing is no longer the active blocker.
