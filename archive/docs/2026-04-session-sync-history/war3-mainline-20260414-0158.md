# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update formalized TSTL host target hints and then validated them against the TSTL skeleton workspace.

# Done since last update

- Updated:
  - `adapters/war3/intent/war3-assembly-sidecar.ts`
  - `adapters/war3/intent/write-preview-artifact.ts`
  - `adapters/war3/intent/review-package.ts`
  - `adapters/war3/intent/index.ts`
  - `scripts/war3-validate-review-package.ts`

- `hostTargetHints` is no longer just a loose object; it is now a small structured TSTL host target contract with separate entries for:
  - `runtime-hook`
  - `feature-module`
  - `host-binding-review`

- Added `hostTargetValidation` to the review-package validator.

- Re-exported a TSTL-skeleton-based review package using:
  - `tmp/war3-schema-tightened-artifact-tstl-skeleton.json`
  - output: `tmp/war3-review/micro_feature_d20d-2026-04-13T18-38-29-485Z`

- Validation result for that package now shows:
  - `workspace flavor = tstl-skeleton`
  - `workspace readiness = recognized`
  - `hostTargetValidation = aligned-with-skeleton`

- At the same time, the old per-slot evidence checks still show the real remaining gap:
  - `runtime-hook = workspace-unrecognized`
  - `shop-target = workspace-unrecognized`
  - `trigger-area = workspace-unrecognized`

- This is useful, not contradictory:
  - target hints are now aligned with the TSTL skeleton
  - but per-slot evidence checks are still mostly map-workspace biased

# Next intended moves

- Primary next move:
  - rework the three host-binding slot evidence checks so they can validate against TSTL workspace seams instead of only classic map-script samples

- Suggested order:
  1. runtime-hook against `src/host/bootstrap.ts`
  2. shop-target against generated binding seam
  3. trigger-area against generated rect/region seam

# Current blockers / risks

Primary blocker:

- host target alignment is now in place, but slot-specific validation still assumes map-root evidence too often

Secondary blockers / risks:

- a TSTL skeleton can now be recognized and aligned at the target-hint level, but that must not be overstated as full slot validation

# Notes for other sessions

- Important new distinction:
  - `hostTargetValidation = aligned-with-skeleton` means the review package is pointing at the right TSTL host seams
  - it does **not** mean runtime-hook/shop-target/trigger-area are already validated inside those seams

- The mainline is now clearly past “what host shape should we target?” and into “how do we validate each slot against that host shape?”
