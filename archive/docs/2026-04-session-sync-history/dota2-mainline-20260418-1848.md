# Current focus

Active mainline step: 3. Resolver / readiness / routing

Current slice: Dota2 mainline has moved into V2 governance-first migration, not continued v1 package-6 validation. The active slice is to keep the governance backbone and downstream write pipeline intact while shifting Dota2 planning/control toward `IntentModel -> DesignDraft -> FeatureContract/DependencyEdges -> StrategySelection -> ArtifactSynthesis -> LocalRepair -> Validation/CommitDecision -> Workspace Lifecycle`. The currently landed slice already includes workspace/schema V2 fields, blueprint weak/exploratory continue semantics, resolver exploratory fallback, and an exploratory continuation path for unknown mechanics. That exploratory path is still transitional and currently routes through synthetic `dota2.exploratory_ability`; it is not yet the final synthesis architecture.

# Done since last update

- Reframed Dota2 same-day mainline truth away from v1 package-6 refresh and toward V2 governance-first migration.
- Confirmed and carried forward the current landed slice:
  - workspace/schema V2 fields are already threaded
  - blueprint weak/exploratory continue semantics are already present
  - unknown mechanics can continue into exploratory handling instead of dying at the old pre-generation gate
  - resolver has an exploratory fallback
  - the current exploratory path is the synthetic transitional target `dota2.exploratory_ability`
- Established the migration boundary for other sessions:
  - root baseline docs still remain the repo-level baseline truth
  - for Dota2 V2 work, prefer the new Dota2 V2 migration doc plus the latest Dota2 session-sync
  - old `grammar-v1`, `gap-fill`, and blueprint authority-stack docs remain migration input only, not V2 target truth

# Next intended moves

1. Finish Step 3 by tightening resolver, readiness, and routing around the V2 strategy model and Dota2 host entry surfaces.
2. Replace the synthetic exploratory fallback with a true `ArtifactSynthesis + LocalRepair` loop that can produce owned Dota2 candidate artifacts honestly.
3. Add dependency-driven revalidation so feature-provider changes recheck declared consumers before commit.
4. Decide in a later round whether to continue code migration first or start root baseline ratification; do not half-update the root baseline during this narrow-sync pass.

# Current blockers / risks

- There is still no complete `ArtifactSynthesis + LocalRepair + dependency revalidation` closed loop.
- The current exploratory lane is still transitional, not the final Dota2 V2 synthesis architecture.
- Root baseline docs have not ratified V2 yet, so agents can still be misrouted if they skip the new Dota2 V2 migration doc and the latest session-sync.
- Old grammar/gap-fill vocabulary still exists in the repo and can be misread as target truth if the migration boundary is not stated explicitly.

# Notes for other sessions

- Dota2 current mainline is now Step 3 V2 governance-first migration, not Step 6 v1 package-6 validation.
- Current admitted slice:
  - schema/workspace V2 fields are wired
  - blueprint weak/exploratory continue semantics are wired
  - unknown mechanics can enter the exploratory path
  - exploratory is still a transitional path, not the final synthesis architecture
- For Dota2 V2 work, prefer:
  - [DOTA2-V2-GOVERNANCE-FIRST-ARCHITECTURE.md](/D:/Rune%20Weaver/docs/hosts/dota2/DOTA2-V2-GOVERNANCE-FIRST-ARCHITECTURE.md)
  - the latest Dota2 session-sync note
  before relying on old root baseline wording or `grammar-v1` / `gap-fill` / blueprint authority-stack planning docs.
