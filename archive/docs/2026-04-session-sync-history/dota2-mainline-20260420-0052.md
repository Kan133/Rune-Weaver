# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: the narrow demo-readiness blocker around the exact `talent_draw` create -> update prompt pair has been re-proved on a fresh host, and the create-side `IntentSchema` governance core for that local weighted-selection family is now stable across same-prompt and paraphrase runs. The mainline remains focused on dependency evidence refresh, synthesis grounding hardening, and exploratory graduation, but this note records that the demo pair itself is no longer blocked by create admission drift, source-backed update drift, host bootstrap gaps, or create-side persistence/external drift on the bounded `talent_draw` semantic core.

# Done since last update

- Re-proved the user's exact `talent_draw` create prompt on a fresh copied host (`D:\rw-demo-run-05`) instead of only relying on the canonical lifecycle harness:
  - the create path now ratifies into `selection_pool` family structure instead of falling back to exploratory modules
  - create write succeeded with runtime validation passing
  - host semantic artifacts now include `intent-schema.create.json`, `blueprint.create.json`, and `final-blueprint.create.json`
- Re-proved the user's exact inventory update prompt on the same host:
  - update write succeeded
  - `selection-pool.source.json` preserves `choiceCount = 3`
  - the update lands `inventory.capacity = 16` and `inventory.blockDrawWhenFull = true`
  - host semantic artifacts now include `intent-schema.update.json`, `update-intent.json`, `blueprint.update.json`, and `final-blueprint.update.json`
- Closed the host bootstrap gap that was making the demo look broken even when the feature chain was correct:
  - `adapters/dota2/init/index.ts` now runs `yarn predev` after `yarn install`
  - fresh `dota2 init` now generates the x-template `src/json` artifacts such as `round_settings.json`
  - the exact runtime failure around missing `../json/round_settings.json` no longer reproduces on a newly initialized host
- Pushed the fresh demo host all the way to doctor-green:
  - `dota2 validate --host D:\rw-demo-run-05` passes
  - `dota2 doctor --host D:\rw-demo-run-05` passes after Panorama build artifacts are emitted
  - `yarn prod:panorama` successfully produced the missing HUD artifacts on the demo host
- Closed the create-side `IntentSchema` governance-core stability gap for the bounded local weighted-selection/talent-draw semantic core:
  - `core/wizard/intent-schema.ts` now canonicalizes bounded candidate-draw asks into stable same-feature/session-local semantics instead of allowing persistence/external drift
  - `core/wizard/stability-harness.ts` now tracks governance-core fingerprints and paraphrase-group consistency instead of only mechanics/core-facet drift
  - live wizard harness proof now passes for the exact create prompt plus two paraphrases:
    - `npm run cli -- wizard stability --runs 5 --corpus tmp/wizard-stability-talent-draw-corpus.json --output tmp/wizard-stability-talent-draw.json`
    - each prompt now reports `governance-core variants = 1`
    - paraphrase group `talent_draw_local_selection` now reports `govVariants = 1`
    - aggregate `gov = 100.0%`

# Next intended moves

1. Refresh dependency-driven provider/consumer revalidation evidence on the cleaner planning/update surface.
2. Continue reducing exploratory grounding warnings with harder exact-symbol evidence instead of reopening prompt overfitting.
3. Decide which repeated exploratory outputs are ready to stabilize or graduate into reusable assets.
4. Only reopen `wizard.create` prompt tuning if a new prompt cluster fails after the create-side canonicalization seam, not because of the now-closed talent-draw stability case.

# Current blockers / risks

- The blocker is no longer the exact `talent_draw` demo prompt pair, host semantic artifact export, or the old `selection.choiceCount -> 1` update drift.
- The blocker is also no longer bounded create-side persistence/external drift for the local weighted-selection / `talent_draw` semantic core.
- The mainline blocker is still broader exploratory quality:
  - dependency-driven revalidation needs a fresh proof pass on the converged surface
  - exploratory grounding still needs harder exact-symbol backing on non-family cases
  - exploratory outputs remain `requiresReview=true` until they accumulate enough repeated evidence to stabilize
- The remaining demo-facing risk is manual runtime proof only:
  - CLI/runtime validation is green on `D:\rw-demo-run-05`
  - a real Dota2 Tools pass is still needed to visually confirm `F4` interaction, selection UI behavior, inventory presentation, and the full-inventory block behavior in-game

# Notes for other sessions

- Freshest Dota2 demo-ready host: `D:\rw-demo-run-05`
- Freshest exact-prompt evidence artifacts:
  - `D:\Rune Weaver\tmp\demo-prompt-stability\run-05\create-write.json`
  - `D:\Rune Weaver\tmp\demo-prompt-stability\run-05\update-write.json`
- Freshest create-side stability artifact:
  - `D:\Rune Weaver\tmp\wizard-stability-talent-draw.json`
- Fresh host semantic outputs now exist under:
  - `D:\rw-demo-run-05\game\scripts\src\rune_weaver\features\talent_draw_demo\artifacts\semantic\`
- Source-backed truth for the successful update is at:
  - `D:\rw-demo-run-05\game\scripts\src\rune_weaver\features\talent_draw_demo\selection-pool.source.json`
- Parallel knowledge cleanup should stay in additive staging / provenance mode until the mainline explicitly enters corpus cutover work.
