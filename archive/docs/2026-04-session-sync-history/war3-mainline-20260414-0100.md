# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update landed the first review-oriented War3 TSTL host skeleton so the mainline now has a concrete provisional workspace sample, not only abstract host-shape discussion.

# Done since last update

- Added:
  - `docs/WAR3-TSTL-SKELETON.md`
  - `tmp/war3-tstl-skeleton/`

- The skeleton includes the smallest intended host shape:
  - `package.json`
  - `tsconfig.json`
  - `config.json`
  - `src/main.ts`
  - `src/host/bootstrap.ts`
  - `src/features/setupMidZoneShop.ts`
  - `maps/demo.w3x/`
  - `rune_weaver/workspace.json`
  - `README.md`

- The skeleton is intentionally review-only:
  - no real build
  - no real map sync
  - no claimed KK runtime proof

- The skeleton now gives the War3 track a concrete place to discuss:
  - TS entry seam
  - host bootstrap seam
  - feature draft seam
  - map workspace location
  - RW-owned area

# Next intended moves

- Wait for real KK platform observations and tighten the provisional host contract against them
- If useful, adapt the existing War3 validator so it can inspect this TSTL skeleton shape in addition to map-script-only samples

# Current blockers / risks

Primary blocker:

- the skeleton is honest but still non-runnable, because KK 1.29 runtime specifics and the exact TSTL build/sync path remain unverified

Secondary blockers / risks:

- current `tsconfig.json` expresses authoring shape only, not a proven TSTL compile configuration
- current bootstrap seam is a placeholder and should not be mistaken for a validated runtime entry

# Notes for other sessions

- This is the first concrete TSTL-oriented host sample in-tree for War3.
- It should be treated as a planning and review artifact, not a deployable host.
- This is a better target for future War3 host discussion than continuing to deepen PyWC3 assumptions.
