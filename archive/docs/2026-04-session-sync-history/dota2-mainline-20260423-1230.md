> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after synthesized grounding recovery and exactness tightening landed
> Do not use for: claiming reveal-only exploratory output is stabilized or that all stale exploratory hosts can auto-upgrade

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: explicit synthesized grounding recovery plus producer-side exactness tightening is now landed. Old exploratory hosts only upgrade when preserved raw `metadata.grounding` still exists; otherwise they remain regenerate-only. Fresh-host proof now shows the current grounding producer has removed the earlier reveal-batch helper/type false positives and shrunk the remaining warning to one honest UI-side unknown.

# Done since last update

- Added an explicit `upgrade_workspace_grounding` repair path for synthesized modules that still preserve raw `metadata.grounding`, while keeping validator strict about missing canonical `groundingAssessment`.
- Kept old exploratory hosts honest:
  - `D:\rw-test14` still fails validation because it lacks raw grounding evidence
  - `repair` and `doctor` now say regenerate explicitly instead of pretending a safe auto-fix exists
- Tightened fresh grounding at the producer seam:
  - Lua grounding now ignores same-file helpers and quoted/comment noise before symbol extraction
  - Panorama TSX grounding stays AST-backed and no longer reports type aliases/generics as symbols
  - exact lookup now uses target-profile constraints and allows `lua_ability` exact hits to include host API types such as `Vector`
- Re-proved fresh exploratory outputs:
  - `D:\rw-test22` confirmed provider exact and removed the old reveal-batch `DOTA_I` / `Vector` false positives
  - `D:\rw-test23` is the clean final proof host: provider shell stays exact, reveal-batch now validates with only one remaining review-only unknown (`TextButton`) instead of the older helper/type noise cluster
- Re-ran targeted regression coverage:
  - `npx tsx adapters/dota2/synthesis/index.test.ts`
  - `npx tsx core/retrieval/dota2-bundles.test.ts`
  - `npx tsx core/retrieval/host-symbol-index.test.ts`
  - `npx tsx adapters/dota2/validator/post-generation-validator.test.ts`
  - `npx tsx adapters/dota2/validator/post-generation-repair.test.ts`
  - `npx tsx apps/cli/dota2/commands/doctor-summary.test.ts`
  - `npm run check-types`
- Stable family regression remains clean:
  - `npm run cli -- dota2 validate --host D:\rw-test-1` passes

# Next intended moves

1. Decide whether the remaining `TextButton` unknown on fresh reveal-only UI is a reference-coverage gap or needs prompt/bundle host-anchor shaping.
2. Use the new stale-host split (`upgrade when raw evidence exists`, `regenerate when it does not`) as input to promotion judgment for repeated exploratory outputs.
3. Add or refresh one explicit proof host that exercises the positive upgrade path end to end from raw grounding-only legacy workspace state.

# Current blockers / risks

- Older exploratory hosts without preserved raw grounding are still regenerate-only. `D:\rw-test14` remains the canonical example.
- Reveal-only weighted-card output remains exploratory and review-required; the current fresh-host residual is one UI-side unknown symbol (`TextButton`) on `D:\rw-test23`.
- Provider shell and reveal-batch are still proof cases, not admitted reusable assets.

# Notes for other sessions

- Current stale-host honesty proofs:
  - `D:\rw-test14`
- Fresh-host grounding proof chain:
  - `D:\rw-test22` for the intermediate false-positive drop
  - `D:\rw-test23` for the current final proof
- Current fresh review artifacts:
  - `D:\Rune Weaver\tmp\cli-review\dota2-review-1776918260797.json`
  - `D:\Rune Weaver\tmp\cli-review\dota2-review-1776918503640.json`
- Do not use the older `D:\rw-test21` reveal counts as current grounding truth; that host predates the latest extractor tightening and is now historical comparison evidence only.
