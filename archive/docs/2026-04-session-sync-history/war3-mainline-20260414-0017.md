# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

This update pushed the validator one step further by checking whether the current `runtime-hook` binding has any real host-side evidence in the configured War3 workspace.

# Done since last update

- Extended War3 review package validation to inspect the configured `hostRoot` through the existing War3 workspace connector.

- Added runtime-hook evidence classification in:
  - `adapters/war3/intent/review-package.ts`

- Runtime-hook validation now distinguishes:
  - `host-evidence-found`
  - `map-script-anchor-only`
  - `workspace-unrecognized`

- Current validation inspects the script entry file and looks for narrow current-slice hook anchors such as:
  - `main()`
  - `InitCustomTriggers()`
  - `RunInitializationTriggers()`
  - `CreateTrigger()`

- Re-ran validation against:
  - `tmp/war3-review/micro_feature_55l6-2026-04-13T16-12-22-795Z`

- Result:
  - `valid = yes`
  - `readyForImplementationDraft = no`
  - `openBindings = 3`
  - `runtimeHookEvidence = map-script-anchor-only`
  - `scriptEntry = war3map.lua`

- The sample host now has explicit evidence that:
  - a map-script-level runtime hook seam exists
  - but this is still not a TypeScript project entry proof

# Next intended moves

- Primary next move:
  - decide whether to validate `shop-target` next, or switch to finding / modeling a real TSTL-oriented War3 host project shape

- My recommendation:
  - do not over-invest in deeper runtime-hook proof on the PyWC3 sample
  - instead, treat it as map-output evidence only and move toward one of:
    - `shop-target` declaration-site validation
    - a real TypeScript-to-Lua host project sample / expected workspace shape

# Current blockers / risks

Primary blocker:

- current host evidence is only map-script level
- we still do not have a real TypeScript-to-Lua War3 project entry seam to validate against

Secondary blockers / risks:

- PyWC3 is useful as a map folder / emitted script reference, but it is not the final host model we want for KK + TSTL
- if we keep validating too deeply against PyWC3, we risk proving the wrong host shape

# Notes for other sessions

- This was still a useful narrowing step:
  - runtime-hook is no longer a totally abstract unknown
  - it now has map-script anchor evidence

- But the evidence level matters:
  - `map-script-anchor-only` should not be treated as `host-evidence-found`
  - it is not enough to claim the War3 runtime hook seam is truly validated for the target host model

- Best continuation is likely:
  - validate `shop-target`
  - or obtain / model a true TSTL War3 host workspace shape for KK 1.29
