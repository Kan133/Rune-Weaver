# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow backend seam.

Current slice: carry the already-tightened War3 TSTL skeleton outward into one bounded demo-consumer loop:

- `setup-mid-zone-shop` review artifact chain
- canonical demo case input
- review package export / validation
- planning-only probe input
- probe runner
- bounded probe-result summary

This is still a review-oriented lane, not a write-ready or runtime-proven lane.

# Done since last update

- Tightened `tmp/war3-tstl-skeleton` beyond lightweight placeholder status and fixed the current local review contract around:
  - typed feature contract
  - bootstrap review object
  - machine-readable snapshot
  - single-feature manifest
  - read-only intake seed
  - handoff-prep surface
  - canonical demo case input
  - demo consumer probe
  - bounded demo artifact bridge

- Landed executable War3-local demo/probe helpers at the repo root:
  - `scripts/war3-build-skeleton-probe-input.ts`
  - `scripts/war3-run-skeleton-demo-probe.ts`
  - `scripts/war3-summarize-probe-result.ts`

- Confirmed the first concrete War3 consumer family is now:
  - review package export
  - review package validation
  - bounded probe-input build
  - bounded result summary

- Updated host-specific War3 docs so they now describe the same bounded story:
  - `docs/hosts/war3/WAR3-TSTL-SKELETON.md`
  - `docs/hosts/war3/WAR3-HANDOFF-PROBE.md`
  - `tmp/war3-tstl-skeleton/README.md`

- Re-ran bounded validation:
  - `npm run review` in `tmp/war3-tstl-skeleton`
  - `npm run test` in `tmp/war3-tstl-skeleton`
  - `npm run war3:build-skeleton-probe-input`
  - `npm run war3:build-skeleton-probe-input -- --package-dir <exported-review-package>`

- Tightened `scripts/war3-probe-runner.ts` so membership / request failures are classified as `tool-execution-failure` instead of being treated as a successful probe.

# Next intended moves

1. Restore a usable probe runner environment and rerun:
   - `npm run war3:run-skeleton-demo-probe`

2. Capture one fresh successful end-to-end bounded probe cycle over the current skeleton-generated demo input:
   - exported review package
   - generated probe input
   - probe runner summary
   - bounded review summary
   - evidence ledger

3. Only after a fresh successful probe should War3 choose the next narrow package:
   - tighten prompt / summary heuristics for the same feature
   - or extend the same bounded pattern to exactly one second feature probe

4. Keep any further doc refresh local to War3 host docs and session-sync artifacts unless cross-track routing truth actually changes.

# Current blockers / risks

- The main code-side loop now exists, but the current live blocker is external probe execution availability rather than skeleton shape.
- The latest run of `npm run war3:run-skeleton-demo-probe` failed honestly because the `kimi` side returned a membership verification error.
- War3 still does not have KK 1.29 runtime proof, write-ready host proof, or real TSTL toolchain proof.
- The bounded result summarizer can classify older successful probe outputs, but a fresh successful run is still needed before using the new loop as stronger evidence for the next package choice.

# Notes for other sessions

- War3 is still Step 7, but the active seam has moved forward:
  - no longer “tighten the skeleton from placeholder status”
  - now “use the bounded demo-probe loop as the next honest consumer proof”

- Do not reopen:
  - map naming
  - `warcraft.json` adoption
  - real TSTL toolchain wiring
  - KK runtime mechanism decisions

- The current frozen authoring seam remains:
  - `TypeScript -> TypeScriptToLua -> Lua output`

- The current canonical demo/case input remains:
  - `setup-mid-zone-shop`
  - through the bounded handoff-prep / review-package path

- The next honest blocker is no longer “is there any War3-local consumer seam?”
  - it is “can the bounded demo-probe loop complete successfully with a real probe response and stay within the current review-only claims?”
