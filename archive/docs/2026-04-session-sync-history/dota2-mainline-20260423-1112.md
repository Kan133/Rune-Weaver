> Status: authoritative
> Audience: agents
> Doc family: control
> Update cadence: on-phase-change
> Last verified: 2026-04-23
> Read when: resuming Dota2 mainline after exploratory grounding governance V1 landed
> Do not use for: claiming exploratory outputs are now stabilized or that old exploratory hosts auto-upgraded themselves

# Current focus

Active mainline step: 6. Validation / evidence / case generalization

Current slice: exploratory grounding governance V1 is now landed. Dota2 still produces raw grounding checks inside the adapter, but governance truth is now the canonical `GroundingAssessment` carried at artifact, module, and feature level. Fresh host proof on `D:\rw-test21` shows new-write provider-shell and reveal-batch features can write successfully, remain `requiresReview = true`, and expose the same grounding judgment through workspace persistence, CLI review artifacts, validator output, and workbench bridge data.

# Done since last update

- Added a core grounding governance layer that:
  - normalizes raw `GroundingCheckResult` into canonical `GroundingAssessment`
  - aggregates artifact -> module -> feature grounding truth
  - builds grounding-specific review reasons for final commit
  - validates assessment counts against raw checks
- Extended canonical schema and workspace persistence so synthesized artifacts, modules, and features can carry grounding assessments and grounding summaries as first-class governance data.
- Switched Dota2 synthesis to keep adapter-local raw symbol grounding as evidence only, then produce canonical assessments for:
  - artifact synthesis results
  - module synthesis results
  - workspace feature records
- Extended review and validation surfaces:
  - post-generation validator now checks synthesized grounding governance honesty
  - CLI review artifacts now emit feature grounding summary plus offender list
  - workbench bridge/workspace mirror now carries grounding summary and module-level assessments for display
- Passed targeted regression coverage for the new governance layer:
  - `npx tsx core/governance/grounding.test.ts`
  - `npx tsx core/pipeline/final-commit-gate.test.ts`
  - `npx tsx core/workspace/manager.test.ts`
  - `npx tsx adapters/dota2/synthesis/index.test.ts`
  - `npx tsx adapters/dota2/validator/post-generation-validator.test.ts`
  - `npm run check-types`
  - `npx tsc -b --pretty false` in `apps/workbench-ui`
  - `npx tsx src/data/featureGroupProjection.test.ts` in `apps/workbench-ui`
- Re-proved the new contract on a fresh host:
  - created `D:\rw-test21` from the x-template seed and initialized it successfully
  - provider shell write now succeeds and reports grounding summary `exact`
  - reveal-batch write now succeeds and reports grounding summary `insufficient`
  - `npm run cli -- dota2 validate --host D:\rw-test21` passes
  - `npm run cli -- dota2 doctor --host D:\rw-test21` returns only a Panorama build warning, not a grounding failure
- Regressed a stable family path:
  - `npm run demo:talent-draw -- --host D:\rw-test-1 --write --force` passes
  - `npm run cli -- dota2 validate --host D:\rw-test-1` passes
  - source-backed `selection_pool` family flow is not incorrectly marked as a synthesized grounding risk

# Next intended moves

1. Decide how to handle stale exploratory hosts that predate canonical grounding assessments: regenerate-only, or a narrow upgrader that does not invent truth.
2. Reduce weak and unknown grounding on fresh exploratory outputs, starting with reveal/provider-style non-family continuations that still surface `partial` or `insufficient`.
3. Feed grounding evidence into promotion judgment so repeated exploratory outputs cannot drift toward pseudo-pattern status without explicit proof.

# Current blockers / risks

- Primary blocker: old exploratory hosts now fail validator honestly if they predate the canonical grounding contract. `D:\rw-test14` currently demonstrates this and needs regenerate or equivalent upgrade before it can validate again.
- Existing-host update/regenerate proof for older exploratory cases still hits separate blockers outside this grounding pass:
  - provider-shell update/regenerate on `rw-test14` is still constrained by unrelated semantic and cleanup gates
  - reveal-batch update/regenerate on `rw-test20` still runs into wording/clarification and regenerate-path friction
- `partial` and `insufficient` grounding remain review-only, not write blockers. That is intentional, but it means exploratory write success must not be mistaken for stabilization.
- Reveal-batch and provider-shell remain exploratory outputs. Grounding governance makes them more honest; it does not promote them.

# Notes for other sessions

- Fresh proof artifacts:
  - `D:\Rune Weaver\tmp\cli-review\grounding-provider-rw-test21.json`
  - `D:\Rune Weaver\tmp\cli-review\grounding-reveal-rw-test21.json`
- Fresh proof host:
  - `D:\rw-test21`
- Grounding truth now appears in:
  - `D:\rw-test21\game\scripts\src\rune_weaver\rune-weaver.workspace.json`
  - `D:\Rune Weaver\apps\workbench-ui\public\bridge-workspace.json`
- Stable family regression host:
  - `D:\rw-test-1`
- Important honesty rule after this change:
  - missing canonical assessments on old synthesized modules are validator failures
  - `partial` / `insufficient` assessments on fresh synthesized modules are warnings plus `requiresReview`, not host-write blockers
