# Current focus

War3 mainline remains at Step 7: Product-facing integration, but still only for a narrow validated write-preview seam.

This round did not widen the War3 surface area. It tightened the current seam between:

- current War3 intake artifact
- intent-like meaning
- War3 host-binding payload
- current skeleton preview generator path

The active slice is still the validated mid-zone shop flow, not broad War3 product shell work.

# Done since last update

- Added a narrow War3 bridge module:
  - `adapters/war3/intent/current-slice-bridge.ts`
  - `adapters/war3/intent/index.ts`
- The bridge now derives a real split from the current slice artifact:
  - `intentSchema`
  - `hostBinding`
  - `warnings`
  - `blockers`
- The bridge keeps host facts on the War3 side instead of letting them blur into intent meaning:
  - trigger area materialization center/radius
  - existing shop anchor binding symbol
  - shop unlock mechanism
  - shop order mode / order id
- The bridge keeps unresolved host facts explicit in `hostBinding.unresolvedBindings` instead of letting the model or preview path silently guess them.
- Updated `apps/workbench-ui/vite.config.ts` so `/api/war3/skeleton-preview` now goes through the bridge before calling the War3 skeleton generator.
- The preview path now has a sharper boundary:
  - if the current slice is explicit and compatible, it generates skeleton preview
  - if host-binding facts are still `unknown` or unsupported, it fails with explicit blockers
- Verified:
  - `apps/workbench-ui` build still passes
  - a happy-path sample artifact with explicit `issue-order` + `neutral-target-order-by-id` + existing-anchor inputs produces bridge output and generator input correctly
  - an artifact that still leaves host-binding fields as `unknown` now blocks honestly instead of drifting forward

# Next intended moves

- Use this bridge as the current War3 entry point into the core-style pipeline boundary.
- Decide the next narrow integration move:
  1. expose bridge preview / bridge status in the War3 workbench so the user can see intent-vs-binding separation directly
  2. map the bridge `intentSchema` into a real Blueprint trial and observe whether current schema vocabulary is enough for this slice
  3. add one more narrow consumer that reads `hostBinding` explicitly instead of re-reading raw artifact fields
- Keep the current slice narrow. Do not expand to broad War3 shell or generic cross-host framework work from this seam alone.

# Current blockers / risks

Primary blocker:

- War3 host-binding is now the real blocker, not broad intent ambiguity.
- The main unresolved point is still runtime binding truth, especially:
  - where the anchored shop symbol is declared / sourced in real emitted host code
  - which parts of shop interaction are safely treated as canonical `issue-order` host behavior versus still host-specific assumptions

Secondary blockers / risks:

- current workbench artifact input still allows fields to remain `unknown`, so users can still reach a blocked bridge state
- the current preview path is still one slice only
- repo-level `core/llm` auth remains separately blocked by `401 Invalid Authentication`, but that is not the primary War3 blocker here

# Notes for other sessions

- This round is important because War3 now has a real intermediate seam instead of jumping directly from artifact to generator mapping.
- The current honest chain is now:
  - map workspace
  - derived workspace
  - intake artifact
  - current-slice intent/host-binding bridge
  - skeleton preview generator
  - module draft string
- The bridge is intentionally narrow. It is not a claim that War3 has a general intent pipeline yet.
- If another session wants to continue from here, the most honest next question is:
  - should the next proof be `bridge -> Blueprint` or `bridge -> richer workbench evidence`?
