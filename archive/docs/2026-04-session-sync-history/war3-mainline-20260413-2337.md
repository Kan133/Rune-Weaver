# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow seam.

This round upgraded the current War3 preview from “sidecar plus code text” to a more complete review artifact package driven by the sidecar.

# Done since last update

- Added a new artifact builder:
  - `adapters/war3/intent/write-preview-artifact.ts`
- Exported it from:
  - `adapters/war3/intent/index.ts`
- The new write-preview artifact combines:
  - sidecar-derived summary
  - host-binding manifest
  - review-only write target hints
  - review-only bridge-update hints
  - generated skeleton module draft

- Updated `apps/workbench-ui/vite.config.ts`:
  - `/api/war3/skeleton-preview` now returns:
    - `bridge`
    - `sidecar`
    - `writePreviewArtifact`
    - `content`

- Updated `apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx`:
  - the War3 panel now displays the write-preview artifact summary and manifest sections

- Re-verified:
  - `apps/workbench-ui` build passes
  - direct generation of the write-preview artifact succeeds
  - current artifact summary remains stable:
    - slice `mid-zone-shop`
    - target binding symbol `central_shop_proxy`
    - unresolved binding count `1`

# Next intended moves

- The next strongest move is likely one of:
  1. add an explicit “copy manifest” / “copy review package” interaction in the workbench
  2. generate a real host-binding manifest file artifact in a temp/review path
  3. start validating a second War3 slice if we want to test whether the new seams generalize at all

- If staying on the current slice, the most valuable next move is probably:
  - manifest artifact persistence
  - then runtime-facing review against real War3 host assumptions

# Current blockers / risks

Primary blocker:

- real runtime host binding is still unresolved, especially the final declaration / sourcing of the anchored shop symbol

Secondary blockers / risks:

- the current chain is honest because it stays review-oriented
- it still does not claim host-write readiness or runtime proof
- generic downstream shared pipeline stages remain insufficiently War3-aware

# Notes for other sessions

- Important state change:
  - War3 now has a review package, not just an isolated generator preview
- Current honest chain:
  - intake artifact
  - bridge
  - pre-Blueprint normalization
  - Blueprint trial
  - War3 assembly sidecar
  - sidecar-driven write-preview artifact
  - sidecar-driven skeleton module draft
