# Current focus

Active mainline step: 7. Productization / UX bridge

Current slice: connect the Workbench onboarding shell to the authoritative Dota2 CLI path without creating a second execution system.

This session is focused on the product-entry seam in `apps/workbench-ui`, specifically:

- host path -> addon name / map name capture
- CLI-backed `init`
- CLI-backed `demo prepare`
- CLI-backed `doctor`
- CLI-backed `validate`
- launch contract visibility via `yarn launch <addon> <map>`

Confidence for this slice is based on real repo changes plus local type/smoke verification, not only on mock plans.

# Done since last update

- Extended Workbench host config state in [useFeatureStore.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/hooks/useFeatureStore.ts) to persist:
  - `addonName`
  - `mapName`
- Extended the UI CLI bridge in [useCLIExecutor.ts](/D:/Rune%20Weaver/apps/workbench-ui/src/hooks/useCLIExecutor.ts) to support:
  - `init`
  - `run`
  - `demo-prepare`
  - `doctor`
  - `validate`
- Extended the Vite local API bridge in [vite.config.ts](/D:/Rune%20Weaver/apps/workbench-ui/vite.config.ts) so Workbench can call:
  - `npm run cli -- dota2 demo prepare --host <path> --addon-name <name> --map <map> --write`
  - `npm run cli -- dota2 doctor --host <path>`
  - `npm run cli -- dota2 validate --host <path>`
- Reworked [ProjectSetupPanel.tsx](/D:/Rune%20Weaver/apps/workbench-ui/src/components/project-setup/ProjectSetupPanel.tsx) so the panel now behaves as a real onboarding/product-entry surface instead of placeholder UI:
  - derives addon name from `hostRoot`
  - allows explicit addon override with `snake_case` normalization
  - stores and edits `mapName`
  - shows a launch preview command
  - exposes buttons for `Initialize`, `Demo Prepare`, `Doctor`, `Validate`, and `Create (Dry-run)`
  - refreshes host status after CLI operations that can change readiness
- Re-aligned implementation intent with:
  - [ROADMAP.md](/D:/Rune%20Weaver/docs/ROADMAP.md)
  - [CURRENT-EXECUTION-PLAN.md](/D:/Rune%20Weaver/docs/CURRENT-EXECUTION-PLAN.md)
- Verified this slice with:
  - `npm run check-types`
  - `npx tsx apps/workbench/workbench-smoke.test.ts`

# Next intended moves

1. Surface more authoritative CLI state into Workbench instead of only exposing buttons and raw output:
   - wire `ActionSummary` / next-step guidance into the onboarding/product-entry experience
   - make the UI reflect the real CLI lifecycle state instead of generic local labels
2. Tighten x-template onboarding completion inside the same thin seam:
   - make init/demo-prepare/doctor/validate status transitions more explicit
   - ensure addon/map contract is visible anywhere launch guidance is shown
3. Keep evidence closure honest:
   - if this Workbench slice becomes part of the canonical walkthrough, add explicit evidence for it rather than inferring readiness from CLI-only proof

# Current blockers / risks

Primary blocker: Workbench now dispatches the right Dota2 CLI commands, but it still does not fully present authoritative CLI lifecycle summaries as first-class product state.

Concrete risks:

- Another session could accidentally turn `ProjectSetupPanel` into a large local state machine instead of keeping it as a thin CLI orchestration layer.
- Another session could reintroduce fake/deferred UI labels that drift from real CLI behavior.
- The repo is currently dirty, so anyone editing the same Workbench UI seam should review local changes before stacking more work onto:
  - `apps/workbench-ui/src/components/project-setup/ProjectSetupPanel.tsx`
  - `apps/workbench-ui/src/hooks/useCLIExecutor.ts`
  - `apps/workbench-ui/src/hooks/useFeatureStore.ts`
  - `apps/workbench-ui/vite.config.ts`

# Notes for other sessions

- Dota2 mainline should still be treated as being on the roadmap-critical path:
  - evidence quality for standing lifecycle claims
  - product entry integration into authoritative CLI
  - x-template onboarding completion
  - one honest canonical walkthrough
- Do not reopen “second runnable case” work from this note. `dash-strike` remains planned only and should not become a hidden active track.
- Workbench is still not the authoritative lifecycle executor. It is an onboarding/orchestration shell over CLI truth.
- If another session continues from here, the clean next seam is:
  - Workbench rendering of CLI-backed next-step / action-summary state
  - not deeper generator changes
  - not unrelated UI redesign
  - not broad War3 crossover
