# Current focus

War3 mainline remains at Step 7: Product-facing integration on a narrow seam.

This round moved the current War3 review package from “in-memory preview bundle” to a real exported artifact set on disk.

# Done since last update

- Added a pure backend exporter in:
  - `adapters/war3/intent/review-package.ts`
- Exported from:
  - `adapters/war3/intent/index.ts`

- The exporter now builds and writes a current-slice War3 review package containing:
  - `package.json`
  - `README.md`
  - `bridge.json`
  - `sidecar.json`
  - `write-preview.json`
  - `setupMidZoneShop.ts`

- Default export root:
  - `tmp/war3-review/`

- Verified by exporting the current sample case to:
  - `tmp/war3-review/micro_feature_3e5w-2026-04-13T15-43-00-980Z`

- Verified the package summary content:
  - slice: `mid-zone-shop`
  - blueprint id: `micro_feature_3e5w`
  - target binding symbol: `central_shop_proxy`
  - unresolved binding count: `1`

- Re-verified `apps/workbench-ui` build and it passes.

# Next intended moves

- The next useful move is likely one of:
  1. add a backend command/helper that exports a review package from a given War3 artifact path without going through UI
  2. make the host-binding manifest richer and more formal
  3. start runtime-facing review against a real War3 project/workspace using the exported package as the handoff unit

- The strongest immediate move is probably:
  - backend export command/helper
  - then host-binding manifest tightening

# Current blockers / risks

Primary blocker:

- runtime host binding is still unresolved, especially anchored shop symbol sourcing/declaration

Secondary blockers / risks:

- exported package is review-ready, not runtime-proven
- current pipeline honesty still depends on local War3 seams rather than shared downstream compatibility

# Notes for other sessions

- Important truth change:
  - War3 now has a persisted review package on disk, not just UI-visible previews
- Current honest chain:
  - intake artifact
  - bridge
  - pre-Blueprint normalization
  - Blueprint trial
  - War3 assembly sidecar
  - write-preview artifact
  - exported review package
