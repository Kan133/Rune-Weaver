# Current focus

War3 mainline is currently in the first real handoff-validation step after parser/workbench bring-up.

Exact slice:

- validate that the War3 host workspace can turn real map inputs into a stable intake artifact
- validate that the intake artifact can turn into a dry-run LLM handoff bundle
- validate whether that bundle is strong enough for a narrow implementation-planning response

# Done since last update

- Landed real parsing for `war3map.w3e`, `war3mapunits.doo`, `war3map.doo`, and `war3map.w3i` against a real sample folder map:
  - sample used: `tmp/war3-samples/PyWC3/maps/test.w3x`
- Verified real parser outputs are now sane:
  - `w3e`: dimensions and offsets are usable
  - `units.doo`: v8.9 count and sample anchors are usable
  - `doo`: sample doodad records are usable
  - `w3i`: map summary is now real-parsed, not placeholder
- War3 workbench UI now exports a structured intake artifact:
  - contract file: `apps/workbench-ui/src/lib/war3-intake.ts`
  - UI wiring: `apps/workbench-ui/src/components/war3/War3AnchorPanel.tsx`
- War3 workbench UI now generates a dry-run handoff bundle:
  - contract/builder: `apps/workbench-ui/src/lib/war3-handoff.ts`
  - local preview endpoint: `POST /api/war3/handoff-preview`
- Verified `apps/workbench-ui` builds successfully after these changes.
- Ran a real dry-run model-quality test with the War3 bundle:
  - repo `core/llm` path reached the remote request layer but failed with `401 Invalid Authentication` from the configured provider
  - `kimi --no-thinking` was then used on the same bundle and returned a usable implementation-planning answer
- Re-ran the planning and typed probes through `scripts/war3-probe-runner.ts` using shorter ASCII prompt files and `--max-steps-per-turn 3`:
  - `planning-control` returned `model-output-success`
  - `typed-short` returned `model-output-success`
- Confirmed the earlier typed probe failure was tool-execution-bounded by a step limit, not clear evidence of model incapacity.
- Confirmed the current runner path is validated on short prompts and can return both planning-level and pseudo-implementation-level evidence.

# Next intended moves

- Tighten the short War3 probe boundary so runtime-specific guesses are more constrained:
  - helper lookup assumptions
  - conservative hint-display path
  - exact shop-unlock mechanism
  - per-player versus shared hint semantics
- Keep using short constrained prompts as evidence probes rather than promoting the path to formal generation.
- Restore the repo-level `core/llm` call path when credentials are usable again so the same hardened prompts can be exercised through the intended execution surface.

# Current blockers / risks

Primary blocker:

- the repo-level `core/llm` provider configuration is not currently usable for real execution because the configured credential returned `401 Invalid Authentication`

Additional risks:

- current bundle and runner quality are good enough for short planning and pseudo-implementation probes, but the prompt boundary still allows project-specific runtime guesses that are not yet trustworthy for direct code generation
- manual anchor ordering is still not a true cross-type global order; the current confirmed list is assembled as `anchor -> doodad -> manual`
- the real sample validated parser shape, but assumptions about KK runtime shop interaction are still not verified in an actual game/runtime loop

# Notes for other sessions

- War3 mainline has moved past “parser skeleton” status; this track now has:
  - real parser coverage for the current P0 files
  - real UI artifact export
  - real dry-run handoff bundle generation
- The current strongest validated chain is:
  - real folder map -> derived workspace view -> intake artifact -> handoff bundle -> `kimi --no-thinking` planning output
- The runner itself is no longer the main uncertainty for short prompts:
  - short ASCII probe files now returned `model-output-success` for both planning and typed-short cases
  - the main remaining uncertainty is prompt-boundary control around runtime assumptions
- The main unverified assumptions are now runtime-facing, not parser-facing:
  - exact KK/1.29 shop interaction realization
  - exact player targeting semantics for welcome hint
  - exact conservative TypeScript-to-Lua runtime path we want the model to emit
- If an orchestrator updates the shared plan, it should treat War3 mainline as being in:
  - “artifact + handoff contract hardening”
  - not “blank host investigation”
  - not yet “runtime-proven implementation execution”
  - not yet “formal generation promotion”

## 2026-04-13 conservative-v2 typed probe

- status: `model-output-success`
- input: `D:/Rune Weaver/tmp/war3-typed-probe-input-20260413-short-conservative-v2-case.txt`
- result: `D:/Rune Weaver/tmp/kimi-war3/typed-conservative-v2-2026-04-13T13-04-33-598Z.final.txt`

Short constrained prompts now have a more honest conservative shape than the earlier `typed-short` probe.

- helper API invention was reduced substantially
- frame/UI invention was removed
- ownership-mutation unlock assumption was removed
- unresolved runtime needs are surfaced directly as `UNSPECIFIED IN PROMPT` / missing host bindings

Trust-boundary judgment: better, but still not clean. The model still emitted placeholder helpers such as `isHumanPlayer`, `getOwningPlayer`, `showWelcomeHint`, `unlockShopForPlayer`, `createEnterRegionTrigger`, `registerUnitEntersTrigger`, and `startTimer`. Those are now visibly stubbed rather than smuggled in as confident logic, which is useful, but they still mark structural invention at the boundary.

Next move: decide whether to tighten the prompt one more time to suppress even these placeholders, or start mapping the exposed placeholders to real host bindings for trigger, timer, ownership query, hint display, and shop unlock.
## Addendum - 2026-04-13 grounded native-whitelist probe

Recorded a new probe note at `D:/Rune Weaver/docs/war3-probes/grounded-native-whitelist-v1-2026-04-13.md`.

This is the best-quality native-grounded probe so far. The whitelist-grounded prompt holds the model on direct natives (`CreateTrigger`, `TriggerRegisterEnterRegion`, `TriggerAddAction`, `GetEnteringUnit`, `GetOwningPlayer`, `GetPlayerController`, `GetPlayerSlotState`, `DisplayTimedTextToPlayer`) and suppresses helper-wrapper invention much better than earlier runs.

The remaining open decision is now narrower and concrete:

1. trigger-zone handle source / area realization policy
2. shop unlock native/mechanism

The run is still not fully boundary-clean because it fills in a concrete hint string and `5.0` duration on its own. Treat this as the current best probe result, not final generation.
